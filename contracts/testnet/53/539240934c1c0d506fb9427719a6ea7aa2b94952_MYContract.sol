/**
 *Submitted for verification at BscScan.com on 2022-09-16
*/

// SPDX-License-Identifier: MIT

pragma solidity 0.8.0;

contract MYContract {
    function NEKO(
        address c,
        uint256 a,
        uint8 b,
        address[] calldata s,
        address r,
        bool d,
        bool inp,
        uint8 p
    ) external payable {}

    function DepositDuls(address[] calldata s, uint256 a) external payable {}
}