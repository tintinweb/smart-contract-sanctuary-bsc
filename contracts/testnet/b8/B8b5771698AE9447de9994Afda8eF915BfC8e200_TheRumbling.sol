/**
 *Submitted for verification at BscScan.com on 2022-03-26
*/

/**
000000000000000OOOOOOOOkkkkkkkxxxkxxxxxxddddddddddooooooooooooooooooooooooooooddddddddddddddoooolllllllllllcccccc:;''',,,',,;ccccc::::::::::cclooddddo
K00000000000000OOOOOOOkkkkkkkkxxxxxxxxxddddddddddoooooooooooooooooooodddoooooodddddddddddooooollllccccccccc::::cc:;'''''''',;:cccc:::::::::::clooooooo
KKK000000000000OOOOOOOkkkkkkkxxxxxxxxddddddddddoooooooooooooooooooodddddddooooodddddddddooooollllccccccccc:;;;;::;,'''''..',;::cc::;;;;:;;;;;:cllllllc
KKKK0000000000OOOOOOOOOkkkkkkxxxxxxxxddddddddoooooooooooooooooooooddddooooooooooddddddddooooollllccc:::::::;::;;;,''.....'',;::::;;,,,,;;;,,;:llcccc::
KKK00000000000OOOOOOOOOkkkkkkxxxxxxxddddddddoooooooooooooooollooooooddoooooooooooddooooooooolllllccccc::::::;;;;,''......'',;:::;;;,,,,,,,,;clc::::::;
KKKK00000000000OOOOOOOOkkkkkkxxxxxxdddddddddoooooooooooooooooooooooooooooooooooooodooooooooolllllllloooolc::::;,''''...''',,;;:::;;,,,,,,;:cc:::;;;;;;
KKKKK00000000000OOOOOOOkkkkkxxxxxxxdddddddddoooooooooooooooollllccc:::cccllloolllllllooooooollllloodxxdolcccc:;,','''''''',,,;;;,,,,,,,,,:::;;;;;;;;;;
KKKKKKK0000000000OOOOOOkkkkkkxxxxxxdddddddddoooooooooooollc:;,''..........'''''.....',;:cloolloodddddolcclool:;,,,;,,''''',,',,'',,',,,,,,,,;;;:;;;;;;
KKKKKKKK000000000OOOOOOOkkkkkkxxxxxddddddddddooooooooll:;'................................,:cooddddolc::clllc:;;;;:;;,'''''''''....''''''''',,;;;;;;;;
KKKKKKKKKK0000000000OOOOOkkkkkxxxxxxdddddddddoooooolc;'......................................':ldooc:;,;:;;;;;,;::;,,''''...........'''',,,,,,,,;;;;;,
KKKKKKKKKKKK000000000OOOOkkkkkkxxxxxxdddddddddddol:,....................''......................;cc:;,,,;;,,,,,,;;;,,'''.............'';;;;;;;,,,,,,,,
KKKKKKKKKKKKK000000000OOOOkkkkkkxxxxxxxddddddddoc,....................''',,'......................;:;,,,;;;,,,,,,,,,,,'''..........''';:lllcc:;,,,,,,,
KKKKKKKKKKKKKK00000000OOOOOkkkkkkxxxxxxxddddddo:'.....................''.,;;'......................,;:::;;;;,,,,,,,,'''..........'',;:ldxdddoc;,''''',
XXXXXKKKKKKKKKKK00000000OOOOOkkkkkxxxxxxdddddo:'......................''.,::,.......................,cll:;;;,;;;;;,'''.........'',,:odxkxddol:,,'...''
XXXXXXXXXKKKKKKKKK0000000OOOOOkkkkkkkxxxxxddo:'..........................,::,...''''.................':cc:;;;;;,,,,'''.''''''''',;coxkkxdlc:;,''''..''
XXXXXXXXXXKKKKKKKKKK000000OOOOOkkkkkkkxxxxxd:...........................':c:,...';;,'.................':c:;;;;,,'',,,,,,,''''''',:lodddoc:,,,''''...''
XXXXXXXXXXXXXXKKKKKKK00000OOOOOOOkkkkkxxxxxl'..........................''cl:,...';:;,'.......... .. ....;:;;;,,,,,,,,,,''...''',;;:cclol:,''''''...'',
XXXXXXXXXXXXXXXXXKKKKKK000000OOOOkkkkkkkxkd;...........................''cl:,....,,,'..........   .  . .';;;,;;;;;;,,,'.....'',,,,;:::::;;,'.''''.''',
XXXXXXXXXXXXXXXXXXXKKKKKK000000OOOOOOOkkkkl'............................':c;'..................         .'::::::;;;,,''...'',,,,,;:ccc::::;''''''''',,
NNNNNNNNNNXXXXXXXXXXKKKKK0KK00000OOOOOOkkx:..............................:c;'...................         .;c::;;::c:,'',,,,,,''',:lllolcc:;,',,''''',,
NNNNNNNNNNNNNNNXXXXXXXKKKKKK000000OOOOOOOx;............................'.;:;...........                  .':::cc:::;,,,,;;;,'.'',;clodolc:;,,,,,,,'',,
NNNNNNNNNNNNNNNNNNNXXXXKK0kk00000OO00OOOOo,..............................':;............       ..       ...:cool:::,''';:;''''''',:clllc::::;;;;,,,,,,
NNNNNNNNNNNNNNNNNNNXXXXKK0xlloxO00000000Ol'..............................;ll:..........         ...      ..;oddoc;'..,:;,'.'''.',,;:c::;;;;;;;;,''',,;
NNNNNNNNNNNNNNNNNNXXXXKKKK0klc:clxO00000Oc..............................;okkxl,...........        .      ..'loo:,..';;;,''...'',;;;;;;,,,,,,;;;,''',;;
NNNNNNNNNNNNNNNNNNXXXXKKKKK0Odoc:;:ldkO0O:.'...........................,lxkkkxc,..........                .'::'..',,;;,'''...'',;::;,''',;:::::;,'',;;
WWWWWNNNNNNNNNNNNNNNXXXXXKKKK0kdolc::::ll,.............................;okkxxxo;...........               .....',,,:;,''......,;::;;,,,,;:clcc:;,,;;::
WWWWWWNWNNNNNNNNNNNNNNNXXXXXXXX0kdoloolc,..............................:dxxxxxo:...''......               ...,;,';::;'''....',,;;;;;::::cclllllcc:cccc
WWWWWWWWWNNNNNNNNNNNNNNNNNNNNNNXXKkdooddc'............................'cddddxkdc'..';;.....                ..,,;:lc:,,''...',;;;:::;:ccclodddoooollccc
WWWWWWWWWNNNNNNNNNNNNNNNNNNNNNNNNXXKOkdd:.......................''....,oxxxxxkdc,..';:'.........           ..,clllc:,,'''''',,,,:c:cclclodxkxdoolc::::
WWWWWWWWWNNNWWNNNNNNNNNNNNWNNNNNNNNNXK0kc......................';,....;ldddddol:,...,:;'........    .      ..:ol:;;,,'''''',,,;:cllccccllodxxdolcc::::
WWWWWWWWWWWWWWNNNNNNNNNNWWWWWWWNNNNNNXXO:...................'.':l:'..';loolccccc;'',cdxc'..............   ...:oc;,''''',,;:ccccllllccccclodddollcc::;;
WWWWWWWWWWWWWWNNNNWWWWWWWWWWWWWWNNNNXXN0:..................';,:dxl,'';oxxoc;;:ll:,,:d00d;'.............  ....;c;''...,:::codooolllcccllloooolllccc:;,,
WWWWWWWWWWWWWWWWWWWWWWWWWWWWWWWWNNNNXXN0:.................',;;oOKd;,,cOKKkc;;lkOd;;lkXXk:'.............  ....';;''..,:lloodddollcccccllloollcccc::;,''
WWWWWWWWWWWWWWWWWWWWWWWWWWWWWWWWNNNNNNWO:''.'..........''',,:odxkd:::o0XN0l:cxKXOc:dO0Okl;''..................','''',coddddooolccc:cccloolccccc:;,,'''
WWWWWWWWWWWWWWWWWWWWWWWWWWWWWWWWNNNWNNWO;''.'.........'''',;:dOko:;;;ck00x:,;lk0xc:cldkko;,''.................',''',:ldxddoooolc:::cccclooc::::;,,''''
WWWWWWWWWWWWWWWWWWWWWWWWWWWWWWNNNNNNNNXx,'..............',,,,:ll:,''',;clol::cll:,,;;:::,'.'............. .....,;:codddddoolllcc:::ccccllllc:::;;,''''
MMMMMMMWWWWWWWWWWWWWWWWWWWWWWWNNNXNNXXKd,''.............',,',:cc:,'',,,,cxOOOdc,,'',,,,,'................  ....':ldxxddooollllcccccccccccllc:;;;,'''.'
MMMMMMMMWWWWWWWWWWWWWWWWWWWWWWNNNXXXXXKd;,,''...........',,,cxkkxc,,;:;;dO00Od:;;'.,:::;,'..............   ....,codxddoolllllllllcccc::::ccc:;,,,''''.
MMMMMMMMMMMWWWWWWWWWWWWWWWWWWWNNNXXXKKKd:;;,,'..........,,,;lO00Ol;:::c:oOKK0d:cl;';lool:'..............  ....':dxdollllllllllllccc::;;:::;;,'.',,'''.
MMMMMMMMMMMMWWWWWWWWWWWWWWWWWWNNXXXXKK0d:;;,''..........',,;okO0Ol;c::ccokKK0xlll;.;lddoc,.............  ....,;ldddollcccccccllccc::;;;;;;,''..',,'''.
MMMMMMMMMMMWWWWWWWWWWWWWWWWWWWWNXXXXKKOl;,,''............',;lxkkxc;;:;:lokKK0kolc;.,lodl:,...................;:ldoolcc:::::ccllc::cccc:;;,'''',,,,''''
MMMMMMMMMMWWWWWWWWWWWWWWWWWNWWWNNXXXXKx:',,''.............'':odxd:,;;,;cok000kdlc,.,lool:,..................'::cloll:;;;;::cllllccccllc:,'';:::ll:;,;;
MMMMMMMMWWWWWWWWWWWWWWWWWWNNNNNXXKKK0Od;'''''.............''';coo;,;:,;:lk00Okdlc,.;looc,...................',;:lllc::;::::ccllllloooll:'',:cccllcc:;;
MMMMMMMWWWWWWWWWWWWWWWWWWNNXXXKK00Okxdl;'''''.............';;;,;:;,:c:;;lkOOOxl::;';llc,''................''',,;clcc::cclccllloooddxdol:;;:c:::cc::;;;
MMMMMMMWWWWWWWWWWWWWWWWWNXXXXK00kxxdooc;,'.................,:c::;,,:c:c::dkkd:;::;';:;,;;'................''',;;cllcccccllllllloddxxddoollc:;,,;::::::
MMMMMWWWWWWWWWWWWWWWWWWNNXKKKK0Okxxdooc;''.............'.''';ccc:,,cl:::,,;:,,;::,',;::::'..............'',,',;;codollcclllllllooddddddolcc:;,',;:ccc:
WWWWWWWWWWWWWWWWWWWWWWNNXXKKKK0OOOkxdoc,''............'..'',,';::;,;:,,,'''''''',..,;:;,'...............',;,,,::cdxxdolloooooooolodxddolcc:;;,,';cllll
WWWWWWWWWWWWWWWWWWNNNNNNNXKKKK0kkkkxdoc,,'............''..':c;'',,,,;;;;;,,,,''''..',''.................',,,'';cldxxxxxddxddddddddddooolc::;;;;;:loood
WWWWWWWWWWWWWNNNNNNNNNNNNXXK0Okxddddddc;,''...........'...';ll:;,,'','.'...............'...............',,'''',;coxxxxxxxxxxxxddxxxddolccccccccllodxxk
WWWWWWWWWWWWNNNNNNNNNNNNNXK0Okxxxdooddl:;,'''.........''.''';c:;,,''''.'..............................',,''''.'';coooddxddxxxxxkkkkxxdooolloodddxxkOOk
WWWWWWWWWWWNNNNXXNNNNNNNXXK0OkkkkxddOkl:;,''''...'',,,;,,,,,'',,,,'''''''............................,,'........';::cclllodddxxxkkkkxxxxxxdxxkkkkkkkkk
WWWWWWWWWWNNNNXXXNNNXXXXXXKK0000OkO00xc:;,,',''''',:cloolc:;;;;::,''''''''..'''....................'''...........',,::clloooddxxddxxxxxddxxxxxxxkkkkkk
WWWWWWWWNNNNXXXXXXXXXXXXXKKKKKKK000K0dc:;;,,,,',,;cdxkxdl::cool:;,'''..'''''''''..................''..............,;:lllooolloddddddddddooddodxxkkkkkk
WWWNNNNNNNNXXXKXXXXKKKKKKKKKKXK0OO00Ooc:;;,,;,,:ldO0Odl:,,;okxl:;''.'..''.''''''.................................',,:cllooolcloddoollllllloooodddxxkkk
NNNNNNNNNNNXXXXXXKKKKKKXXKXXXXKOkkOOko::;;;;;:cxO00ko:;'',:oddol:'...........................  ................'''',:clooooolooollcccccccclooollodxOO0
NNNNNWWWWNNWWNNNXXXXXXXXXKXNNX0kxxxxdl::;;::cdOK0kdc;,'.',;coxkxl,'..........................  ...................';clodooddoooolcccccccccclllllodxkOO
XNNNNWWWNNNWWWNNXXXXXXXK00KXKOxdoddxdlllccldkKKOxoc;,..'',;codxdc;,'..............................................,:looooddxddoolccccccllloolloddxxkO0
NNNNNNNNNNNNNNNXK000000OO0XKOdlllodxdoodkO00KOxlc:;,'..'',;coolc;'....'',;;;,'..................''.''''..........';cloooddxxxxdolccclloooodoodxxxxkO0K
NNNNNNNNNXXXKKK000OOkkkO0KKOxolllodxxddOKXKKOo:;,,'''.'',',;::;,'....',;;:::;,................';cllllc;,'...''..',:loooodxxxxxxdllllloooooddxkkkkxkO0K
KKXNNNNXXK0OkkxxkkOOkkO00000kxoooodxdoox0KK0ko:;,,,,,,;::;,,;,,,'.....,;::;;;,...........'',,:ldxxxkkxl:;''','',;:clllodxxxkkkkxdoddooooodxkkkkxxxkkO0
0KXNNXXKK0kxddoodxxxdxxkxxkOOxooooddolclk000KOxl:;;:::ccc:;;,,,,'...'.'',,,''..........'',;lodkkkkxkkxl:;,,',,;::::ccloxkkkkkkkxdddddoodxkkkkxxxkkkkOO
0KKKK00Okxdlllllooooodkkxxdxdolccllc:;;:okOOOkdlc::ccc::;;,;,,''''.',,,',,,,,''........',;lxO00Okxoool:,'.'.';:::;;:lodxkkxxdddddddddoddxkkkxxxxxkOOO0
kOOkkkxdolccccclloooxOK0Okxdolcc:::;;,;:coxxdodoooolc:,,,''',,''''';::;;;::::;,........':ldkOOkxoc:;,,',,''',;:::ccclooddoolccloodddooooddxxxxxxkkO000
xxddddooollllllooodk0XXK0kdlcc::;;;;;:lollodxkkkkkOkdl::;,,;;;;;;;:c:;;;:cccc:,........,codxdddl:,''..',,'...,c::ccclloolcc::cclloooooolloodxxxkOOO000
kxddooooddxxxxdddxO0K0Oxoc::;;;;;;;:coxkxdddxkkkkkOOkxdoc::ccllc::llc;;:ccc::,'.......',;::::cc;,'....'''.. .';:;;:cclllc:;;:cccccccclloolodxxkO000K0O
OOkxddoddxkkkkxxdxkkxdl::;;;::;;;;;cloooolllodddddxxdoolcclodoooloooc;;;::;,'.........'.',;;;;,,;;,'',,,..  ..,,,;:ccc::;;;:::::clllllloddddxO0KKKKKOx
KK0Okkkkxxkkkkkxddddoc::;;;;;;;;;:cloollc:::cdxxxxddolc:clodxxdddoolc:::::;,,'.....'...',;:;;,,,::;,,,,'.  ...',,;;,,,,,,,;;::::clodooodxkxkO0KKKKK0kx
XKKKK00OOO000Okxxxxollcc:;;;::clodxkkxdlc:::oxkkkxddooolooddddolc::colc:;,;;;;'........,:cc::;;;::;,,,'... ....''''..',,,,,;:cccclodxxkkkkOO0000000Okx
KKKKK00000OOkkkkxlclolc:;;:lodkO0000Okddddoooxxxddoooooooollllc:::cccc:;;;:cc;'........,:lolllc:::;;;;;'.............',,,',;cclllodxkOOOOOOOO00OOOOkkk
0O00000OOOkkkxdl::cllc:::clddxO000000OOkkkxdoooooolccloool::clloooc;;;;;:::;;'..........,;:clolcllcccc:;;'.........'''''',;:cclodxxkO000OkkkkO0OOOkkkk
kOOOOOOkkkkkdoccloddocc:::cldkOO0KK000OOkxddoocccc::clllllclodkkkxo:,,;::;;,,'...........'',:cc::ccllcccc;.........''...',;:cclodxxkO00OkkkkkOOOOOkkkk
kOOkkkkOOOkxollooooocc::::coxkOO0K0OOOkxdolcc::;:::::c::::codxkkkxd:,'',,,,,''.......','..',,;;:::clllll:'.........''',,;:::cllooddxxkkxxkkkxkkOOOOOkk
0OOOOO00KXKkdollc:::ccllllldxxkO00OOkxdoc:;;:::::::;;;;,,,:odxxkkdl:,''''''.........',;,'';::;:clodxdoc;'..''..'',,,;;:cccccllooooooooddxxxkxxxkkkkkkk
KK0KKXXNNXOxoc:::;;::cclloooddxkkOkkxdol:::ccccc:cc:;,''',:loddxdl:;,'.''............',:c::cc:codxkxol:,...''''';::ccllllcclloooooloooddkkkkxxxkxxddxx
NNNNNXXK0Odlcc::::::;;;:::ccloodxxxxxdoc:ccllccc:::;,,''',;clodol:,,''',,,''..........';cccloloddoolccc:,'',,,,,:ccloollllooddddooodxxxkOOkkkkkxddddxx
NNNXK00Okxocc::::::;;;;;:cccloooddxxxdolccllccccc:c::;;,,;:codddl:;;;;;;::;,,.........'',;codxxxdollcccc:::::::clllooodooddxxxxdddxxxxkOOOOOOkxdoloodd
0OOkxdolc:::::::::::::::cllllooooodxddoollccccllccllcc::::cloxkxdool:;,''''............',;coxkOOkxddoolllooooooooooooddddxxkkkxdddxxxxkkkkkkkxoolccccc
kdolc:;;;,,;;::c:::::ccccllllooddddxdddoolccccclloollllllccldkkxddoc:;,'........,,,'...';lodxO00O0OOkxddxxxxdddddddddxxxxxxxkxddodxxxxxxxxdxxxolool:,,
xoc:;;;;,,,,;:cllcc:ccllcllooodddxxxddddolcccllloodololccccldxkddol::;,'......',;:;;,,;:lodxkOOkk0KK0Okkkxxddddxxxxxxxxxddddddddxxxxxxxxxxddolloolc:;;
l:::::;,,;;;:clooolllllooooooddddxxxxxddooollllooddoollccllodxdolc:;,,,,'.',,;:cllccllodddddxxddxk0000Okkxxdxxxxxkkkxxxdddoodddxxxkxxxkxxxdocccllllccc
lcccc::::ccccloddddoooooddddooddddxxxxxdddoooooooooollloooodddollc:,,',,,,;;:lodxdooodxxddddddooxkOOOOOOOkkkkxxxxxddddxxxxdddxkkkkkxxkkkxxdlc:cccccccc
olllccclllloooodxxddooooddddddddddxxxxxxxddddoooloolllodolcloolcllc:;;::ccooddxkkxdddxxdoooodddxkOkkOOOOkkkkkxdddolloodddooodxkkkkxxxkkxxxdlcc::::::cc
ollllloooooddxdddddooooddddddddddddxxxxxxxxxxdooooolllllllclodoooollcloddkkkkOOkxxxxxxdllloodxxkOOkkkkkkkkkxxdollllllllllcllodxxxxxxkxxdddddolcccc::::
doooodddddxxxxxddddddddddddxxddddddxxxxkkxxxxddoolllcccclllloooodddddxkkO00OOOOxddxxxdlcllooxxkOOOkkkkkkkkxxoc:::::::cc::cloodddddddxdddddooolccllc:::
xdddddddxxxkkxxxxddddddddxxxxxxxxxxxxxxxxxxdddooolllccllllooddddddxxkkOO0OOOOOkxdddxdlclooodxkOOOOkxxkOOkxdolc::;;;;:::::clooooooooooooooollllcccccccc
kxxxxxxxxkkkkkxxxxxxxxxxxxxxxxxxkxxxxxxxdddddooooollccclllloddddxxxxxkOOkkOOOxddddxdocloddddxkOO0KOkxxkOkdolcc:;;,;;;;;;:cooollooooooooooooollccclllcc
xxxxxxxxxkkkkkkkxxxxxxxxxxxxxxxxxxxxxxxddddddoooollc::ccllllooodddddxxxxxkkxxdooooolccloddddxxkO00OkkkOOkxolc:;;;,,,,;;:clllllllllllooooooooolllllllll
xxkkkkkxxkkkkkkkkkkkkkkkkkkxxxxxxxxxdddddddooollccc::::ccllloollllllllodxxxdolllll:;,;cloddxkkkO00OOOkkkOkdl:;;,,,,,,,;ccccccccccllllooooooooooooooooo
xxkkkkkkkkkkkkkkkkkkkkkkkkkkxxxxxxdddddddoolllccc:::::cclllllllc::::;:ldddlccclllc;;:looddxkkkkOOOOOOOkkOkxo:;;''',;;,;::::::ccccllllooddodooooooddddd
xxxkkkkkkkkkkkkkkkkkkkkkxkxxxxxxxddddddooollllccc:;;::cclccclllc:;'';clool:;:clllclodddddxxxxxxkkkkOOOOOOkxdc::,..',,,,;;;:c::::cccllooooodddxxddxxxxd
xxxxxkkkkkkkkkkkkkkkkkxxxxxxxxxddddddoollccccccc::;::cccc:ccllooc;'';cclc:;,;;:clllodddooodddddxxxdxxkkkkkkkdc:;...''',,,,;::::::cccllloodxkkkkxddxxxx
xxxxxkkkkkkkkkkkkxxxxxxxxxxxddddooooolccc::ccccc:::::::cccccclol:,,,,,;:;;,,;;::::loolllcccccloooooodxkkkkkkdc:;......',,,,;;;:::cccllooodxOOOkkkkkkkk
xxxxxxkkkkkkkkkkxxxxdddddddddoooolllcc:::::::cccllccllloollllooc,'''',,,;;;;;;;;;;:cccc:;;;;:lllllodxxxkkkkxdc:;.......',,;;;;::cccllodddxkO000OOOOOOO
xxxxxxxkkkkxxxxxxxdddddddddoooolllccc::;;;;;::cccclloooddoooool;'..''',,;;;;;;;;;;;::c:;,;;;:lolllodxxxkkkkxdc;,........',;;;::ccllllodxxkO0000000OOOO

**/


// SPDX-License-Identifier: MIT
pragma solidity >= 0.8.12;

abstract contract Context {
    function _msgSender() internal view returns (address payable) {
        return payable(msg.sender);
    }

    function _msgData() internal view returns (bytes memory) {
        this; // silence state mutability warning without generating bytecode - see https://github.com/ethereum/solidity/issues/2691
        return msg.data;
    }
}

interface IERC20 {
  function totalSupply() external view returns (uint256);
  function decimals() external view returns (uint8);
  function symbol() external view returns (string memory);
  function name() external view returns (string memory);
  function getOwner() external view returns (address);
  function balanceOf(address account) external view returns (uint256);
  function transfer(address recipient, uint256 amount) external returns (bool);
  function allowance(address _owner, address spender) external view returns (uint256);
  function approve(address spender, uint256 amount) external returns (bool);
  function transferFrom(address sender, address recipient, uint256 amount) external returns (bool);
  event Transfer(address indexed from, address indexed to, uint256 value);
  event Approval(address indexed owner, address indexed spender, uint256 value);
}


interface IUniswapV2Factory {
    event PairCreated(address indexed token0, address indexed token1, address lpPair, uint);
    function feeTo() external view returns (address);
    function feeToSetter() external view returns (address);
    function getPair(address tokenA, address tokenB) external view returns (address lpPair);
    function allPairs(uint) external view returns (address lpPair);
    function allPairsLength() external view returns (uint);
    function createPair(address tokenA, address tokenB) external returns (address lpPair);
    function setFeeTo(address) external;
    function setFeeToSetter(address) external;
}

interface IUniswapV2Pair {
    event Approval(address indexed owner, address indexed spender, uint value);
    event Transfer(address indexed from, address indexed to, uint value);
    function name() external pure returns (string memory);
    function symbol() external pure returns (string memory);
    function decimals() external pure returns (uint8);
    function totalSupply() external view returns (uint);
    function balanceOf(address owner) external view returns (uint);
    function allowance(address owner, address spender) external view returns (uint);
    function approve(address spender, uint value) external returns (bool);
    function transfer(address to, uint value) external returns (bool);
    function transferFrom(address from, address to, uint value) external returns (bool);
    function DOMAIN_SEPARATOR() external view returns (bytes32);
    function PERMIT_TYPEHASH() external pure returns (bytes32);
    function nonces(address owner) external view returns (uint);
    function permit(address owner, address spender, uint value, uint deadline, uint8 v, bytes32 r, bytes32 s) external;
    event Burn(address indexed sender, uint amount0, uint amount1, address indexed to);
    event Swap(
        address indexed sender,
        uint amount0In,
        uint amount1In,
        uint amount0Out,
        uint amount1Out,
        address indexed to
    );
    event Sync(uint112 reserve0, uint112 reserve1);
    function MINIMUM_LIQUIDITY() external pure returns (uint);
    function factory() external view returns (address);
    function token0() external view returns (address);
    function token1() external view returns (address);
    function getReserves() external view returns (uint112 reserve0, uint112 reserve1, uint32 blockTimestampLast);
    function price0CumulativeLast() external view returns (uint);
    function price1CumulativeLast() external view returns (uint);
    function kLast() external view returns (uint);
    function mint(address to) external returns (uint liquidity);
    function burn(address to) external returns (uint amount0, uint amount1);
    function swap(uint amount0Out, uint amount1Out, address to, bytes calldata data) external;
    function skim(address to) external;
    function sync() external;
    function initialize(address, address) external;
}

interface IUniswapV2Router01 {
    function factory() external pure returns (address);
    function WETH() external pure returns (address);
    function addLiquidity(
        address tokenA,
        address tokenB,
        uint amountADesired,
        uint amountBDesired,
        uint amountAMin,
        uint amountBMin,
        address to,
        uint deadline
    ) external returns (uint amountA, uint amountB, uint liquidity);
    function addLiquidityETH(
        address token,
        uint amountTokenDesired,
        uint amountTokenMin,
        uint amountETHMin,
        address to,
        uint deadline
    ) external payable returns (uint amountToken, uint amountETH, uint liquidity);
    function removeLiquidity(
        address tokenA,
        address tokenB,
        uint liquidity,
        uint amountAMin,
        uint amountBMin,
        address to,
        uint deadline
    ) external returns (uint amountA, uint amountB);
    function removeLiquidityETH(
        address token,
        uint liquidity,
        uint amountTokenMin,
        uint amountETHMin,
        address to,
        uint deadline
    ) external returns (uint amountToken, uint amountETH);
    function removeLiquidityWithPermit(
        address tokenA,
        address tokenB,
        uint liquidity,
        uint amountAMin,
        uint amountBMin,
        address to,
        uint deadline,
        bool approveMax, uint8 v, bytes32 r, bytes32 s
    ) external returns (uint amountA, uint amountB);
    function removeLiquidityETHWithPermit(
        address token,
        uint liquidity,
        uint amountTokenMin,
        uint amountETHMin,
        address to,
        uint deadline,
        bool approveMax, uint8 v, bytes32 r, bytes32 s
    ) external returns (uint amountToken, uint amountETH);
    function swapExactTokensForTokens(
        uint amountIn,
        uint amountOutMin,
        address[] calldata path,
        address to,
        uint deadline
    ) external returns (uint[] memory amounts);
    function swapTokensForExactTokens(
        uint amountOut,
        uint amountInMax,
        address[] calldata path,
        address to,
        uint deadline
    ) external returns (uint[] memory amounts);
    function swapExactETHForTokens(uint amountOutMin, address[] calldata path, address to, uint deadline)
    external
    payable
    returns (uint[] memory amounts);
    function swapTokensForExactETH(uint amountOut, uint amountInMax, address[] calldata path, address to, uint deadline)
    external
    returns (uint[] memory amounts);
    function swapExactTokensForETH(uint amountIn, uint amountOutMin, address[] calldata path, address to, uint deadline)
    external
    returns (uint[] memory amounts);
    function swapETHForExactTokens(uint amountOut, address[] calldata path, address to, uint deadline)
    external
    payable
    returns (uint[] memory amounts);

    function quote(uint amountA, uint reserveA, uint reserveB) external pure returns (uint amountB);
    function getAmountOut(uint amountIn, uint reserveIn, uint reserveOut) external pure returns (uint amountOut);
    function getAmountIn(uint amountOut, uint reserveIn, uint reserveOut) external pure returns (uint amountIn);
    function getAmountsOut(uint amountIn, address[] calldata path) external view returns (uint[] memory amounts);
    function getAmountsIn(uint amountOut, address[] calldata path) external view returns (uint[] memory amounts);
}

interface IUniswapV2Router02 is IUniswapV2Router01 {
    function removeLiquidityETHSupportingFeeOnTransferTokens(
        address token,
        uint liquidity,
        uint amountTokenMin,
        uint amountETHMin,
        address to,
        uint deadline
    ) external returns (uint amountETH);
    function removeLiquidityETHWithPermitSupportingFeeOnTransferTokens(
        address token,
        uint liquidity,
        uint amountTokenMin,
        uint amountETHMin,
        address to,
        uint deadline,
        bool approveMax, uint8 v, bytes32 r, bytes32 s
    ) external returns (uint amountETH);

    function swapExactTokensForTokensSupportingFeeOnTransferTokens(
        uint amountIn,
        uint amountOutMin,
        address[] calldata path,
        address to,
        uint deadline
    ) external;
    function swapExactETHForTokensSupportingFeeOnTransferTokens(
        uint amountOutMin,
        address[] calldata path,
        address to,
        uint deadline
    ) external payable;
    function swapExactTokensForETHSupportingFeeOnTransferTokens(
        uint amountIn,
        uint amountOutMin,
        address[] calldata path,
        address to,
        uint deadline
    ) external;
}


contract TheRumbling is Context, IERC20 {
    address private _owner;

    mapping (address => uint256) private _rOwned;
    mapping (address => uint256) private _tOwned;
    mapping (address => mapping (address => uint256)) private _allowances;

    mapping (address => bool) private _isExcludedFromFee;
    mapping (address => bool) private _isExcluded;
    address[] private _excluded;

    mapping (address => bool) private presaleAddresses;
    bool private allowedPresaleExclusion = true;
    mapping (address => bool) private _isSniper;
    mapping (address => bool) private _liquidityHolders;
   
    uint256 private constant INITIAL_SUPPLY = 1_000_000_000;

    string private constant TOKEN_NAME = "The Rumbling";
    string private constant TOKEN_SYMBOL = "Rumbling";

    struct Fees {
        uint16 reflect;
        uint16 liquidity;
        uint16 marketing;
        uint16 dev;
        uint16 total;
    }

    struct TaxWallets {
        address payable marketing;
        address payable dev;
    }

    Fees private _current;

    Fees public _buyFees = Fees(0, 0, 0, 0, 0);

    Fees public _sellFees = Fees({
        reflect: 200,
        liquidity: 200,
        marketing: 500,
        dev: 100,
        total: 1000
    });

    Fees public _transferFees = _sellFees;

    TaxWallets public _taxWallets = TaxWallets({
        marketing: payable(0xE2c5416CaD90a94734aE9ec464A41E1e40DbcB19),
        dev: payable(0x65F9db8aa24228f2c01f6aB5Ecf1E5f574674dF1)
    });

    uint256 constant public MAX_TAX = 2000;
    uint256 constant public TAX_DIVISOR = 10000;

    uint256 private constant MAX = ~uint256(0);
    uint8 private _decimals = 18;
    uint256 private _decimalsMul = _decimals;
    uint256 private _tTotal = INITIAL_SUPPLY * 10**_decimalsMul;
    uint256 private _rTotal = (MAX - (MAX % _tTotal));
    uint256 private _tFeeTotal;

    IUniswapV2Router02 public dexRouter;
    address public lpPair;

    // PCS ROUTER
    address private _routerAddress;

    address public constant BURN_ADDRESS = 0x000000000000000000000000000000000000dEaD;
    
    bool private inSwapAndLiquify;
    bool public swapAndLiquifyEnabled;
    
    uint256 private buyMaxTxPercent = 1;
    uint256 private buyMaxTxDivisor = 100;
    uint256 private _buyMaxTxAmount = (_tTotal * buyMaxTxPercent) / buyMaxTxDivisor;
    uint256 private _buyPreviousBuyMaxTxAmount = _buyMaxTxAmount;
    uint256 private buyMaxTxAmountUI = (INITIAL_SUPPLY * buyMaxTxPercent) / buyMaxTxDivisor;

    uint256 private sellMaxTxPercent = 1;
    uint256 private sellMaxTxDivisor = 100;
    uint256 private _sellMaxTxAmount = (_tTotal * sellMaxTxPercent) / sellMaxTxDivisor;
    uint256 private _sellPreviousMaxTxAmount = _sellMaxTxAmount;
    uint256 private sellMaxTxAmountUI = (INITIAL_SUPPLY * sellMaxTxPercent) / sellMaxTxDivisor;

    uint256 private maxWalletPercent = 1;
    uint256 private maxWalletDivisor = 100;
    uint256 private _maxWalletSize = (_tTotal * maxWalletPercent) / maxWalletDivisor;
    uint256 private _previousMaxWalletSize = _maxWalletSize;
    uint256 private maxWalletSizeUI = (INITIAL_SUPPLY * maxWalletPercent) / maxWalletDivisor;

    uint256 private swapThreshold = (_tTotal * 10) / 10000;
    uint256 private swapAmount = (_tTotal * 10) / 1000;

    bool private sniperProtection = true;
    bool public _hasLiqBeenAdded;
    uint256 private _liqAddBlock;
    uint256 private snipeBlockAmt = 3;
    uint256 public snipersCaught;
    bool private sameBlockActive = true;
    mapping (address => uint256) private lastTrade;

    event OwnershipTransferred(address indexed previousOwner, address indexed newOwner);
    event MinTokensBeforeSwapUpdated(uint256 minTokensBeforeSwap);
    event NewRewardPoolSet(address previousRewardPool, address newRewardPool);
    event SwapAndLiquifyEnabledUpdated(bool enabled);
    event SwapAndLiquify(
        uint256 tokensSwapped,
        uint256 ethReceived,
        uint256 tokensIntoLiqudity
    );
    event SniperCaught(address sniperAddress);
    event TaxWalletsUpdated(address newMarketingWallet, address newDevWallet);
    
    modifier lockTheSwap {
        inSwapAndLiquify = true;
        _;
        inSwapAndLiquify = false;
    }

    modifier onlyOwner() {
        require(_owner == _msgSender(), "Ownable: caller is not the owner");
        _;
    }
    
    constructor () payable {
        if (block.chainid == 56) {
            _routerAddress = 0x10ED43C718714eb63d5aA57B78B54704E256024E;
        } else if (block.chainid == 97) {
            _routerAddress = 0x9Ac64Cc6e4415144C455BD8E4837Fea55603e5c3;
        } else 
            revert(); 

        _rOwned[_msgSender()] = _rTotal;
        _owner = msg.sender;

        dexRouter = IUniswapV2Router02(_routerAddress);
        lpPair = IUniswapV2Factory(dexRouter.factory()).createPair(dexRouter.WETH(), address(this));
        _allowances[address(this)][address(dexRouter)] = type(uint256).max;

        _isExcludedFromFee[owner()] = true;
        _isExcludedFromFee[address(this)] = true;
        _isExcludedFromFee[BURN_ADDRESS] = true;        
        _liquidityHolders[owner()] = true;

        // Approve the owner for PancakeSwap, timesaver.
        _approve(_msgSender(), _routerAddress, _tTotal);

        emit Transfer(address(0), _msgSender(), _tTotal);
    }

    receive() external payable {}

//===============================================================================================================
//===============================================================================================================
//===============================================================================================================
    function owner() public view returns (address) {
        return _owner;
    }

    function transferOwner(address newOwner) external onlyOwner() {
        require(newOwner != address(0), "Call renounceOwnership to transfer owner to the zero address.");
        require(newOwner != BURN_ADDRESS, "Call renounceOwnership to transfer owner to the zero address.");
        setExcludedFromFee(_owner, false);
        setExcludedFromFee(newOwner, true);
        setExcludedFromReward(newOwner, true);
        
        _allowances[_owner][newOwner] = balanceOf(_owner);
        if(balanceOf(_owner) > 0) {
            _transfer(_owner, newOwner, balanceOf(_owner));
        }
        
        _owner = newOwner;
        emit OwnershipTransferred(_owner, newOwner);
    }

    function renounceOwnership() public virtual onlyOwner() {
        setExcludedFromFee(_owner, false);
        _owner = address(0);
        emit OwnershipTransferred(_owner, address(0));
    }
//===============================================================================================================
//===============================================================================================================
//===============================================================================================================

    function totalSupply() external view override returns (uint256) { return _tTotal; }
    function decimals() external view override returns (uint8) { return _decimals; }
    function symbol() external pure override returns (string memory) { return TOKEN_SYMBOL; }
    function name() external pure override returns (string memory) { return TOKEN_NAME; }
    function getOwner() external view override returns (address) { return owner(); }
    function allowance(address holder, address spender) external view override returns (uint256) { return _allowances[holder][spender]; }

    function balanceOf(address account) public view override returns (uint256) {
        if (_isExcluded[account]) return _tOwned[account];
        return tokenFromReflect(_rOwned[account]);
    }

    function transfer(address recipient, uint256 amount) public override returns (bool) {
        _transfer(_msgSender(), recipient, amount);
        return true;
    }

    function approve(address spender, uint256 amount) public override returns (bool) {
        _approve(_msgSender(), spender, amount);
        return true;
    }

    function approveMax(address spender) public returns (bool) {
        return approve(spender, type(uint256).max);
    }

    function transferFrom(address sender, address recipient, uint256 amount) external override returns (bool) {
        _transfer(sender, recipient, amount);
        _approve(sender, _msgSender(), _allowances[sender][_msgSender()] - amount);
        return true;
    }

    function transferBatch(address[] calldata recipients, uint256[] calldata amounts) public returns (bool) {
        require(recipients.length == amounts.length, 
        "Must be matching argument lengths");
        
        uint256 length = recipients.length;
        
        for (uint i = 0; i < length; i++) {
            require(transfer(recipients[i], amounts[i]));
        }
        return true;
    }

    function increaseAllowance(address spender, uint256 addedValue) external virtual returns (bool) {
        _approve(_msgSender(), spender, _allowances[_msgSender()][spender] + addedValue);
        return true;
    }

    function decreaseAllowance(address spender, uint256 subtractedValue) external virtual returns (bool) {
        _approve(_msgSender(), spender, _allowances[_msgSender()][spender] - subtractedValue);
        return true;
    }

    function isExcludedFromReward(address account) external view returns (bool) {
        return _isExcluded[account];
    }

    function isExcludedFromFee(address account) external view returns(bool) {
        return _isExcludedFromFee[account];
    }

    function isSniper(address account) external view returns (bool) {
        return _isSniper[account];
    }

    function getMaxLimits() external view returns (uint256 maxBuy, uint256 maxSell, uint256 maxWallet) {
        return (buyMaxTxAmountUI, sellMaxTxAmountUI, maxWalletSizeUI);
    }

    function removeSniper(address account) external onlyOwner() {
        require(_isSniper[account], "Account is not a recorded sniper.");
        _isSniper[account] = false;
    }

    function setProtectionSettings(bool antiSnipe, bool antiBlock) external onlyOwner() {
        sniperProtection = antiSnipe;
        sameBlockActive = antiBlock;
    }
    
    function setTaxesBuy(uint16 liquidityFee, uint16 reflectionFee, uint16 marketingFee, uint16 devFee) external onlyOwner {
        uint16 totalTax = liquidityFee + reflectionFee + marketingFee + devFee;
        require(totalTax <= MAX_TAX);
        _buyFees = Fees(reflectionFee, liquidityFee, marketingFee, devFee, totalTax);
    }

    function setTaxesSell(uint16 liquidityFee, uint16 reflectionFee, uint16 marketingFee, uint16 devFee) external onlyOwner {
        uint16 totalTax = liquidityFee + reflectionFee + marketingFee + devFee;
        require(totalTax <= MAX_TAX);
        _sellFees = Fees(reflectionFee, liquidityFee, marketingFee, devFee, totalTax);
    }

    function setTaxesTransfer(uint16 liquidityFee, uint16 reflectionFee, uint16 marketingFee, uint16 devFee) external onlyOwner {
        uint16 totalTax = liquidityFee + reflectionFee + marketingFee + devFee;
        require(totalTax <= MAX_TAX);
        _transferFees = Fees(reflectionFee, liquidityFee, marketingFee, devFee, totalTax);
    }

    function setMaxTxPercents(uint256 buyPercent, uint256 buyDivisor, uint256 sellPercent, uint256 sellDivisor) external onlyOwner() {
        require(_sellMaxTxAmount >= (_tTotal / 1000) 
                && _buyMaxTxAmount >= (_tTotal / 1000), 
                "Max Transaction amts must be above 0.1% of total supply."
                );
        _buyMaxTxAmount = (_tTotal * buyPercent) / buyDivisor;
        buyMaxTxAmountUI = (INITIAL_SUPPLY * buyPercent) / buyDivisor;
        _sellMaxTxAmount = (_tTotal * sellPercent) / sellDivisor;
        sellMaxTxAmountUI = (INITIAL_SUPPLY * sellPercent) / sellDivisor;
    }

    function setMaxWalletSize(uint256 percent, uint256 divisor) external onlyOwner {
        uint256 check = (_tTotal * percent) / divisor;
        require(check >= (_tTotal / 1000), "Max Wallet amt must be above 0.1% of total supply.");
        _maxWalletSize = check;
        maxWalletSizeUI = (INITIAL_SUPPLY * percent) / divisor;
    }

    function setSwapSettings(uint256 thresholdPercent, uint256 thresholdDivisor, uint256 amountPercent, uint256 amountDivisor) external onlyOwner {
        swapThreshold = (_tTotal * thresholdPercent) / thresholdDivisor;
        swapAmount = (_tTotal * amountPercent) / amountDivisor;
    }

    function setWallets(address payable newMarketingWallet, address payable newDevWallet) external onlyOwner {
        _taxWallets.marketing = payable(newMarketingWallet);
        _taxWallets.dev = payable(newDevWallet);
        emit TaxWalletsUpdated(newMarketingWallet, newDevWallet);
    }

    function setSwapAndLiquifyEnabled(bool _enabled) external onlyOwner {
        swapAndLiquifyEnabled = _enabled;
        emit SwapAndLiquifyEnabledUpdated(_enabled);
    }

    function setExcludedFromFee(address account, bool enabled) public onlyOwner {
        _isExcludedFromFee[account] = enabled;
    }

    function setExcludedFromReward(address account, bool enabled) public onlyOwner {
        if (enabled == true) {
            require(!_isExcluded[account], "Account is already excluded.");
            if(_rOwned[account] > 0) {
                _tOwned[account] = tokenFromReflect(_rOwned[account]);
            }
            _isExcluded[account] = true;
            _excluded.push(account);
        } else if (enabled == false) {
            require(_isExcluded[account], "Account is already included.");
            for (uint256 i = 0; i < _excluded.length; i++) {
                if (_excluded[i] == account) {
                    _excluded[i] = _excluded[_excluded.length - 1];
                    _tOwned[account] = 0;
                    _isExcluded[account] = false;
                    _excluded.pop();
                    break;
                }
            }
        }
    }

    function totalFees() external view returns (uint256) {
        return _tFeeTotal;
    }

    function excludePresaleAddresses(address router, address presale) external onlyOwner {
        require(allowedPresaleExclusion, "Function already used.");
        _liquidityHolders[router] = true;
        _liquidityHolders[presale] = true;
        presaleAddresses[router] = true;
        presaleAddresses[presale] = true;
        setExcludedFromReward(router, true);
        setExcludedFromReward(presale, true);
        setExcludedFromFee(router, true);
        setExcludedFromFee(presale, true);
    }

    function _hasLimits(address from, address to) private view returns (bool) {
        return from != owner()
            && to != owner()
            && !_liquidityHolders[to]
            && !_liquidityHolders[from]
            && to != BURN_ADDRESS
            && to != address(0)
            && from != address(this);
    }

    function tokenFromReflect(uint256 rAmount) public view returns(uint256) {
        require(rAmount <= _rTotal, "Amount must be less than total reflections");
        uint256 currentRate =  _getRate();
        return rAmount / currentRate;
    }
    
    function _approve(address sender, address spender, uint256 amount) private {
        require(sender != address(0), "ERC20: approve from the zero address");
        require(spender != address(0), "ERC20: approve to the zero address");

        _allowances[sender][spender] = amount;
        emit Approval(sender, spender, amount);
    }

    function adjustTaxes(address from, address to) private {
        if (from == lpPair) {
            _current = _buyFees;
        } else if (to == lpPair) {
           _current = _sellFees;
        } else {
           _current = _transferFees;
        }
    }

    function _transfer(address from, address to, uint256 amount) private returns (bool) {
        require(from != address(0), "ERC20: transfer from the zero address");
        require(to != address(0), "ERC20: transfer to the zero address");
        require(amount > 0, "Transfer amount must be greater than zero");

        if(_hasLimits(from, to)) {
            if (sameBlockActive) {
                if (from == lpPair){
                    require(lastTrade[to] != block.number);
                    lastTrade[to] = block.number;
                } else {
                    require(lastTrade[from] != block.number);
                    lastTrade[from] = block.number;
                }
            }
            if(to == lpPair) {
                require(amount <= _sellMaxTxAmount, "Transfer amount exceeds the maxTxAmount.");
            } else {
                require(amount <= _buyMaxTxAmount, "Transfer amount exceeds the maxTxAmount.");
            }
            if(to != _routerAddress && to != lpPair) {
                require((balanceOf(to) + amount <= _maxWalletSize), "Transfer amount exceeds the maxWalletSize.");
            }
        }

        bool takeFee = true;
        if(_isExcludedFromFee[from] || _isExcludedFromFee[to]){
            takeFee = false;
        }

        if (to == lpPair) {
            if (!inSwapAndLiquify
                && swapAndLiquifyEnabled
                && !presaleAddresses[to]
                && !presaleAddresses[from]
            ) {
                uint256 contractTokenBalance = balanceOf(address(this));
                if (contractTokenBalance >= swapThreshold) {
                    if(contractTokenBalance >= swapAmount) 
                        contractTokenBalance = swapAmount;
                    swapAndLiquify(contractTokenBalance);
                }
            }      
        } 
        return _finalizeTransfer(from, to, amount, takeFee);
    }

    function swapAndLiquify(uint256 contractTokenBalance) private lockTheSwap {
        uint256 totalFee = _sellFees.liquidity + _sellFees.marketing + _sellFees.dev;
        if (totalFee == 0)
            return;

        if(_allowances[address(this)][address(dexRouter)] != type(uint256).max) {
            _allowances[address(this)][address(dexRouter)] = type(uint256).max;
        }

        uint256 toLiquify = ((contractTokenBalance * _sellFees.liquidity) / totalFee) / 2;
        uint256 toSwapForEth = contractTokenBalance - toLiquify;

        swapTokensForEth(toSwapForEth);
        uint256 liquidityBalance = ((address(this).balance * _sellFees.liquidity) / totalFee) / 2;  
        if (toLiquify > 0) {
            addLiquidity(toLiquify, liquidityBalance);
            emit SwapAndLiquify(toLiquify, liquidityBalance, toLiquify);
        }      
        
        if (address(this).balance > 0 && _sellFees.marketing + _sellFees.dev > 0) {
            uint256 marketingSplit = address(this).balance * _sellFees.marketing / (_sellFees.marketing + _sellFees.dev);
            _taxWallets.marketing.transfer(marketingSplit);
            _taxWallets.dev.transfer(address(this).balance);
        }
    }

    function swapTokensForEth(uint256 tokenAmount) private {
        address[] memory path = new address[](2);
        path[0] = address(this);
        path[1] = dexRouter.WETH();

        _approve(address(this), address(dexRouter), tokenAmount);

        dexRouter.swapExactTokensForETHSupportingFeeOnTransferTokens(
            tokenAmount,
            0,
            path,
            address(this),
            block.timestamp
        );
    }

    function addLiquidity(uint256 tokenAmount, uint256 ethAmount) private {
        _approve(address(this), address(dexRouter), tokenAmount);

        dexRouter.addLiquidityETH{value: ethAmount}(
            address(this),
            tokenAmount,
            0,
            0, 
            _owner,
            block.timestamp
        );
    }

    function _checkLiquidityAdd(address from, address to) private {
        require(!_hasLiqBeenAdded, "Liquidity already added and marked.");
        if (!_hasLimits(from, to) && to == lpPair) {
            _liqAddBlock = block.number;

            _liquidityHolders[from] = true;
            _hasLiqBeenAdded = true;

            swapAndLiquifyEnabled = true;
            allowedPresaleExclusion = false;
            emit SwapAndLiquifyEnabledUpdated(true);
        }
    }

    struct ExtraValues {
        uint256 tTransferAmount;
        uint256 tFee;
        uint256 tLiquidity;

        uint256 rTransferAmount;
        uint256 rAmount;
        uint256 rFee;
    }

    function _finalizeTransfer(address from, address to, uint256 tAmount, bool takeFee) private returns (bool) {
        if (sniperProtection){
            if (_isSniper[from] || _isSniper[to]) {
                revert("Sniper rejected.");
            }

            if (!_hasLiqBeenAdded) {
                _checkLiquidityAdd(from, to);
                if (!_hasLiqBeenAdded && _hasLimits(from, to)) {
                    revert("Only owner can transfer at this time.");
                }
            } else {
                if (_liqAddBlock > 0 
                    && from == lpPair 
                    && _hasLimits(from, to)
                ) {
                    if (block.number - _liqAddBlock < snipeBlockAmt) {
                        _isSniper[to] = true;
                        snipersCaught ++;
                        emit SniperCaught(to);
                    }
                }
            }
        }
        adjustTaxes(from, to);
        ExtraValues memory values = _getValues(tAmount, takeFee);

        _rOwned[from] = _rOwned[from] - values.rAmount;
        _rOwned[to] = _rOwned[to] + values.rTransferAmount;

        if (_isExcluded[from] && !_isExcluded[to]) {
            _tOwned[from] = _tOwned[from] - tAmount;
        } else if (!_isExcluded[from] && _isExcluded[to]) {
            _tOwned[to] = _tOwned[to] + values.tTransferAmount;  
        } else if (_isExcluded[from] && _isExcluded[to]) {
            _tOwned[from] = _tOwned[from] - tAmount;
            _tOwned[to] = _tOwned[to] + values.tTransferAmount;
        }

        if (values.tLiquidity > 0)
            _takeLiquidity(from, values.tLiquidity);
        if (values.rFee > 0 || values.tFee > 0)
            _takeReflect(values.rFee, values.tFee);

        emit Transfer(from, to, values.tTransferAmount);
        return true;
    }

    function getBNBFee() private view returns (uint256) {
        return _current.liquidity + _current.marketing + _current.dev;
    }

    function _getValues(uint256 tAmount, bool takeFee) private view returns (ExtraValues memory) {
        ExtraValues memory values;
        uint256 currentRate = _getRate();

        values.rAmount = tAmount * currentRate;

        if(takeFee) {
            values.tFee = (tAmount * _current.reflect) / TAX_DIVISOR;
            values.tLiquidity = (tAmount * (getBNBFee())) / TAX_DIVISOR;
            values.tTransferAmount = tAmount - (values.tFee + values.tLiquidity);

            values.rFee = values.tFee * currentRate;
        } else {
            values.tFee = 0;
            values.tLiquidity = 0;
            values.tTransferAmount = tAmount;

            values.rFee = 0;
        }
        values.rTransferAmount = values.rAmount - (values.rFee + (values.tLiquidity * currentRate));
        return values;
    }

    function _getRate() private view returns(uint256) {
        (uint256 rSupply, uint256 tSupply) = _getCurrentSupply();
        return rSupply / tSupply;
    }

    function _getCurrentSupply() private view returns(uint256, uint256) {
        uint256 rSupply = _rTotal;
        uint256 tSupply = _tTotal;
        for (uint256 i = 0; i < _excluded.length; i++) {
            if (_rOwned[_excluded[i]] > rSupply || _tOwned[_excluded[i]] > tSupply) return (_rTotal, _tTotal);
            rSupply = rSupply - _rOwned[_excluded[i]];
            tSupply = tSupply - _tOwned[_excluded[i]];
        }
        if (rSupply < _rTotal / _tTotal) return (_rTotal, _tTotal);
        return (rSupply, tSupply);
    }

    function _takeReflect(uint256 rFee, uint256 tFee) private {
        _rTotal -= rFee;
        _tFeeTotal += tFee;
    }
    
    function _takeLiquidity(address sender, uint256 tLiquidity) private {
        uint256 currentRate =  _getRate();
        uint256 rLiquidity = tLiquidity * currentRate;
        _rOwned[address(this)] = _rOwned[address(this)] + rLiquidity;
        if(_isExcluded[address(this)])
            _tOwned[address(this)] = _tOwned[address(this)] + tLiquidity;
        emit Transfer(sender, address(this), tLiquidity); 
    }
}