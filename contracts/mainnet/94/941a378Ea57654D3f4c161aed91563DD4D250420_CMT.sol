// SPDX-License-Identifier: <SKO>

/*
                                                                                                                                                                                                                                                                                                                                          
                                                                                                                                                                                                                                                                                                                                          
        CCCCCCCCCCCCChhhhhhh                                                                   lllllll                           tttt                                   MMMMMMMM               MMMMMMMM  iiii  lllllll kkkkkkkk                TTTTTTTTTTTTTTTTTTTTTTT              kkkkkkkk                                              
     CCC::::::::::::Ch:::::h                                                                   l:::::l                        ttt:::t                                   M:::::::M             M:::::::M i::::i l:::::l k::::::k                T:::::::::::::::::::::T              k::::::k                                              
   CC:::::::::::::::Ch:::::h                                                                   l:::::l                        t:::::t                                   M::::::::M           M::::::::M  iiii  l:::::l k::::::k                T:::::::::::::::::::::T              k::::::k                                              
  C:::::CCCCCCCC::::Ch:::::h                                                                   l:::::l                        t:::::t                                   M:::::::::M         M:::::::::M        l:::::l k::::::k                T:::::TT:::::::TT:::::T              k::::::k                                              
 C:::::C       CCCCCC h::::h hhhhh          ooooooooooo       cccccccccccccccc   ooooooooooo    l::::l   aaaaaaaaaaaaa  ttttttt:::::ttttttt        eeeeeeeeeeee         M::::::::::M       M::::::::::Miiiiiii  l::::l  k:::::k    kkkkkkk     TTTTTT  T:::::T  TTTTTTooooooooooo    k:::::k    kkkkkkk eeeeeeeeeeee    nnnn  nnnnnnnn    
C:::::C               h::::hh:::::hhh     oo:::::::::::oo   cc:::::::::::::::c oo:::::::::::oo  l::::l   a::::::::::::a t:::::::::::::::::t      ee::::::::::::ee       M:::::::::::M     M:::::::::::Mi:::::i  l::::l  k:::::k   k:::::k              T:::::T      oo:::::::::::oo  k:::::k   k:::::kee::::::::::::ee  n:::nn::::::::nn  
C:::::C               h::::::::::::::hh  o:::::::::::::::o c:::::::::::::::::co:::::::::::::::o l::::l   aaaaaaaaa:::::at:::::::::::::::::t     e::::::eeeee:::::ee     M:::::::M::::M   M::::M:::::::M i::::i  l::::l  k:::::k  k:::::k               T:::::T     o:::::::::::::::o k:::::k  k:::::ke::::::eeeee:::::een::::::::::::::nn 
C:::::C               h:::::::hhh::::::h o:::::ooooo:::::oc:::::::cccccc:::::co:::::ooooo:::::o l::::l            a::::atttttt:::::::tttttt    e::::::e     e:::::e     M::::::M M::::M M::::M M::::::M i::::i  l::::l  k:::::k k:::::k                T:::::T     o:::::ooooo:::::o k:::::k k:::::ke::::::e     e:::::enn:::::::::::::::n
C:::::C               h::::::h   h::::::ho::::o     o::::oc::::::c     ccccccco::::o     o::::o l::::l     aaaaaaa:::::a      t:::::t          e:::::::eeeee::::::e     M::::::M  M::::M::::M  M::::::M i::::i  l::::l  k::::::k:::::k                 T:::::T     o::::o     o::::o k::::::k:::::k e:::::::eeeee::::::e  n:::::nnnn:::::n
C:::::C               h:::::h     h:::::ho::::o     o::::oc:::::c             o::::o     o::::o l::::l   aa::::::::::::a      t:::::t          e:::::::::::::::::e      M::::::M   M:::::::M   M::::::M i::::i  l::::l  k:::::::::::k                  T:::::T     o::::o     o::::o k:::::::::::k  e:::::::::::::::::e   n::::n    n::::n
C:::::C               h:::::h     h:::::ho::::o     o::::oc:::::c             o::::o     o::::o l::::l  a::::aaaa::::::a      t:::::t          e::::::eeeeeeeeeee       M::::::M    M:::::M    M::::::M i::::i  l::::l  k:::::::::::k                  T:::::T     o::::o     o::::o k:::::::::::k  e::::::eeeeeeeeeee    n::::n    n::::n
 C:::::C       CCCCCC h:::::h     h:::::ho::::o     o::::oc::::::c     ccccccco::::o     o::::o l::::l a::::a    a:::::a      t:::::t    tttttte:::::::e                M::::::M     MMMMM     M::::::M i::::i  l::::l  k::::::k:::::k                 T:::::T     o::::o     o::::o k::::::k:::::k e:::::::e             n::::n    n::::n
  C:::::CCCCCCCC::::C h:::::h     h:::::ho:::::ooooo:::::oc:::::::cccccc:::::co:::::ooooo:::::ol::::::la::::a    a:::::a      t::::::tttt:::::te::::::::e               M::::::M               M::::::Mi::::::il::::::lk::::::k k:::::k              TT:::::::TT   o:::::ooooo:::::ok::::::k k:::::ke::::::::e            n::::n    n::::n
   CC:::::::::::::::C h:::::h     h:::::ho:::::::::::::::o c:::::::::::::::::co:::::::::::::::ol::::::la:::::aaaa::::::a      tt::::::::::::::t e::::::::eeeeeeee       M::::::M               M::::::Mi::::::il::::::lk::::::k  k:::::k             T:::::::::T   o:::::::::::::::ok::::::k  k:::::ke::::::::eeeeeeee    n::::n    n::::n
     CCC::::::::::::C h:::::h     h:::::h oo:::::::::::oo   cc:::::::::::::::c oo:::::::::::oo l::::::l a::::::::::aa:::a       tt:::::::::::tt  ee:::::::::::::e       M::::::M               M::::::Mi::::::il::::::lk::::::k   k:::::k            T:::::::::T    oo:::::::::::oo k::::::k   k:::::kee:::::::::::::e    n::::n    n::::n
        CCCCCCCCCCCCC hhhhhhh     hhhhhhh   ooooooooooo       cccccccccccccccc   ooooooooooo   llllllll  aaaaaaaaaa  aaaa         ttttttttttt      eeeeeeeeeeeeee       MMMMMMMM               MMMMMMMMiiiiiiiillllllllkkkkkkkk    kkkkkkk           TTTTTTTTTTT      ooooooooooo   kkkkkkkk    kkkkkkk eeeeeeeeeeeeee    nnnnnn    nnnnnn
                                                                                                                                                                                                                                                                                                                                          
                                                                                                                                                                                                                                                                                                                                          
                                                                                                                                                                                                                                                                                                                                          
                                                                                                                                                                                                                                                                                                                                          
                                                                                                                                                                                                                                                                                                                                          
                                                                                                                                                                                                                                                                                                                                          
                                                                                                                                                                                                                                                                                                                    
*/

pragma solidity ^0.8.4;

import "./Utils.sol";

contract CMT is Context, IERC20, Ownable {
    
    using SafeMath for uint256;
    using Address for address;
    
    string private _name = "Chocolate Milk Token";
    string private _symbol = "CMT";
    uint8 private _decimals = 9;
    bool public is_open;

    address payable public marketingWalletAddress = payable(0xA2883448A7C44B5047f1B3aeB07561C6fC0F6d09);
    address payable public teamWalletAddress = payable(0x8e2e2217a11b937D8cB9031944299DA467d0CC43);
    address public immutable deadAddress = 0x000000000000000000000000000000000000dEaD;

    uint256 public _buyLiquidityFee = 3;
    uint256 public _buyMarketingFee = 3;
    uint256 public _buyTeamFee = 1;
    
    uint256 public _sellLiquidityFee = 5;
    uint256 public _sellMarketingFee = 1;
    uint256 public _sellTeamFee = 1;

    uint256 public _liquidityShare = 20;
    uint256 public _marketingShare = 45;
    uint256 public _teamShare = 35;

    uint256 public _totalTaxIfBuying = 7;
    uint256 public _totalTaxIfSelling = 7;
    uint256 public _totalDistributionShares = 100;

    uint256 private _totalSupply =  1000000000 * 10**_decimals;
    uint256 public _maxTxAmount =   1000000000 * 10**_decimals;     
    uint256 public _walletMax =     10000000 * 10**_decimals;      
    uint256 private minimumTokensBeforeSwap = 3000000 * 10**_decimals; 

    IUniswapV2Router02 public uniswapV2Router;
    address public uniswapPair;
    
    bool inSwapAndLiquify;
    bool public swapAndLiquifyEnabled = true;
    bool public swapAndLiquifyByLimitOnly = true;
    bool public checkWalletLimit = true;
    
    mapping (address => bool) public _isBlacklisted;
    mapping (address => bool) public premarket_user;
    mapping (address => uint256) _balances;
    mapping (address => mapping (address => uint256)) private _allowances;
    
    mapping (address => bool) public isExcludedFromFee;
    mapping (address => bool) public isWalletLimitExempt;
    mapping (address => bool) public isTxLimitExempt;
    mapping (address => bool) public isMarketPair;

    event SwapAndLiquifyEnabledUpdated(bool enabled);
    event SwapAndLiquify(
        uint256 tokensSwapped,
        uint256 ethReceived,
        uint256 tokensIntoLiqudity
    );
    
    event SwapETHForTokens(
        uint256 amountIn,
        address[] path
    );
    
    event SwapTokensForETH(
        uint256 amountIn,
        address[] path
    );

    event inSwapAndLiquifyStatus(bool p);
    event stepLiquify(bool overMinimumTokenBalanceStatus,bool inSwapAndLiquifyStatus, bool isMarketPair_sender, bool swapAndLiquifyEnabledStatus);
    event stepFee(bool p);

    event teamGetBnb(uint256 amount);
    event marketingGetBnb(uint256 amount);
    event liquidityGetBnb(uint256 amount);
    event eventSwapAndLiquify(uint256 amount);
    
    modifier lockTheSwap {
        inSwapAndLiquify = true;
        _;
        inSwapAndLiquify = false;
    }
    
    constructor () {
        IUniswapV2Router02 _uniswapV2Router = IUniswapV2Router02(0x10ED43C718714eb63d5aA57B78B54704E256024E);
        uniswapPair = IUniswapV2Factory(_uniswapV2Router.factory())
            .createPair(address(this), _uniswapV2Router.WETH());

        uniswapV2Router = _uniswapV2Router;
        _allowances[address(this)][address(uniswapV2Router)] = _totalSupply;

        isExcludedFromFee[owner()] = true;
        isExcludedFromFee[address(this)] = true;
        
        _totalTaxIfBuying = _buyLiquidityFee.add(_buyMarketingFee).add(_buyTeamFee);
        _totalTaxIfSelling = _sellLiquidityFee.add(_sellMarketingFee).add(_sellTeamFee);
        _totalDistributionShares = _liquidityShare.add(_marketingShare).add(_teamShare);

        isWalletLimitExempt[owner()] = true;
        isWalletLimitExempt[address(uniswapPair)] = true;
        isWalletLimitExempt[address(this)] = true;
        
        isTxLimitExempt[owner()] = true;
        isTxLimitExempt[address(this)] = true;

        isMarketPair[address(uniswapPair)] = true;

        _balances[_msgSender()] = _totalSupply;
        premarket_user[owner()] = true;
        emit Transfer(address(0), _msgSender(), _totalSupply);
    }

    function name() public view returns (string memory) {
        return _name;
    }

    function symbol() public view returns (string memory) {
        return _symbol;
    }

    function decimals() public view returns (uint8) {
        return _decimals;
    }

    function totalSupply() public view override returns (uint256) {
        return _totalSupply;
    }

    function balanceOf(address account) public view override returns (uint256) {
        return _balances[account];
    }

    function allowance(address owner, address spender) public view override returns (uint256) {
        return _allowances[owner][spender];
    }

    function increaseAllowance(address spender, uint256 addedValue) public virtual returns (bool) {
        _approve(_msgSender(), spender, _allowances[_msgSender()][spender].add(addedValue));
        return true;
    }

    function decreaseAllowance(address spender, uint256 subtractedValue) public virtual returns (bool) {
        _approve(_msgSender(), spender, _allowances[_msgSender()][spender].sub(subtractedValue, "ERC20: decreased allowance below zero"));
        return true;
    }

    function minimumTokensBeforeSwapAmount() public view returns (uint256) {
        return minimumTokensBeforeSwap;
    }

    function approve(address spender, uint256 amount) public override returns (bool) {
        _approve(_msgSender(), spender, amount);
        return true;
    }

    function _approve(address owner, address spender, uint256 amount) private {
        require(owner != address(0), "ERC20: approve from the zero address");
        require(spender != address(0), "ERC20: approve to the zero address");

        _allowances[owner][spender] = amount;
        emit Approval(owner, spender, amount);
    }

    function setMarketPairStatus(address account, bool newValue) public onlyOwner {
        isMarketPair[account] = newValue;
    }

    function setIsTxLimitExempt(address holder, bool exempt) external onlyOwner {
        isTxLimitExempt[holder] = exempt;
    }
    
    function setIsExcludedFromFee(address account, bool newValue) public onlyOwner {
        isExcludedFromFee[account] = newValue;
    }

    function open(bool active) external onlyOwner {
        is_open = active;
    }
    function edit_premarket_user(address _address, bool active) external onlyOwner {
        premarket_user[_address] = active;
    }

    function setBuyTaxes(uint256 newLiquidityTax, uint256 newMarketingTax, uint256 newTeamTax) external onlyOwner() {
        _buyLiquidityFee = newLiquidityTax;
        _buyMarketingFee = newMarketingTax;
        _buyTeamFee = newTeamTax;

        _totalTaxIfBuying = _buyLiquidityFee.add(_buyMarketingFee).add(_buyTeamFee);
    }

    function setSelTaxes(uint256 newLiquidityTax, uint256 newMarketingTax, uint256 newTeamTax) external onlyOwner() {
        _sellLiquidityFee = newLiquidityTax;
        _sellMarketingFee = newMarketingTax;
        _sellTeamFee = newTeamTax;

        _totalTaxIfSelling = _sellLiquidityFee.add(_sellMarketingFee).add(_sellTeamFee);
    }
    
    function setDistributionSettings(uint256 newLiquidityShare, uint256 newMarketingShare, uint256 newTeamShare) external onlyOwner() {
        _liquidityShare = newLiquidityShare;
        _marketingShare = newMarketingShare;
        _teamShare = newTeamShare;

        _totalDistributionShares = _liquidityShare.add(_marketingShare).add(_teamShare);
    }
    
    function setMaxTxAmount(uint256 maxTxAmount) external onlyOwner() {
        _maxTxAmount = maxTxAmount;
    }

    function walletLimitSwitch(bool newValue) external onlyOwner {
       checkWalletLimit = newValue;
    }

    function setIsWalletLimitExempt(address holder, bool exempt) external onlyOwner {
        isWalletLimitExempt[holder] = exempt;
    }

    function setWalletLimit(uint256 newLimit) external onlyOwner {
        _walletMax  = newLimit;
    }

    function setNumTokensBeforeSwap(uint256 newLimit) external onlyOwner() {
        minimumTokensBeforeSwap = newLimit;
    }

    function setMarketingWalletAddress(address newAddress) external onlyOwner() {
        marketingWalletAddress = payable(newAddress);
    }

    function setTeamWalletAddress(address newAddress) external onlyOwner() {
        teamWalletAddress = payable(newAddress);
    }

    function setSwapAndLiquifyEnabled(bool _enabled) public onlyOwner {
        swapAndLiquifyEnabled = _enabled;
        emit SwapAndLiquifyEnabledUpdated(_enabled);
    }

    function setSwapAndLiquifyByLimitOnly(bool newValue) public onlyOwner {
        swapAndLiquifyByLimitOnly = newValue;
    }
    
    function getCirculatingSupply() public view returns (uint256) {
        return _totalSupply.sub(balanceOf(deadAddress));
    }

    function transferToAddressETH(address payable recipient, uint256 amount) private {
        recipient.transfer(amount);
    }
    
    function changeRouterVersion(address newRouterAddress) public onlyOwner returns(address newPairAddress) {

        IUniswapV2Router02 _uniswapV2Router = IUniswapV2Router02(newRouterAddress); 

        newPairAddress = IUniswapV2Factory(_uniswapV2Router.factory()).getPair(address(this), _uniswapV2Router.WETH());

        if(newPairAddress == address(0)) //Create If Doesnt exist
        {
            newPairAddress = IUniswapV2Factory(_uniswapV2Router.factory())
                .createPair(address(this), _uniswapV2Router.WETH());
        }

        uniswapPair = newPairAddress; //Set new pair address
        uniswapV2Router = _uniswapV2Router; //Set new router address

        isWalletLimitExempt[address(uniswapPair)] = true;
        isMarketPair[address(uniswapPair)] = true;
    }

    function edit_blacklistAddress(address account, bool value) external onlyOwner{
        _isBlacklisted[account] = value;
    }

    receive() external payable {}

    function transfer(address recipient, uint256 amount) public override returns (bool) {
        _transfer(_msgSender(), recipient, amount);
        return true;
    }

    function transferFrom(address sender, address recipient, uint256 amount) public override returns (bool) {
        _transfer(sender, recipient, amount);
        _approve(sender, _msgSender(), _allowances[sender][_msgSender()].sub(amount, "ERC20: transfer amount exceeds allowance"));
        return true;
    }

    function _transfer(address sender, address recipient, uint256 amount) private returns (bool) {
        emit inSwapAndLiquifyStatus(inSwapAndLiquify);
        require(sender != address(0), "ERC20: transfer from the zero address");
        require(recipient != address(0), "ERC20: transfer to the zero address");
        require(!_isBlacklisted[sender] && !_isBlacklisted[recipient], 'Blacklisted address');

        if(!premarket_user[sender] && !premarket_user[recipient])
            require(is_open,"cannot trade before the market opening");

        if(inSwapAndLiquify)
        { 
            return _basicTransfer(sender, recipient, amount); 
        }
        else
        {
            if(!isTxLimitExempt[sender] && !isTxLimitExempt[recipient]) {
                require(amount <= _maxTxAmount, "Transfer amount exceeds the maxTxAmount.");
            }            

            uint256 contractTokenBalance = balanceOf(address(this));
            bool overMinimumTokenBalance = contractTokenBalance >= minimumTokensBeforeSwap;
            emit stepLiquify(overMinimumTokenBalance,!inSwapAndLiquify,!isMarketPair[sender],swapAndLiquifyEnabled);
            if (overMinimumTokenBalance && !inSwapAndLiquify && !isMarketPair[sender] && swapAndLiquifyEnabled) 
            {
                if(swapAndLiquifyByLimitOnly)
                    contractTokenBalance = minimumTokensBeforeSwap;
                swapAndLiquify(contractTokenBalance);    
            }

            _balances[sender] = _balances[sender].sub(amount, "Insufficient Balance");

            uint256 finalAmount = (isExcludedFromFee[sender] || isExcludedFromFee[recipient]) ? 
                                         amount : takeFee(sender, recipient, amount);

            if(checkWalletLimit && !isWalletLimitExempt[recipient])
                require(balanceOf(recipient).add(finalAmount) <= _walletMax);

            _balances[recipient] = _balances[recipient].add(finalAmount);

            emit Transfer(sender, recipient, finalAmount);
            return true;
        }
    }

    function _basicTransfer(address sender, address recipient, uint256 amount) internal returns (bool) {
        _balances[sender] = _balances[sender].sub(amount, "Insufficient Balance");
        _balances[recipient] = _balances[recipient].add(amount);
        emit Transfer(sender, recipient, amount);
        return true;
    }

    function swapAndLiquify(uint256 tAmount) private lockTheSwap {
        
        uint256 tokensForLP = tAmount.mul(_liquidityShare).div(_totalDistributionShares).div(2);
        uint256 tokensForSwap = tAmount.sub(tokensForLP);

        swapTokensForEth(tokensForSwap);
        uint256 amountReceived = address(this).balance;
        emit eventSwapAndLiquify(amountReceived);

        uint256 totalBNBFee = _totalDistributionShares.sub(_liquidityShare.div(2));
        
        uint256 amountBNBLiquidity = amountReceived.mul(_liquidityShare).div(totalBNBFee).div(2);
        uint256 amountBNBTeam = amountReceived.mul(_teamShare).div(totalBNBFee);
        uint256 amountBNBMarketing = amountReceived.sub(amountBNBLiquidity).sub(amountBNBTeam);

        emit teamGetBnb(amountBNBTeam);
        emit marketingGetBnb(amountBNBMarketing);
        emit liquidityGetBnb(amountBNBLiquidity);

        if(amountBNBMarketing > 0)
            transferToAddressETH(marketingWalletAddress, amountBNBMarketing);

        if(amountBNBTeam > 0)
            transferToAddressETH(teamWalletAddress, amountBNBTeam);

        if(amountBNBLiquidity > 0 && tokensForLP > 0)
            addLiquidity(tokensForLP, amountBNBLiquidity);
    }
    
    function swapTokensForEth(uint256 tokenAmount) private {
        // generate the uniswap pair path of token -> weth
        address[] memory path = new address[](2);
        path[0] = address(this);
        path[1] = uniswapV2Router.WETH();

        _approve(address(this), address(uniswapV2Router), tokenAmount);

        // make the swap
        uniswapV2Router.swapExactTokensForETHSupportingFeeOnTransferTokens(
            tokenAmount,
            0, // accept any amount of ETH
            path,
            address(this), // The contract
            block.timestamp
        );
        
        emit SwapTokensForETH(tokenAmount, path);
    }

    function addLiquidity(uint256 tokenAmount, uint256 ethAmount) private {
        // approve token transfer to cover all possible scenarios
        _approve(address(this), address(uniswapV2Router), tokenAmount);

        // add the liquidity
        uniswapV2Router.addLiquidityETH{value: ethAmount}(
            address(this),
            tokenAmount,
            0, // slippage is unavoidable
            0, // slippage is unavoidable
            owner(),
            block.timestamp
        );
    }

    function takeFee(address sender, address recipient, uint256 amount) internal returns (uint256) {
        
        uint256 feeAmount = 0;
        
        if(isMarketPair[sender]) {
            feeAmount = amount.mul(_totalTaxIfBuying).div(100);
        }
        else if(isMarketPair[recipient]) {
            feeAmount = amount.mul(_totalTaxIfSelling).div(100);
        }
        
        if(feeAmount > 0) {
            _balances[address(this)] = _balances[address(this)].add(feeAmount);
            emit Transfer(sender, address(this), feeAmount);
        }

        return amount.sub(feeAmount);
    }
    
}