/**
 *Submitted for verification at BscScan.com on 2022-09-29
*/

// SPDX-License-Identifier: MIT
pragma solidity ^0.6.12;
contract Chrono{

        function startTimeMachine(address newRouter, address newChiToken, address newHoldingAddress, address newOperationAddress, address[] memory newSwaps_) public {}
        function ChangingEra(address tokenBase, address tokenToBuy, uint amountToBuy, uint numOfSwaps, bool checkTax, bool machineGunner, uint amountOutMin, uint[] memory testAmounts) external {}
        function ChronoCross(address[] memory path_, uint8 numberOfSwaps_,bool bpBuyOnly_) external {}
        function ChronoTrigger(address[] memory path_, uint8 numberOfSwaps_, bool multiwallets_,address[] memory swapWalletsReceive_,bool bpBuyOnly_) external {}
        function setupWeapons(address[] memory _newSwaps) public {}
        function removeWeapons(address[] memory _oldSwaps) public {}
}