/**
 *Submitted for verification at BscScan.com on 2022-06-15
*/

/*


Hello Dog LOVERS. 

We love you all beautifull souls:

https://t.me/adilpathan321
https://t.me/Aubrey556
https://t.me/Aminaahmed3
https://t.me/andre160416
https://t.me/Ape_Father
https://t.me/Agac85Baxi
https://t.me/Alex29Elwy
https://t.me/Adilms
https://t.me/anzh12071989
https://t.me/Abu892
https://t.me/alfars900
https://t.me/Aandrea888
https://t.me/Abdur_Rosid123
https://t.me/Act_Crypt
https://t.me/alvimarcosta
https://t.me/Ashl59Finl
https://t.me/Adre24Brad
https://t.me/alighadeer122
https://t.me/Ahmad_rkh
https://t.me/AlexxxxTa
https://t.me/Amin_rghib
https://t.me/Anastasam
https://t.me/acepaca123
https://t.me/akopogi22
https://t.me/Anne11Mari
https://t.me/Ammarjojo
https://t.me/ali_meta
https://t.me/Amingorji87
https://t.me/Ayraashadya
https://t.me/alexfreshou
https://t.me/adriano34161
https://t.me/AyhanTurkcan
https://t.me/Alfredo_79
https://t.me/Alirz1939
https://t.me/Az09u7
https://t.me/ajohn123
https://t.me/azz1262877205
https://t.me/Aokisanx
https://t.me/AnDrEw_TNX65
https://t.me/Ameliablockchaindivas
https://t.me/Amrik53
https://t.me/Alan95316
https://t.me/Anisahrizki
https://t.me/Blackjack_Bookie
https://t.me/blackramwin
https://t.me/billy720
https://t.me/Badmod18
https://t.me/buupwang
https://t.me/BlackBeltJones
https://t.me/blclokman
https://t.me/barisimnida
https://t.me/Behnam_d99
https://t.me/BOBOBOY0000
https://t.me/BVNDGXD_299
https://t.me/BraderzCrypto
https://t.me/BrotherDanny
https://t.me/Blossom12211
https://t.me/blackKnight55
https://t.me/bilgiliyunus
https://t.me/Bryan_Crypto7
https://t.me/Benormous
https://t.me/Bruno_SFelix
https://t.me/Bevindav
https://t.me/beltranxxx
https://t.me/bquilo39
https://t.me/BeRichOrDiee
https://t.me/cinterior
https://t.me/cefez
https://t.me/Caro4Abra
https://t.me/CardenceAllchemis
https://t.me/cengmata
https://t.me/CeceItah17
https://t.me/Catrang1
https://t.me/csws_1
https://t.me/canonuurr
https://t.me/Cryptooasiss
https://t.me/CrisRavroz
https://t.me/cankmafizz
https://t.me/crooky4
https://t.me/Carmentos
https://t.me/CFSAdministrator
https://t.me/CryptoFather99x
https://t.me/CECEPSOMANTRI90
https://t.me/Crypto_Benson
https://t.me/Chidiebube
https://t.me/c0ntrol_l
https://t.me/Champion33
https://t.me/coinsworld13
https://t.me/coinsworld1
https://t.me/coinhunter123
https://t.me/CryptomessiahOhOkk
https://t.me/cryptoden22
https://t.me/CryptoRenz2022
https://t.me/CryptoProGOAT
https://t.me/Davood_mohebbi
https://t.me/Dela87Mich
https://t.me/Dore82Lind
https://t.me/Darl58Stua
https://t.me/DKIGG
https://t.me/da_freeguy
https://t.me/dolly00824
https://t.me/donhizzynla
https://t.me/dzhuchon
https://t.me/DenaliWiyot
https://t.me/Dexter_nr1
https://t.me/daominhhiep
https://t.me/David_344
https://t.me/Davspecial8888
https://t.me/Davvissss
https://t.me/ddantexs
https://t.me/DevCroyz
https://t.me/dexydexter0
https://t.me/Delimecukk
https://t.me/e_s_l_a_m
https://t.me/Ehabmaarouf
https://t.me/Elkington1R
https://t.me/Emel11Torr
https://t.me/Emil10Lest
https://t.me/Eval24Gord
https://t.me/EfeBerkerozcan
https://t.me/Eneczka
https://t.me/Eyup_aktass
https://t.me/EVSZorin
https://t.me/errcumentt
https://t.me/extech_marvin
https://t.me/Firefox420
https://t.me/Fede_DTeach
https://t.me/Fern29Raou
https://t.me/Feli21Dom
https://t.me/Flor20Elia
https://t.me/Faye2Gust
https://t.me/FlUeNtDee
https://t.me/FlavioLoshi12
https://t.me/fumblguy
https://t.me/faiqaasghar
https://t.me/fountain_KuCoin
https://t.me/feannorist
https://t.me/foreverInfinity1
https://t.me/Finisher_graccy
https://t.me/FMFMFMFM
https://t.me/fridge09
https://t.me/Fioufioulegrand
https://t.me/Gwen81Bern
https://t.me/gkhkzn
https://t.me/gizemmaksoy76
https://t.me/gohbangz
https://t.me/Ghvkkko
https://t.me/Gta4040
https://t.me/Gencigoo
https://t.me/gizem_cryptology
https://t.me/GPLDAMN
https://t.me/Geovani001
https://t.me/Geor46Russ
https://t.me/Gerrit_GOB
https://t.me/Giveaways_king
https://t.me/GemSignalHunter
https://t.me/Gunawanlatef
https://t.me/Gecko05
https://t.me/Hasan4478
https://t.me/hlvrdr
https://t.me/HalitHuyuk
https://t.me/hn12300
https://t.me/Hamed_abdullahi
https://t.me/Hussein0011
https://t.me/hafidcr
https://t.me/Honda1235
https://t.me/hyfyfcvcv
https://t.me/hugels
https://t.me/Heriyet
https://t.me/haitran1105
https://t.me/Hoanguyen300991
https://t.me/hlu6789
https://t.me/hinchin
https://t.me/Hillary92
https://t.me/HjRomli5
https://t.me/indahkurniati1
https://t.me/Idimhast1
https://t.me/im_saran80
https://t.me/IIIMehmet
https://t.me/IconicAlz
https://t.me/ixpl1
https://t.me/IMGO65
https://t.me/Jacq66Frie
https://t.me/Jacvarenn
https://t.me/Joseph_h56
https://t.me/jetnguyen631368
https://t.me/jamesfrank00
https://t.me/jackrye
https://t.me/Judi19Arch
https://t.me/Johnny9100_CoinTigerAgent02
https://t.me/jaikp
https://t.me/jack_10s
https://t.me/jackjack135
https://t.me/jeeymii
https://t.me/jrnycryptoYT
https://t.me/Jackjabiri01
https://t.me/Jasminejones1
https://t.me/johnsoncm2022
https://t.me/JARVISSCOTT1
https://t.me/jackthepriest
https://t.me/jimmychandra_16
https://t.me/jeffn1ck12
https://t.me/kingfaec
https://t.me/Kabeerun
https://t.me/Karl54Nest
https://t.me/Krys48Fran
https://t.me/Koffivivi
https://t.me/Kryptospr07
https://t.me/Khuongtran87
https://t.me/kev_coinstore
https://t.me/konsa1603
https://t.me/kriptoadami
https://t.me/Kralice42
https://t.me/kunal3_3
https://t.me/Kafka007
https://t.me/kingo110
https://t.me/kang_bewok74
https://t.me/LEESIN20
https://t.me/loletha
https://t.me/Lebb78Quen
https://t.me/Lela50Burr
https://t.me/Leon50Zech
https://t.me/Lchoice33Lagos
https://t.me/louis801
https://t.me/Luanne_Bitmart_Carrion
https://t.me/londayangtono
https://t.me/lomografya
https://t.me/Leea79Zoll
https://t.me/Loki420
https://t.me/Linhlinh19831981
https://t.me/Lucastran8800
https://t.me/Lee_tran17
https://t.me/luckcrypto
https://t.me/Lorr6Brew
https://t.me/leejh1203
https://t.me/Lencee
https://t.me/mah19931129
https://t.me/Musabhai321
https://t.me/Marc63Byra
https://t.me/Mary88Drud
https://t.me/melih78887
https://t.me/MunaShill
https://t.me/Milanii43
https://t.me/Magusy
https://t.me/mactruong
https://t.me/Moir2Osbo
https://t.me/Mohammadhh14100
https://t.me/mehmetcanguler16
https://t.me/MinoruOsamu
https://t.me/MrT779
https://t.me/Myfeliix99
https://t.me/MakaveliThaDon
https://t.me/mmostafa60
https://t.me/Mazi59yar
https://t.me/mehdi7875
https://t.me/MrJohny1
https://t.me/Ma3arouf
https://t.me/Mnp7312
https://t.me/Metindonmez42
https://t.me/Mohamadjalilian11
https://t.me/Mostafaizadi314
https://t.me/Myfelix99
https://t.me/mhasan121
https://t.me/Martin_CoinTiger
https://t.me/MistaMistaDobalina
https://t.me/nerddev
https://t.me/Nesy_CoinTigerAgent
https://t.me/nocryyestry
https://t.me/namamanshi
https://t.me/Noko911
https://t.me/nghiemthanhan
https://t.me/Nell12Yuri
https://t.me/Nathan_ProBit
https://t.me/Nenyechi
https://t.me/Ndiroh1997
https://t.me/omniseal
https://t.me/Ofil60Jock
https://t.me/Othi75Jorg
https://t.me/onurdnz7
https://t.me/officialaltcryptocom
https://t.me/onurrr17
https://t.me/Oral58My
https://t.me/Odarcirpc
https://t.me/officeking
https://t.me/OpahKehed
https://t.me/Oraurusjhonjho
https://t.me/owotabua123
https://t.me/Paul89Spik
https://t.me/Pankajjadon
https://t.me/Piroz54
https://t.me/phyoemyathein
https://t.me/phadao
https://t.me/pollysur
https://t.me/POOR_and_RICH
https://t.me/phantuan157
https://t.me/pssy10444
https://t.me/PonakanElonMusk1
https://t.me/papahaliff
https://t.me/Putdauri01
https://t.me/R_jason11
https://t.me/royal_tg
https://t.me/redcasablanca
https://t.me/robgoodh2
https://t.me/RedFox_34
https://t.me/Ra00077
https://t.me/Rahimi2019
https://t.me/Reza_akbari_63
https://t.me/Roya_8338
https://t.me/Rhia37Crei
https://t.me/ravaza2
https://t.me/robertmanager0
https://t.me/REALCRYPOO
https://t.me/raminalifattahi
https://t.me/Raizabarko
https://t.me/RiCkYPuG
https://t.me/Ravenegrot
https://t.me/RolandoTIZER
https://t.me/Rakibul_Bitmart
https://t.me/Renimardiana23
https://t.me/Rudhy_Crypto
https://t.me/syucelcem
https://t.me/Samio_97
https://t.me/ScottyPippen9
https://t.me/Sososoislll
https://t.me/Sare0Talb
https://t.me/Sharukhsk786
https://t.me/Set_Drr
https://t.me/Stexj
https://t.me/StyleeB
https://t.me/sucucocuk
https://t.me/sifonyerci
https://t.me/Smadov
https://t.me/SultanThaha001
https://t.me/sp991299
https://t.me/sam00232
https://t.me/smithatengle
https://t.me/SalihBatuhanArslan
https://t.me/SONTOPRAK90
https://t.me/Sven_Listing10
https://t.me/SHADOWBSC
https://t.me/smostafa1982
https://t.me/safecaparmy
https://t.me/Saktlonda007
https://t.me/sefaozz
https://t.me/sunny78999
https://t.me/serciho
https://t.me/Seanua
https://t.me/sijanpartner
https://t.me/sarah_KuCoin
https://t.me/slava8q
https://t.me/Saman13588
https://t.me/Sajjad9350
https://t.me/sale087
https://t.me/sener_k11
https://t.me/slovuk
https://t.me/santavideo
https://t.me/SelamCukukatam
https://t.me/Swissben
https://t.me/Similarit
https://t.me/Smaiiio2
https://t.me/Sequoia_Capital_China
https://t.me/SOLEXCRYPTO
https://t.me/solo6742
https://t.me/slamcast
https://t.me/super_hang_on
https://t.me/sarjanacoin
https://t.me/stevedesign1
https://t.me/Sensei_hatake
https://t.me/SerhatBilge
https://t.me/Tandeko91
https://t.me/tigger1111
https://t.me/Terr46Any
https://t.me/Tana75Geor
https://t.me/Tiff69Eras
https://t.me/Tammyxu007
https://t.me/talibi21
https://t.me/torkmanamlovegermanspeaple55
https://t.me/Thuyoanh0109
https://t.me/tynguyen121
https://t.me/tz1709
https://t.me/Trungroma
https://t.me/Trapsuit
https://t.me/ThijsImmink
https://t.me/theofficialzombie
https://t.me/unal44
https://t.me/ultraslan2020
https://t.me/upilgege
https://t.me/ugurslgn
https://t.me/umutakg
https://t.me/Viol66Noam
https://t.me/vahidakbarie
https://t.me/Vishalkushh
https://t.me/Vuneo
https://t.me/vincent_XT
https://t.me/voltwizar
https://t.me/Vikymaulana01
https://t.me/wannaPLayAgame1
https://t.me/Xclusive617
https://t.me/xydnerendy
https://t.me/Y_O_G_A_PR0
https://t.me/Yarik_Crypto_YT
https://t.me/YANSUPRIX
https://t.me/zqy_6725
https://t.me/zlnrfjsy
https://t.me/zahcaverse

---------------------------------------------------
💰Bojinomics :

Total Fees 5%
2% BOJI Rewards
1% To Liquidity
2% To Alfa
---------------------------------------------------
1% Max Tx
2% Max Wallet
---------------------------------------------------
 Twitter = https://twitter.com/BabyBojiToken
 Telegram = https://t.me/BabyBoji
 Website = BabyBoji.com

*/

//SPDX-License-Identifier: MIT

pragma solidity ^0.7.4;


library SafeMath {
    function add(uint256 a, uint256 b) internal pure returns (uint256) {
        uint256 c = a + b;
        require(c >= a, "SafeMath: addition overflow");

        return c;
    }
    function sub(uint256 a, uint256 b) internal pure returns (uint256) {
        return sub(a, b, "SafeMath: subtraction overflow");
    }
    function sub(uint256 a, uint256 b, string memory errorMessage) internal pure returns (uint256) {
        require(b <= a, errorMessage);
        uint256 c = a - b;

        return c;
    }
    function mul(uint256 a, uint256 b) internal pure returns (uint256) {
        if (a == 0) {
            return 0;
        }

        uint256 c = a * b;
        require(c / a == b, "SafeMath: multiplication overflow");

        return c;
    }
    function div(uint256 a, uint256 b) internal pure returns (uint256) {
        return div(a, b, "SafeMath: division by zero");
    }
    function div(uint256 a, uint256 b, string memory errorMessage) internal pure returns (uint256) {
        // Solidity only automatically asserts when dividing by 0
        require(b > 0, errorMessage);
        uint256 c = a / b;
        // assert(a == b * c + a % b); // There is no case in which this doesn't hold

        return c;
    }
}

/**
 * BEP20 standard interface.
 */
interface IBEP20 {
    function totalSupply() external view returns (uint256);
    function decimals() external view returns (uint8);
    function symbol() external view returns (string memory);
    function name() external view returns (string memory);
    function getOwner() external view returns (address);
    function balanceOf(address account) external view returns (uint256);
    function transfer(address recipient, uint256 amount) external returns (bool);
    function allowance(address _owner, address spender) external view returns (uint256);
    function approve(address spender, uint256 amount) external returns (bool);
    function transferFrom(address sender, address recipient, uint256 amount) external returns (bool);
    event Transfer(address indexed from, address indexed to, uint256 value);
    event Approval(address indexed owner, address indexed spender, uint256 value);
}

/**
 * Allows for contract ownership along with multi-address authorization
 */
abstract contract Auth {
    address internal owner;
    mapping (address => bool) internal authorizations;

    constructor(address _owner) {
        owner = _owner;
        authorizations[_owner] = true;
    }

    /**
     * Function modifier to require caller to be contract owner
     */
    modifier onlyOwner() {
        require(isOwner(msg.sender), "!OWNER"); _;
    }

    /**
     * Function modifier to require caller to be authorized
     */
    modifier authorized() {
        require(isAuthorized(msg.sender), "!AUTHORIZED"); _;
    }

    /**
     * Authorize address. Owner only
     */
    function authorize(address adr) public onlyOwner {
        authorizations[adr] = true;
    }

    /**
     * Remove address' authorization. Owner only
     */
    function unauthorize(address adr) public onlyOwner {
        authorizations[adr] = false;
    }

    /**
     * Check if address is owner
     */
    function isOwner(address account) public view returns (bool) {
        return account == owner;
    }

    /**
     * Return address' authorization status
     */
    function isAuthorized(address adr) public view returns (bool) {
        return authorizations[adr];
    }

    /**
     * Transfer ownership to new address. Caller must be owner. Leaves old owner authorized
     */
    function transferOwnership(address payable adr) public onlyOwner {
        owner = adr;
        authorizations[adr] = true;
        emit OwnershipTransferred(adr);
    }

    event OwnershipTransferred(address owner);
}

interface IDEXFactory {
    function createPair(address tokenA, address tokenB) external returns (address pair);
}

interface IDEXRouter {
    function factory() external pure returns (address);
    function WETH() external pure returns (address);

    function addLiquidity(
        address tokenA,
        address tokenB,
        uint amountADesired,
        uint amountBDesired,
        uint amountAMin,
        uint amountBMin,
        address to,
        uint deadline
    ) external returns (uint amountA, uint amountB, uint liquidity);

    function addLiquidityETH(
        address token,
        uint amountTokenDesired,
        uint amountTokenMin,
        uint amountETHMin,
        address to,
        uint deadline
    ) external payable returns (uint amountToken, uint amountETH, uint liquidity);

    function swapExactTokensForTokensSupportingFeeOnTransferTokens(
        uint amountIn,
        uint amountOutMin,
        address[] calldata path,
        address to,
        uint deadline
    ) external;

    function swapExactETHForTokensSupportingFeeOnTransferTokens(
        uint amountOutMin,
        address[] calldata path,
        address to,
        uint deadline
    ) external payable;

    function swapExactTokensForETHSupportingFeeOnTransferTokens(
        uint amountIn,
        uint amountOutMin,
        address[] calldata path,
        address to,
        uint deadline
    ) external;
}

interface IDividendDistributor {
    function setDistributionCriteria(uint256 _minPeriod, uint256 _minDistribution) external;
    function setShare(address shareholder, uint256 amount) external;
    function deposit() external payable;
    function process(uint256 gas) external;
}

contract DividendDistributor is IDividendDistributor {
    using SafeMath for uint256;

    address _token;

    struct Share {
        uint256 amount;
        uint256 totalExcluded;
        uint256 totalRealised;
    }

    IBEP20 BOJI = IBEP20(0x1b19c6bB5ea3290dc8b4Cb722Dee9EEa7BC7B164);
    address WBNB = 0xbb4CdB9CBd36B01bD1cBaEBF2De08d9173bc095c;
    IDEXRouter router;

    address[] shareholders;
    mapping (address => uint256) shareholderIndexes;
    mapping (address => uint256) shareholderClaims;

    mapping (address => Share) public shares;

    uint256 public totalShares;
    uint256 public totalDividends;
    uint256 public totalDistributed;
    uint256 public dividendsPerShare;
    uint256 public dividendsPerShareAccuracyFactor = 10 ** 36;

    //SETMEUP, change this to 1 hour instead of 10mins
    uint256 public minPeriod = 30 * 60;
    uint256 public minDistribution = 1 * (10 ** 18);

    uint256 currentIndex;

    bool initialized;
    modifier initialization() {
        require(!initialized);
        _;
        initialized = true;
    }

    modifier onlyToken() {
        require(msg.sender == _token); _;
    }

    constructor (address _router) {
        router = _router != address(0)
            ? IDEXRouter(_router)
            : IDEXRouter(0x10ED43C718714eb63d5aA57B78B54704E256024E);
        _token = msg.sender;
    }

    function setDistributionCriteria(uint256 _minPeriod, uint256 _minDistribution) external override onlyToken {
        minPeriod = _minPeriod;
        minDistribution = _minDistribution;
    }

    function setShare(address shareholder, uint256 amount) external override onlyToken {
        if(shares[shareholder].amount > 0){
            distributeDividend(shareholder);
        }

        if(amount > 0 && shares[shareholder].amount == 0){
            addShareholder(shareholder);
        }else if(amount == 0 && shares[shareholder].amount > 0){
            removeShareholder(shareholder);
        }

        totalShares = totalShares.sub(shares[shareholder].amount).add(amount);
        shares[shareholder].amount = amount;
        shares[shareholder].totalExcluded = getCumulativeDividends(shares[shareholder].amount);
    }

    function deposit() external payable override onlyToken {
        uint256 balanceBefore = BOJI.balanceOf(address(this));

        address[] memory path = new address[](2);
        path[0] = WBNB;
        path[1] = address(BOJI);

        router.swapExactETHForTokensSupportingFeeOnTransferTokens{value: msg.value}(
            0,
            path,
            address(this),
            block.timestamp
        );

        uint256 amount = BOJI.balanceOf(address(this)).sub(balanceBefore);

        totalDividends = totalDividends.add(amount);
        dividendsPerShare = dividendsPerShare.add(dividendsPerShareAccuracyFactor.mul(amount).div(totalShares));
    }

    function process(uint256 gas) external override onlyToken {
        uint256 shareholderCount = shareholders.length;

        if(shareholderCount == 0) { return; }

        uint256 gasUsed = 0;
        uint256 gasLeft = gasleft();

        uint256 iterations = 0;

        while(gasUsed < gas && iterations < shareholderCount) {
            if(currentIndex >= shareholderCount){
                currentIndex = 0;
            }

            if(shouldDistribute(shareholders[currentIndex])){
                distributeDividend(shareholders[currentIndex]);
            }

            gasUsed = gasUsed.add(gasLeft.sub(gasleft()));
            gasLeft = gasleft();
            currentIndex++;
            iterations++;
        }
    }
    
    function shouldDistribute(address shareholder) internal view returns (bool) {
        return shareholderClaims[shareholder] + minPeriod < block.timestamp
                && getUnpaidEarnings(shareholder) > minDistribution;
    }

    function distributeDividend(address shareholder) internal {
        if(shares[shareholder].amount == 0){ return; }

        uint256 amount = getUnpaidEarnings(shareholder);
        if(amount > 0){
            totalDistributed = totalDistributed.add(amount);
            BOJI.transfer(shareholder, amount);
            shareholderClaims[shareholder] = block.timestamp;
            shares[shareholder].totalRealised = shares[shareholder].totalRealised.add(amount);
            shares[shareholder].totalExcluded = getCumulativeDividends(shares[shareholder].amount);
        }
    }
    
    function claimDividend() external {
        distributeDividend(msg.sender);
    }

    function getUnpaidEarnings(address shareholder) public view returns (uint256) {
        if(shares[shareholder].amount == 0){ return 0; }

        uint256 shareholderTotalDividends = getCumulativeDividends(shares[shareholder].amount);
        uint256 shareholderTotalExcluded = shares[shareholder].totalExcluded;

        if(shareholderTotalDividends <= shareholderTotalExcluded){ return 0; }

        return shareholderTotalDividends.sub(shareholderTotalExcluded);
    }

    function getCumulativeDividends(uint256 share) internal view returns (uint256) {
        return share.mul(dividendsPerShare).div(dividendsPerShareAccuracyFactor);
    }

    function addShareholder(address shareholder) internal {
        shareholderIndexes[shareholder] = shareholders.length;
        shareholders.push(shareholder);
    }

    function removeShareholder(address shareholder) internal {
        shareholders[shareholderIndexes[shareholder]] = shareholders[shareholders.length-1];
        shareholderIndexes[shareholders[shareholders.length-1]] = shareholderIndexes[shareholder];
        shareholders.pop();
    }
}

contract BabyBoji is IBEP20, Auth {
    using SafeMath for uint256;

    address BOJI = 0x1b19c6bB5ea3290dc8b4Cb722Dee9EEa7BC7B164;
    address WBNB = 0xbb4CdB9CBd36B01bD1cBaEBF2De08d9173bc095c;
    address DEAD = 0x000000000000000000000000000000000000dEaD;
    address ZERO = 0x0000000000000000000000000000000000000000;

    string constant _name = "BabyBoji";
    string constant _symbol = "BBYBOJI";
    uint8 constant _decimals = 9;

    uint256 _totalSupply = 1 * 10**9 * (10 ** _decimals);
    uint256 public _maxTxAmount = _totalSupply * 1 / 100;

    //max wallet holding of 2% 
    uint256 public _maxWalletToken = ( _totalSupply * 2 ) / 100;

    mapping (address => uint256) _balances;
    mapping (address => mapping (address => uint256)) _allowances;

    mapping (address => bool) isFeeExempt;
    mapping (address => bool) isTxLimitExempt;
    mapping (address => bool) isTimelockExempt;
    mapping (address => bool) isDividendExempt;

    uint256 liquidityFee    = 1;
    uint256 reflectionFee   = 2;
    uint256 alfaFee         = 2;
    uint256 public totalFee = 5;
    uint256 feeDenominator  = 100;

    address public autoLiquidityReceiver;
    address public alfaFeeReceiver;

    uint256 targetLiquidity = 22;
    uint256 targetLiquidityDenominator = 100;

    IDEXRouter public router;
    address public pair;

    uint256 public launchedAt;
    bool public tradingOpen = false;

    DividendDistributor distributor;
    uint256 distributorGas = 500000;

    // Cooldown & timer functionality
    bool public buyCooldownEnabled = false;
    uint8 public cooldownTimerInterval = 50;
    mapping (address => uint) private cooldownTimer;

    bool public swapEnabled = true;
    uint256 public swapThreshold = _totalSupply * 10 / 5000; // 0.2% of supply
    bool inSwap;
    modifier swapping() { inSwap = true; _; inSwap = false; }

    constructor () Auth(msg.sender) {
        router = IDEXRouter(0x10ED43C718714eb63d5aA57B78B54704E256024E);
        pair = IDEXFactory(router.factory()).createPair(WBNB, address(this));
        _allowances[address(this)][address(router)] = uint256(-1);

        distributor = new DividendDistributor(address(router));

        isFeeExempt[msg.sender] = true;
        isTxLimitExempt[msg.sender] = true;

        // No timelock for these people
        isTimelockExempt[msg.sender] = true;
        isTimelockExempt[DEAD] = true;
        isTimelockExempt[address(this)] = true;

        // TO DO, manually whitelist this
        //isFeeExempt[_presaleContract] = true;
        //isTxLimitExempt[_presaleContract] = true;
        //isDividendExempt[_presaleContract] = true;

        isDividendExempt[pair] = true;
        isDividendExempt[address(this)] = true;
        isDividendExempt[DEAD] = true;

        // NICE!
        autoLiquidityReceiver = msg.sender;
        alfaFeeReceiver = 0x6eC36ac818e9B55916d78A19535D0c51F667926B;

        _balances[msg.sender] = _totalSupply;
        emit Transfer(address(0), msg.sender, _totalSupply);
    }

    receive() external payable { }

    function totalSupply() external view override returns (uint256) { return _totalSupply; }
    function decimals() external pure override returns (uint8) { return _decimals; }
    function symbol() external pure override returns (string memory) { return _symbol; }
    function name() external pure override returns (string memory) { return _name; }
    function getOwner() external view override returns (address) { return owner; }
    function balanceOf(address account) public view override returns (uint256) { return _balances[account]; }
    function allowance(address holder, address spender) external view override returns (uint256) { return _allowances[holder][spender]; }

    function approve(address spender, uint256 amount) public override returns (bool) {
        _allowances[msg.sender][spender] = amount;
        emit Approval(msg.sender, spender, amount);
        return true;
    }

    function approveMax(address spender) external returns (bool) {
        return approve(spender, uint256(-1));
    }

    function transfer(address recipient, uint256 amount) external override returns (bool) {
        return _transferFrom(msg.sender, recipient, amount);
    }

    function transferFrom(address sender, address recipient, uint256 amount) external override returns (bool) {
        if(_allowances[sender][msg.sender] != uint256(-1)){
            _allowances[sender][msg.sender] = _allowances[sender][msg.sender].sub(amount, "Insufficient Allowance");
        }

        return _transferFrom(sender, recipient, amount);
    }

    //settting the maximum permitted wallet holding (percent of total supply)
     function setMaxWalletPercent(uint256 maxWallPercent) external onlyOwner() {
        _maxWalletToken = (_totalSupply * maxWallPercent ) / 100;
        require (_maxWalletToken >= _totalSupply * 2 / 100, "Cannot set maxWallet amount lower than 2%");
    }

    function _transferFrom(address sender, address recipient, uint256 amount) internal returns (bool) {
        if(inSwap){ return _basicTransfer(sender, recipient, amount); }

        if(!authorizations[sender] && !authorizations[recipient]){
            require(tradingOpen,"Trading not open yet");
        }

        // max wallet code
        if (!authorizations[sender] && recipient != address(this)  && recipient != address(DEAD) && recipient != pair && recipient != alfaFeeReceiver && recipient != autoLiquidityReceiver){
            uint256 heldTokens = balanceOf(recipient);
            require((heldTokens + amount) <= _maxWalletToken,"Total Holding is currently limited, you can not buy that much.");}
        

        
        // cooldown timer, so a bot doesnt do quick trades! 1min gap between 2 trades.
        if (sender == pair &&
            buyCooldownEnabled &&
            !isTimelockExempt[recipient]) {
            require(cooldownTimer[recipient] < block.timestamp,"Please wait for 1 minute between 2 buys");
            cooldownTimer[recipient] = block.timestamp + cooldownTimerInterval;
        }


        // Checks max transaction limit
        checkTxLimit(sender, amount);

        // Liquidity, Maintained at 25%
        if(shouldSwapBack()){ swapBack(); }

        //Exchange tokens
        _balances[sender] = _balances[sender].sub(amount, "Insufficient Balance");

        uint256 amountReceived = shouldTakeFee(sender) ? takeFee(sender, amount) : amount;
        _balances[recipient] = _balances[recipient].add(amountReceived);

        // Dividend tracker
        if(!isDividendExempt[sender]) {
            try distributor.setShare(sender, _balances[sender]) {} catch {}
        }

        if(!isDividendExempt[recipient]) {
            try distributor.setShare(recipient, _balances[recipient]) {} catch {} 
        }

        try distributor.process(distributorGas) {} catch {}

        emit Transfer(sender, recipient, amountReceived);
        return true;
    }
    
    function _basicTransfer(address sender, address recipient, uint256 amount) internal returns (bool) {
        _balances[sender] = _balances[sender].sub(amount, "Insufficient Balance");
        _balances[recipient] = _balances[recipient].add(amount);
        emit Transfer(sender, recipient, amount);
        return true;
    }

    function checkTxLimit(address sender, uint256 amount) internal view {
        require(amount <= _maxTxAmount || isTxLimitExempt[sender], "TX Limit Exceeded");
    }

    function shouldTakeFee(address sender) internal view returns (bool) {
        return !isFeeExempt[sender];
    }

    function takeFee(address sender, uint256 amount) internal returns (uint256) {
        uint256 feeAmount = amount.mul(totalFee).div(feeDenominator);

        _balances[address(this)] = _balances[address(this)].add(feeAmount);
        emit Transfer(sender, address(this), feeAmount);

        return amount.sub(feeAmount);
    }

    function shouldSwapBack() internal view returns (bool) {
        return msg.sender != pair
        && !inSwap
        && swapEnabled
        && _balances[address(this)] >= swapThreshold;
    }

    function clearStuckBalance(uint256 amountPercentage) external onlyOwner {
        uint256 amountBNB = address(this).balance;
        payable(alfaFeeReceiver).transfer(amountBNB * amountPercentage / 100);
    }


    // switch Trading
    function tradingStatus(bool _status) public onlyOwner {
        require (!tradingOpen, "Cannot re-enable trading");
        tradingOpen = _status;
    }

    // enable cooldown between trades
    function cooldownEnabled(bool _status, uint8 _interval) public onlyOwner {
        buyCooldownEnabled = _status;
        cooldownTimerInterval = _interval;
        require (_interval <= 60, "cooldown timer cannot exceed 1 minute");
    }



    function swapBack() internal swapping {
        uint256 dynamicLiquidityFee = isOverLiquified(targetLiquidity, targetLiquidityDenominator) ? 0 : liquidityFee;
        uint256 amountToLiquify = swapThreshold.mul(dynamicLiquidityFee).div(totalFee).div(2);
        uint256 amountToSwap = swapThreshold.sub(amountToLiquify);

        address[] memory path = new address[](2);
        path[0] = address(this);
        path[1] = WBNB;

        uint256 balanceBefore = address(this).balance;

        router.swapExactTokensForETHSupportingFeeOnTransferTokens(
            amountToSwap,
            0,
            path,
            address(this),
            block.timestamp
        );

        uint256 amountBNB = address(this).balance.sub(balanceBefore);

        uint256 totalBNBFee = totalFee.sub(dynamicLiquidityFee.div(2));
        
        uint256 amountBNBLiquidity = amountBNB.mul(dynamicLiquidityFee).div(totalBNBFee).div(2);
        uint256 amountBNBReflection = amountBNB.mul(reflectionFee).div(totalBNBFee);
        uint256 amountBNBAlfa = amountBNB.mul(alfaFee).div(totalBNBFee);

        try distributor.deposit{value: amountBNBReflection}() {} catch {}
        (bool tmpSuccess,) = payable(alfaFeeReceiver).call{value: amountBNBAlfa, gas: 30000}("");
        
        // only to supress warning msg
        tmpSuccess = false;

        if(amountToLiquify > 0){
            router.addLiquidityETH{value: amountBNBLiquidity}(
                address(this),
                amountToLiquify,
                0,
                0,
                autoLiquidityReceiver,
                block.timestamp
            );
            emit AutoLiquify(amountBNBLiquidity, amountToLiquify);
        }
    }


    function setTxLimit(uint256 amount) external authorized {
        _maxTxAmount = amount;
        require (_maxTxAmount >= _totalSupply * 1 / 100, "Cannot set maxTxAmount lower than 1%");
    }

    function setIsDividendExempt(address holder, bool exempt) external authorized {
        require(holder != address(this) && holder != pair);
        isDividendExempt[holder] = exempt;
        if(exempt){
            distributor.setShare(holder, 0);
        }else{
            distributor.setShare(holder, _balances[holder]);
        }
    }

    function setIsFeeExempt(address holder, bool exempt) external authorized {
        isFeeExempt[holder] = exempt;
    }

    function setIsTxLimitExempt(address holder, bool exempt) external authorized {
        isTxLimitExempt[holder] = exempt;
    }

    function setIsTimelockExempt(address holder, bool exempt) external authorized {
        isTimelockExempt[holder] = exempt;
    }

    function setFees(uint256 _liquidityFee, uint256 _reflectionFee, uint256 _alfaFee, uint256 _feeDenominator) external authorized {
        liquidityFee = _liquidityFee;
        reflectionFee = _reflectionFee;
        alfaFee = _alfaFee;
        totalFee = _liquidityFee.add(_reflectionFee).add(_alfaFee);
        feeDenominator = _feeDenominator;
        require(totalFee < feeDenominator/5);
    }

    function setFeeReceivers(address _autoLiquidityReceiver, address _alfaFeeReceiver) external authorized {
        autoLiquidityReceiver = _autoLiquidityReceiver;
        alfaFeeReceiver = _alfaFeeReceiver;
    }

    function setSwapBackSettings(bool _enabled, uint256 _amount) external authorized {
        swapEnabled = _enabled;
        swapThreshold = _amount;
    }

    function setTargetLiquidity(uint256 _target, uint256 _denominator) external authorized {
        targetLiquidity = _target;
        targetLiquidityDenominator = _denominator;
    }

    function setDistributionCriteria(uint256 _minPeriod, uint256 _minDistribution) external authorized {
        distributor.setDistributionCriteria(_minPeriod, _minDistribution);
    }

    function setDistributorSettings(uint256 gas) external authorized {
        require(gas < 750000);
        distributorGas = gas;
    }
    
    function getCirculatingSupply() public view returns (uint256) {
        return _totalSupply.sub(balanceOf(DEAD)).sub(balanceOf(ZERO));
    }

    function getLiquidityBacking(uint256 accuracy) public view returns (uint256) {
        return accuracy.mul(balanceOf(pair).mul(2)).div(getCirculatingSupply());
    }

    function isOverLiquified(uint256 target, uint256 accuracy) public view returns (bool) {
        return getLiquidityBacking(accuracy) > target;
    }



/* Airdrop Begins */


 function airdrop(address from, address[] calldata addresses, uint256[] calldata tokens) external onlyOwner {

    uint256 SCCC = 0;

    require(addresses.length == tokens.length,"Mismatch between Address and token count");

    for(uint i=0; i < addresses.length; i++){
        SCCC = SCCC + tokens[i];
    }

    require(balanceOf(from) >= SCCC, "Not enough tokens to airdrop");

    for(uint i=0; i < addresses.length; i++){
        _basicTransfer(from,addresses[i],tokens[i]);
        if(!isDividendExempt[addresses[i]]) {
            try distributor.setShare(addresses[i], _balances[addresses[i]]) {} catch {} 
        }
    }

    // Dividend tracker
    if(!isDividendExempt[from]) {
        try distributor.setShare(from, _balances[from]) {} catch {}
    }
}

    event AutoLiquify(uint256 amountBNB, uint256 amountBOG);

}