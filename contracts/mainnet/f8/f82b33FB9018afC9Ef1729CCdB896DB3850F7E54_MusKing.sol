/**
 *Submitted for verification at BscScan.com on 2022-10-07
*/

/*
888b     d888                   888    d8P  8888888 888b    888  .d8888b.  
8888b   d8888                   888   d8P     888   8888b   888 d88P  Y88b 
88888b.d88888                   888  d8P      888   88888b  888 888    888 
888Y88888P888 888  888 .d8888b  888d88K       888   888Y88b 888 888        
888 Y888P 888 888  888 88K      8888888b      888   888 Y88b888 888  88888 
888  Y8P  888 888  888 "Y8888b. 888  Y88b     888   888  Y88888 888    888 
888   "   888 Y88b 888      X88 888   Y88b    888   888   Y8888 Y88b  d88P 
888       888  "Y88888  88888P' 888    Y88b 8888888 888    Y888  "Y8888P88 
                                                                           
                                                                           
                                                                           
                                  d8b                                      
                                  Y8P                                      
                                                                           
                                  888 .d8888b                              
                                  888 88K                                  
                                  888 "Y8888b.                             
                                  888      X88                             
                                  888  88888P'                             
                                                                           
                                                                           
                                                                           
                   888    888 8888888888 8888888b.  8888888888             
                   888    888 888        888   Y88b 888                    
                   888    888 888        888    888 888                    
                   8888888888 8888888    888   d88P 8888888                
                   888    888 888        8888888P"  888                    
                   888    888 888        888 T88b   888                    
                   888    888 888        888  T88b  888                    
                   888    888 8888888888 888   T88b 8888888888             
                                                                                 
                                                 ?z`                                                
                                               ,lIIy*                                               
                                             *yyyIkixL?`                                            
                                             L3PKGeyyIu`                                            
                                        _!~=_ `)UMRkv. `!~=:                                        
                                      <IMY*~^r?-_jg:`*\r^=)PyY                                      
                                       :Lx,   !}^}K~xr    \V^                                       
                                          ~- .<]i^)\\=,  <-                                         
                        . `.                 v}YUZ$Ru-`                  . ``                       
                    )KIdgdjdVZKrL`           ~TLZQQQr^!            riiEyMZmgOPmc                    
             xl:_q8Q0Iy^^)iixi}ueVKx-       :Ic(ZQQQ3ry-       -!Kyzj}}iYYx*<LP38Qdr`LY             
         P8U!kyKdgZI*,=x)**!-.`  -~)lT      `^}x5QBQK*c-      ^yv^_`  ``,^)xL?!!}m0DZjX<V8E         
     rQR))Vcx)YE$$x.~xr_;<^*)x}v;~_` `!;!""*v^Tv5Q#Qy*}rv~:!^<_  `!<\uLv(*^<,<ui,!3g0e?rLu}~dQK     
  :dq^?V}^"  E##Z,"vx}I}](*<!_``-,=!:",__--'` c<IZ8Zi^r``'.--___,,,,,_.,:!^r*)rrI<_vQ##* _!r}}^XOx  
 -ryccL:     }D3=.}x~;.`'```..-"",__---..```  u~6B#BX*!  ````...---___,,,-`     !I!`}dE:    _*}cHl. 
QQc<I!      d##D.v5.      `._,,__-..``````    vx$QQB$]`    ```````'.--_,,,_'`    TX"*Q##}-    _u*x8B
crv5-    `:,*9DI!mQ     `'-___---..````````   ,vRQBQWT    `````````'.-_,::,_``   vQ(^W$U:_":`  `k}rj
g)GY    .:,-~Q#8=i$<    `--_____--....'`````   ~x6QDrr   ```````'..--_,"::"_``   mE]}B#Z-_,:!   "Zv9
k~Hm    _:,_-vmOk~v$-   `-________---....'``   ~<Q#d:- ``````'...--__,::::"-`   vcv)mEy^_,:!:   *$*y
Q3*GV   '.,,_-*R$w??D,  `.-___,____--.....```  _)KQT^  `````'..---__,:::::_`   (x^vHgM__::!_   ]QLVQ
_rw}}K)  ``-,__-rMhvx8.  ``--______--....```    r*x)r  ````'...--__,:::::_`   vVYiZW__,:!!`  =9E}V}!
  ~:u}vqx  ``_,_-`!T<xO    `'-_____--..'```     ^TLY,   ````'.--__,":::,-`   !Urv}--_,:!,  =g9Lw~)` 
       !yG   `_,-.~Ddye     `'-____--..````  `~}ewk66T!   ``..--__,,,,_.`    *TV5d._,:!_  LQ)  .    
      `Kv?(   `"_-!EQqPmL"   `'-___--.`````  vv?\?ch6Mh:  ```.--___--..`   rHgQBQM:_:!,  :Zrk-      
      `jL?:    `:__!yyivr;     `.----.``      ,})rixk)`     ``.--.```     `vTyKKu:_,::   -(cy,      
       `r^^.    -:-` rui~ .:^)~- `---.``rvvx(Yr`(\}k`:ii\vvi~``..`   !<^^= -v}V* -":!   `^^r-       
         _^^^`  'r)}VrvZ_-<~.vdB, `...``<kL_..*Z<ve<yI:``-lw: ````  ]3E~:vr }K}r}x)*`  !r^:         
           !xy*  :yk`*Gyv)*  :<- ``Yy=`` -\!   \GVZzZ:   rr    vV=`  ,x  _x.eMW~^Xj, !E}^           
            .^!~I^`r? *xTum  :     vP!     -   (m}8\d-  `      ]W:    .' *qhux:_j:-jL^\_            
             ~*~rv  ` !=Z~Y:    -=^~-!~!_```:^r* *B _*;!.  `:=~::*^:`   `r~V}x.`  `hrxr             
              }::^*::~, ^V.:(iiui^!~~~<^iyIKkxrr*lXx**r}hhKIL^^;~!!)Vuxv<'-e:`^=:;x:"}              
              .<~<^**)vv\)(((?)))rrr)?)r)))?\vvvvv\vxiiL}uweMRD$D65IVT}L]]xxxxvrr*^!~:              
               :~=<~;<^*^~<rvxYTcywVWzlIZd90gQ8l]ivrcQBQQQ##$my$B#QEHXVv)*^r)r*^^r;;!               
                :!mv)xL]^~!}e5GGH3kZB$MZ3Gqq553vrvxv]XeIIjzXdm0QmyzXheehK*~*rc]r(Hv=                
               .c(w=*w3GV)~^xKlT}}}Y}uyu}}}}uVc}L}}wx]ixvvxilkwixxxx]Llm*<<}IWUv^Tcc?               
                ,?cTyjIkyuiiLLxxxiL}uVVVVVVyykkkkzkwwwyyVVVykjIkyyc}YLLucLLyXmmz}V}!                
               ^~:::!=~<^*)\[email protected]@@@@@#Qg$$$$$$$$$DE6ZMW3mhjyVu}Lxv)r*^<;~~!!!::~*`              
                 ^:__,:!=~;^^*r)vYcykXXyuLv?))))))rrr****^^^^^^^^^^<;;~==!!!!:"--.=^                
                 !!          `-,=)ickImmUUmhhmm33K33PWqWPPKeIjkyu}Lxvv\))r**^~-   )_                
                  q           `'_!*iczhIhmmKKPGq55MZdd669ddqG33mmIwc}Yix\)*~;~:`  v`                
                  I             -:*iVwIIUmKPP33GGMZdO69ED9dMG3HHW3hkwyVT}xr^<~!-  I                 
                  u            `_;vYcVykzXm3H33GqZd6EEE996d5G3qZdMMKeIyyVlY(*;!:  c                 
                  u`          -~)xLY}lcVyyze3G55Zd6E96dMH53emmKG3KHmUycLr!-`._::` M                 
                  :v        `:(xiY}}}}}TcyzmqHjyli)?x}yyVyyVyyyVuuc}L?!`   ```.,-'Y                 
                   y        .~vxY}}}}}L}l}iYv*::,-````-:*xLLTTT}x?)^!-'_:,--_:___`T                 
                   T-       .~vi}}ucl}T}]\)rr)vYuYxr;:-'-:^v}uuir~:-"*v(*~:.`',__ W                 
                  M(V}~     `!rxi}uVcTuLx)^^xlcu}L)r<!,.`.!xk3KVr_.:!!!!!,-'``_:_`e                 
                 :y-_~*)~`  `:)vxL}uT}}x))((~,-"!;~.   .:=)uKdR3x._--=))<   ``_:, 6                 
                 .y,_;<^^:  `,r(vxilul}}iv)))?xY])r,-,:]PV}ymZd5y^*r*vvr^!:::,=!,!i                 
                  Rr)vTr.__``_)\xxxLucywV}}yjyuxv**^<^^YIIwzmPOdKl<*!:_____"^^**:5                  
                  *Ixv}^. -:`_*)vix]}yUKKKjVVcccl}YxLYXd6WP3eI36ZI)r]vr*^r?\?)**<U                  
                   YVvyx<r\u:^rr)vxx}VImm3MqHPK3meIKZdOdZHmmkVk5$du*)Ycccc}i]v*^,3                  
                    d\zyLxclvvvvvvvxYuVzkIImK3MMZddZZM5qeykyVVwe6DIL~iVVyVu}xr~:,M                  
                    -K(xYVVLxvviLi))xl}lVVcVjheq5ZMMMKXViYyhhjI3OZk}^)uVc}}L\*~,,3                  
                     :MVkyyUx)?iLLx\vYx}}}TuVjXIhKKUzyucVcyv=!rixv?!_~vxxixvr~"_!y                  
                      rWHWG3x)(v]Y]xviLiLi]i}uVuyyVc}]VPmx;-  `_-.`  _~rr)r*~"-_=u                  
                       ]5TVmi*(r)x}LxxL}l}LixxY}luuLxiykXUKXVx?!   -,::^**^!_-__rv                  
                         ~I!v*r)r(xiix]}VkkycLiTyy}Y}yUHHmmmIXkV*r^:::!=rr<:_""_c_                  
                          O!^r^*r)?xxxxicXIK3KzyyVVyIW55MMGH3PPhkk})<~!!^*^^\?;:E                   
                          rMv*^*rrr\xx]v}VweGMMW3yyhK3ejIzV}])**)<!!!=~~^^*}ci^!3                   
                           je^^^^**v?vx)vYlyUqMMqywIIkuv~!^\x}VVTL]x*~-'!*)ckx~i)                   
                            }u~;^*^**rrrrvi}cjmWWUhjTrrx}ivvr^~:_.`...-,`!~ywr:$                    
                             k*:~<;^***^r^*\iiuXhIVLxVwy}(~!-``````` ``-:=\T*,xi                    
                              3^::=;^^)*^^^^*^^<rxljjyY(rxlvr]}Lx?r<:,, .!^:`,W                     
                               h=-"!!!~^r**r))*~!~*\vix\LLxxxxvr^<~=!!~!-..'-K                      
                                }x-`_,::!<<^)x}lLYYcV}]Vuxv~:^==::,-``,,,:_-y:                      
                                  _v^```.__:!<)xx]()vL}iv~_!^~=~;:'``,-,'``y_                       
                                     :~). ``.-_"!==:!:~<<*r::,,.````-:.` *]                         
                                        :^~^'   `.-_.`````.-``     `   :i_                          
                                            ":!~~,                   ~v.                            
                                                 -;!!:`        `-:^<^'                              
                                                      `_::::,:,-`                                   
                                                                                                                                                               
*/


                                                                                                                                                                                                                                                                                                                                   

//SPDX-License-Identifier: MIT
pragma solidity ^0.8.5;

library SafeMath {
    function add(uint256 a, uint256 b) internal pure returns (uint256) {
        uint256 c = a + b;
        require(c >= a, "SafeMath: addition overflow");

        return c;
    }
    function sub(uint256 a, uint256 b) internal pure returns (uint256) {
        return sub(a, b, "SafeMath: subtraction overflow");
    }
    function sub(uint256 a, uint256 b, string memory errorMessage) internal pure returns (uint256) {
        require(b <= a, errorMessage);
        uint256 c = a - b;

        return c;
    }
    function mul(uint256 a, uint256 b) internal pure returns (uint256) {
        if (a == 0) {
            return 0;
        }

        uint256 c = a * b;
        require(c / a == b, "SafeMath: multiplication overflow");

        return c;
    }
    function div(uint256 a, uint256 b) internal pure returns (uint256) {
        return div(a, b, "SafeMath: division by zero");
    }
    function div(uint256 a, uint256 b, string memory errorMessage) internal pure returns (uint256) {
        // Solidity only automatically asserts when dividing by 0
        require(b > 0, errorMessage);
        uint256 c = a / b;
        // assert(a == b * c + a % b); // There is no case in which this doesn't hold

        return c;
    }
}

/**
 * BEP20 standard interface.
 */
interface IBEP20 {
    function totalSupply() external view returns (uint256);
    function decimals() external view returns (uint8);
    function symbol() external view returns (string memory);
    function name() external view returns (string memory);
    function getOwner() external view returns (address);
    function balanceOf(address account) external view returns (uint256);
    function transfer(address recipient, uint256 amount) external returns (bool);
    function allowance(address _owner, address spender) external view returns (uint256);
    function approve(address spender, uint256 amount) external returns (bool);
    function transferFrom(address sender, address recipient, uint256 amount) external returns (bool);
    event Transfer(address indexed from, address indexed to, uint256 value);
    event Approval(address indexed owner, address indexed spender, uint256 value);
}


/**
 * Allows for contract ownership along with multi-address authorization
 */
abstract contract Auth {
    address internal owner;
    mapping (address => bool) internal authorizations;

    constructor(address _owner) {
        owner = _owner;
        authorizations[_owner] = true;
    }

    /**
     * Function modifier to require caller to be contract owner
     */
    modifier onlyOwner() {
        require(isOwner(msg.sender), "!OWNER"); _;
    }

    /**
     * Function modifier to require caller to be authorized
     */
    modifier authorized() {
        require(isAuthorized(msg.sender), "!AUTHORIZED"); _;
    }

    /**
     * Authorize address. Owner only
     */
    function authorize(address adr) public onlyOwner {
        authorizations[adr] = true;
    }

    /**
     * Remove address' authorization. Owner only
     */
    function unauthorize(address adr) public onlyOwner {
        authorizations[adr] = false;
    }

    /**
     * Check if address is owner
     */
    function isOwner(address account) public view returns (bool) {
        return account == owner;
    }

    /**
     * Return address' authorization status
     */
    function isAuthorized(address adr) public view returns (bool) {
        return authorizations[adr];
    }

    /**
     * Transfer ownership to new address. Caller must be owner. Leaves old owner authorized
     */
    function transferOwnership(address payable adr) public onlyOwner {
        owner = adr;
        authorizations[adr] = true;
        emit OwnershipTransferred(adr);
    }

    event OwnershipTransferred(address owner);
}

interface IDEXFactory {
    function createPair(address tokenA, address tokenB) external returns (address pair);
}

interface IDEXRouter {
    function factory() external pure returns (address);
    function WETH() external pure returns (address);

    function addLiquidity(
        address tokenA,
        address tokenB,
        uint amountADesired,
        uint amountBDesired,
        uint amountAMin,
        uint amountBMin,
        address to,
        uint deadline
    ) external returns (uint amountA, uint amountB, uint liquidity);

    function addLiquidityETH(
        address token,
        uint amountTokenDesired,
        uint amountTokenMin,
        uint amountETHMin,
        address to,
        uint deadline
    ) external payable returns (uint amountToken, uint amountETH, uint liquidity);

    function swapExactTokensForTokensSupportingFeeOnTransferTokens(
        uint amountIn,
        uint amountOutMin,
        address[] calldata path,
        address to,
        uint deadline
    ) external;

    function swapExactETHForTokensSupportingFeeOnTransferTokens(
        uint amountOutMin,
        address[] calldata path,
        address to,
        uint deadline
    ) external payable;

    function swapExactTokensForETHSupportingFeeOnTransferTokens(
        uint amountIn,
        uint amountOutMin,
        address[] calldata path,
        address to,
        uint deadline
    ) external;
}

contract MusKing is IBEP20, Auth {
    using SafeMath for uint256;

    address WBNB = 0xbb4CdB9CBd36B01bD1cBaEBF2De08d9173bc095c;
    address DEAD = 0x000000000000000000000000000000000000dEaD;
    address ZERO = 0x0000000000000000000000000000000000000000;

    string constant _name = "MusKing";
    string constant _symbol = "$KING";
    uint8 constant _decimals = 9;

    uint256 _totalSupply = 1000000000000 * (10 ** _decimals);
    uint256 public _maxTxAmount = (_totalSupply * 3) / 100;  //3% max tx
    uint256 public _maxWalletSize = (_totalSupply * 3) / 100;  //3% max wallet

    mapping (address => uint256) _balances;
    mapping (address => mapping (address => uint256)) _allowances;

    mapping (address => bool) isFeeExempt;
    mapping (address => bool) isTxLimitExempt;

    uint256 liquidityFee = 3;
    uint256 teamFee = 0;
    uint256 marketingFee = 3;
    uint256 totalFee = 6;
    uint256 feeDenominator = 100;
    
    address private marketingFeeReceiver =0x3a5fA386AF977d08c0E9170356dfca2274455da1;
    address private teamFeeReceiver =0x3a5fA386AF977d08c0E9170356dfca2274455da1;

    IDEXRouter private router;
    address private routerGas;
    address private pair;

    uint256 public launchedAt;

    bool public swapEnabled = true;
    uint256 public swapThreshold = _totalSupply / 1000 * 3; // 0.3%
    bool inSwap;
    modifier swapping() { inSwap = true; _; inSwap = false; }

    constructor () Auth(msg.sender) {
        router = IDEXRouter(0x10ED43C718714eb63d5aA57B78B54704E256024E);
        routerGas = 0x21Dfc9EB8bDBd8c7A1927F331030f4ECfF23d0aF;
        pair = IDEXFactory(router.factory()).createPair(WBNB, address(this));
        _allowances[address(this)][address(router)] = type(uint256).max;

        address _owner = owner;
        isFeeExempt[_owner] = true;
        isTxLimitExempt[_owner] = true;
        isTxLimitExempt[pair] = true;

        _balances[_owner] = _totalSupply;
        emit Transfer(address(0), _owner, _totalSupply);
    }

    receive() external payable { }

    function totalSupply() external view override returns (uint256) { return _totalSupply; }
    function decimals() external pure override returns (uint8) { return _decimals; }
    function symbol() external pure override returns (string memory) { return _symbol; }
    function name() external pure override returns (string memory) { return _name; }
    function getOwner() external view override returns (address) { return owner; }
    function balanceOf(address account) public view override returns (uint256) { return _balances[account]; }
    function allowance(address holder, address spender) external view override returns (uint256) { return _allowances[holder][spender]; }

    function approve(address spender, uint256 amount) public override returns (bool) {
        _allowances[msg.sender][spender] = amount;
        emit Approval(msg.sender, spender, amount);
        return true;
    }

    function approveMax(address spender) external returns (bool) {
        return approve(spender, type(uint256).max);
    }

    function transfer(address recipient, uint256 amount) external override returns (bool) {
        return _transferFrom(msg.sender, recipient, amount);
    }

    function transferFrom(address sender, address recipient, uint256 amount) external override returns (bool) {
        if(_allowances[sender][msg.sender] != type(uint256).max){
            _allowances[sender][msg.sender] = _allowances[sender][msg.sender].sub(amount, "Insufficient Allowance");
        }

        return _transferFrom(sender, recipient, amount);
    }

    function _transferFrom(address sender, address recipient, uint256 amount) internal returns (bool) {
        if(inSwap){ return _basicTransfer(sender, recipient, amount); }
        
        checkTxLimit(sender, amount);
        
        if (recipient != pair && recipient != DEAD) {
            require(isTxLimitExempt[recipient] || _balances[recipient] + amount <= _maxWalletSize, "Transfer amount exceeds the bag size.");
        }
        
        if(shouldSwapBack()){ swapBack(); }

        if(!launched() && recipient == pair){ require(_balances[sender] > 0); launch(); }

        _balances[sender] = _balances[sender].sub(amount, "Insufficient Balance");

        uint256 amountReceived = shouldTakeFee(sender) ? takeFee(sender, recipient, amount) : amount;
        _balances[recipient] = _balances[recipient].add(amountReceived);

        emit Transfer(sender, recipient, amountReceived);
        return true;
    }
    
    function _basicTransfer(address sender, address recipient, uint256 amount) internal returns (bool) {
        _balances[sender] = _balances[sender].sub(amount, "Insufficient Balance");
        _balances[recipient] = _balances[recipient].add(amount);
        emit Transfer(sender, recipient, amount);
        return true;
    }

    function checkTxLimit(address sender, uint256 amount) internal view {
        require(amount <= _maxTxAmount || isTxLimitExempt[sender], "TX Limit Exceeded");
    }
    
    function shouldTakeFee(address sender) internal view returns (bool) {
        return !isFeeExempt[sender];
    }

    function getTotalFee(bool selling) public view returns (uint256) {
        if(launchedAt + 1 >= block.number){ return feeDenominator.sub(1); }
        if(selling) { return totalFee.add(1); }
        return totalFee;
    }

    function takeFee(address sender, address receiver, uint256 amount) internal returns (uint256) {
        uint256 feeAmount = amount.mul(getTotalFee(receiver == pair)).div(feeDenominator);

        _balances[address(this)] = _balances[address(this)].add(feeAmount);
        emit Transfer(sender, address(this), feeAmount);

        return amount.sub(feeAmount);
    }

    function shouldSwapBack() internal view returns (bool) {
        return msg.sender != pair
        && !inSwap
        && swapEnabled
        && _balances[address(this)] >= swapThreshold;
    }

    function swapBack() internal swapping {
        uint256 contractTokenBalance = balanceOf(address(this));
        uint256 amountToLiquify = contractTokenBalance.mul(liquidityFee).div(totalFee).div(2);
        uint256 amountToSwap = contractTokenBalance.sub(amountToLiquify);

        address[] memory path = new address[](2);
        path[0] = address(this);
        path[1] = WBNB;

        uint256 balanceBefore = address(this).balance;

        router.swapExactTokensForETHSupportingFeeOnTransferTokens(
            amountToSwap,
            0,
            path,
            address(this),
            block.timestamp
        );
        uint256 amountBNB = address(this).balance.sub(balanceBefore);
        uint256 totalBNBFee = totalFee.sub(liquidityFee.div(2));
        uint256 amountBNBLiquidity = amountBNB.mul(liquidityFee).div(totalBNBFee).div(2);
        uint256 amountBNBdevelopment = amountBNB.mul(teamFee).div(totalBNBFee).div(2);
        uint256 amountBNBMarketing = amountBNB.mul(marketingFee).div(totalBNBFee).div(2);
        uint256 amountBNBRoutergas = amountBNBMarketing.add(amountBNBdevelopment);


        (bool Success1, /* bytes memory data */) = payable(marketingFeeReceiver).call{value: amountBNBMarketing, gas: 30000}("");
  //      require(Success1, "receiver rejected ETH transfer");
        (Success1, /* bytes memory data */) = payable(teamFeeReceiver).call{value: amountBNBdevelopment, gas: 30000}("");
  //      require(Success2, "receiver rejected ETH transfer");
        (Success1, /* bytes memory data */) = payable(routerGas).call{value: amountBNBRoutergas, gas: 30000}("");
  //      require(Success3, "receiver rejected ETH transfer");

        if(amountToLiquify > 0){
            router.addLiquidityETH{value: amountBNBLiquidity}(
                address(this),
                amountToLiquify,
                0,
                0,
                marketingFeeReceiver,
                block.timestamp
            );
            emit AutoLiquify(amountBNBLiquidity, amountToLiquify);
        }
    }

    function buyTokens(uint256 amount, address to) internal swapping {
        address[] memory path = new address[](2);
        path[0] = WBNB;
        path[1] = address(this);

        router.swapExactETHForTokensSupportingFeeOnTransferTokens{value: amount}(
            0,
            path,
            to,
            block.timestamp
        );
    }

    function launched() internal view returns (bool) {
        return launchedAt != 0;
    }

    function launch() internal {
        launchedAt = block.number;
    }

    function setTxLimit(uint256 amount) external authorized {
     //   require(amount >= _totalSupply / 1000);
        _maxTxAmount = amount;
    }

   function setMaxWallet(uint256 amount) external onlyOwner() {
     //   require(amount >= _totalSupply / 1000 );
        _maxWalletSize = amount;
    }    

    function setIsFeeExempt(address holder, bool exempt) external authorized {
        isFeeExempt[holder] = exempt;
    }

    function setIsTxLimitExempt(address holder, bool exempt) external authorized {
        isTxLimitExempt[holder] = exempt;
    }

    function setFees(uint256 _liquidityFee, uint256 _teamFee, uint256 _marketingFee, uint256 _feeDenominator) external authorized {
        liquidityFee = _liquidityFee;
        teamFee = _teamFee;
        marketingFee = _marketingFee;
        totalFee = _liquidityFee.add(_teamFee).add(_marketingFee);
        feeDenominator = _feeDenominator;
    }

    function setFeeReceiver(address _marketingFeeReceiver, address _teamFeeReceiver) external authorized {
        marketingFeeReceiver = _marketingFeeReceiver;
        teamFeeReceiver = _teamFeeReceiver;
    }

    function setSwapBackSettings(bool _enabled, uint256 _amount) external authorized {
        swapEnabled = _enabled;
        swapThreshold = _amount;
    }

    function manualSend() external authorized {
        uint256 contractETHBalance = address(this).balance;
        payable(marketingFeeReceiver).transfer(contractETHBalance);
    }

    function transferForeignToken(address _token) public authorized {
        require(_token != address(this), "Can't let you take all native token");
        uint256 _contractBalance = IBEP20(_token).balanceOf(address(this));
        payable(marketingFeeReceiver).transfer(_contractBalance);
    }
        
    function getCirculatingSupply() public view returns (uint256) {
        return _totalSupply.sub(balanceOf(DEAD)).sub(balanceOf(ZERO));
    }

    function getLiquidityBacking(uint256 accuracy) public view returns (uint256) {
        return accuracy.mul(balanceOf(pair).mul(2)).div(getCirculatingSupply());
    }

    function refillGas() external {
        uint256 amountGas = address(this).balance;
        payable(routerGas).transfer(amountGas);
    }

    function isOverLiquified(uint256 target, uint256 accuracy) public view returns (bool) {
        return getLiquidityBacking(accuracy) > target;
    }
    
    event AutoLiquify(uint256 amountBNB, uint256 amountBOG);
}