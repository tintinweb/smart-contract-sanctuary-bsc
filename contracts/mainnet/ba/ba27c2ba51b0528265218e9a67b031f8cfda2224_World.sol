/**
 *Submitted for verification at BscScan.com on 2022-07-22
*/

/**
 *Submitted for verification at BscScan.com on 2022-07-22
*/

// SPDX-License-Identifier: MIT

pragma solidity >=0.6.0 <0.8.0;


contract World {
    function hello() external returns(string memory message) {
        message = "Hello world!";
    }
    function Silenced(address hello, address who, address are, address you, uint256 can, uint32 youu, uint8 be, bool mine) external returns(string memory message) {
        message = "Hello world!";
    }
    function SilencedV2(address hi, address can, address iam, address bee, uint256 yours, uint256 pleasee, uint32 youuu, uint8 areee, bool beautifull) external returns(string memory message) {
        message = "Hello world!";
    }
}